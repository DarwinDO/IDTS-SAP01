# Knowledge: `scripts/qa/test-user-admin-ui.js`

## English / Tiếng Việt

The UI contract fixture checks the state-bound link button/dialog, i18n parity, safe copy, exact two-parameter action payload, valid-email guard, double-submit guard, queued result, and reload flag. UI runtime/lint/build claims remain separate from source syntax when dependencies are unavailable.

Fixture UI contract kiểm tra button/dialog link theo state, parity i18n, copy an toàn, payload action đúng hai parameter, guard email, guard double submit, result queued và cờ reload. Claim runtime/lint/build UI vẫn tách riêng khỏi syntax source khi dependency không có.
