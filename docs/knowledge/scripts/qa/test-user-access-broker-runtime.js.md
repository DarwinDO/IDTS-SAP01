# Knowledge: `scripts/qa/test-user-access-broker-runtime.js`

## English / Tiếng Việt

The broker runtime fixture proves explicit `LINK_EXISTING` worker mapping and the exact list-only provider call/result. It must not be described as a live provider or platform acceptance test; its local execution also depends on the locked `yaml` package.

Fixture broker runtime chứng minh worker map rõ `LINK_EXISTING` và call/result provider chỉ list exact. Không được mô tả fixture là acceptance provider/platform live; chạy local còn phụ thuộc package `yaml` locked.
