# Knowledge: `app/bug-management-ui/webapp/ext/login/ProfileShell.js`

> **Ownership / debug anchor:** DatDT owns the signed-in profile shell (backup: SangVN). It shows safe session profile data; backend authorization remains authoritative after every click.
> **Ownership / điểm debug:** DatDT sở hữu profile shell khi đã login (backup: SangVN). Nó hiển thị profile session an toàn; backend authorization vẫn là lớp quyết định sau mỗi click.

## English

### What this file is for

`ProfileShell.js` renders the SAP Fiori-style signed-in profile menu for IDTS. It is the main implementation file for `IDTS-53`.

It creates a small profile button, opens a `ResponsivePopover`, shows the signed-in user's display name, email, role, and session expiry, and provides a visible Sign Out action.

### Beginner explanation

The IDTS main screens are generated by Fiori Elements. We should not modify generated toolbar internals just to add a user menu. This file instead renders a small profile shell into a stable host from `index.html`.

This is safer because:

- `index.html` owns the host element.
- `Component.js` calls this module only after UI5 is available.
- `auth-guard.js` still handles authentication before UI5 starts.
- The profile menu uses normal SAPUI5 controls instead of raw DOM manipulation.

### Flow in IDTS

1. The user logs in through `login.html`.
2. `login-page.js` stores a safe public user profile in `sessionStorage`.
3. `index.html` starts the protected Fiori app.
4. `Component.js` or a standalone page calls a `ProfileShell` entry point.
5. `ProfileShell` reads the current user through `window.idtsCurrentUser()`.
6. It either renders a profile button into `#idtsProfileShellHost` or returns a header button for a standalone SAPUI5 page such as the dashboard.
7. The user opens the popover to see name/email/role/session expiry.
8. The user presses Sign Out.
9. `ProfileShell` calls `window.idtsLogout()`, which clears the browser session and redirects to `login.html`.

### Important source anchors

| Location | IDTS concept | Impact if broken | Must check together |
| --- | --- | --- | --- |
| `PROFILE_HOST_ID = "idtsProfileShellHost"` | Stable render target | Profile menu cannot render if the host id changes. | `index.html`, `css/idts-shell.css` |
| `createHeaderButton()` | Reusable profile action for standalone pages | Dashboard/profile entry can overlap page actions or disappear if the standalone-page integration is removed. | `dashboard-page.js`, browser smoke |
| `currentUser()` | Signed-in user lookup | Popover cannot show the correct account if stored profile shape changes. | `auth-guard.js`, `login-page.js`, `srv/auth.js` |
| `new Button({ icon: "sap-icon://person-placeholder" ... })` | Profile menu entry | User may not notice the signed-in account control. | `css/idts-shell.css`, browser smoke |
| `new ResponsivePopover(...)` | SAP Fiori profile popover pattern | Profile details/sign-out may be unreadable or unavailable. | UI5 MCP/linter |
| `new ObjectStatus({ text: role, state: "Information" })` | Role visibility | User cannot easily confirm whether they are PM/Tester/Developer. | `srv/auth.js publicUser()` |
| `window.idtsLogout()` | Sign Out action | Session may remain active if logout is not called. | `auth-guard.js`, `srv/auth.js` |

### Cross-folder impact

- `srv/auth.js` returns the public user profile that provides `displayName`, `email`, `role_code`, and `roleName`.
- `db/schema.cds` defines `Users` and `UserRoles`, which are the source of the displayed role/profile data.
- `auth-guard.js` exposes `window.idtsCurrentUser()` and `window.idtsLogout()` consumed by this module.
- `index.html` provides the host element and CSS link needed by this module.
- `dashboard-page.js` can request a profile button directly for `sap.m.Page` header content when a fixed overlay would clash with page actions.

### Safe editing checklist

- Do not read or display bearer tokens.
- Do not query internal Fiori Elements generated control IDs.
- Keep profile data limited to safe public fields: name, email, role, session expiry.
- If `srv/auth.js publicUser()` changes shape, update `currentUser()` usage and browser smoke.
- If logout behavior changes, verify refresh-after-logout cannot access protected app content.

## IDTS-58 follow-up notes

### English

IDTS-58 added `createHeaderButton()` so the same profile popover can be reused in standalone pages. The dashboard now uses this helper in `headerContent` instead of relying on the fixed overlay host, which removed the overlap between the profile trigger and the `Refresh` button.

The fixed-host render path is still important for the main Fiori Elements shell (`index.html`). So a safe change must keep both integration modes working: host-based render for the generated shell and direct button creation for standalone SAPUI5 pages.

### Vietnamese

IDTS-58 them `createHeaderButton()` de cung mot profile popover duoc dung lai tren cac trang standalone. Dashboard hien dung helper nay trong `headerContent` thay vi dua vao fixed overlay host, nen da bo duoc viec profile trigger de len nut `Refresh`.

Duong render qua fixed host van quan trong cho Fiori Elements shell chinh (`index.html`). Vi vay moi thay doi an toan phai giu duoc ca hai cach tich hop: render qua host cho generated shell va tao truc tiep button cho trang SAPUI5 standalone.

## Vietnamese

### File này dùng để làm gì

`ProfileShell.js` render profile menu theo phong cách SAP Fiori cho IDTS sau khi user đã đăng nhập. Đây là file implement chính của `IDTS-53`.

File này tạo một profile button nhỏ, mở `ResponsivePopover`, hiển thị display name, email, role, thời hạn session của user đang đăng nhập, và cung cấp action Sign Out rõ ràng.

### Giải thích cho người mới

Các màn hình chính của IDTS được Fiori Elements generate. Ta không nên sửa toolbar nội bộ generated chỉ để thêm user menu. File này chọn cách an toàn hơn: render một profile shell nhỏ vào host ổn định từ `index.html`.

Cách này an toàn hơn vì:

- `index.html` sở hữu host element.
- `Component.js` chỉ gọi module này sau khi UI5 đã sẵn sàng.
- `auth-guard.js` vẫn xử lý authentication trước khi UI5 start.
- Profile menu dùng SAPUI5 controls bình thường thay vì thao tác DOM thô.

### Flow hoạt động trong IDTS

1. User đăng nhập qua `login.html`.
2. `login-page.js` lưu public user profile an toàn vào `sessionStorage`.
3. `index.html` start protected Fiori app.
4. `Component.js` gọi `ProfileShell.init()`.
5. `ProfileShell` đọc user hiện tại qua `window.idtsCurrentUser()`.
6. Nó render profile button vào `#idtsProfileShellHost`.
7. User mở popover để xem name/email/role/session expiry.
8. User bấm Sign Out.
9. `ProfileShell` gọi `window.idtsLogout()`, hàm này xóa browser session và redirect về `login.html`.

### Các điểm source quan trọng

| Vị trí | Khái niệm IDTS | Ảnh hưởng nếu sai | Phải kiểm tra cùng |
| --- | --- | --- | --- |
| `PROFILE_HOST_ID = "idtsProfileShellHost"` | Target render ổn định | Profile menu không render nếu host id đổi. | `index.html`, `css/idts-shell.css` |
| `currentUser()` | Lấy user đang đăng nhập | Popover không hiện đúng account nếu shape profile lưu trong browser đổi. | `auth-guard.js`, `login-page.js`, `srv/auth.js` |
| `new Button({ icon: "sap-icon://person-placeholder" ... })` | Điểm vào profile menu | User có thể không nhận ra control account đang đăng nhập. | `css/idts-shell.css`, browser smoke |
| `new ResponsivePopover(...)` | Pattern profile popover SAP Fiori | Chi tiết profile/sign-out có thể khó đọc hoặc không mở được. | UI5 MCP/linter |
| `new ObjectStatus({ text: role, state: "Information" })` | Hiển thị role | User khó biết mình đang là PM/Tester/Developer. | `srv/auth.js publicUser()` |
| `window.idtsLogout()` | Action Sign Out | Session có thể còn active nếu không gọi logout. | `auth-guard.js`, `srv/auth.js` |

### Liên kết với folder khác

- `srv/auth.js` trả public user profile gồm `displayName`, `email`, `role_code`, và `roleName`.
- `db/schema.cds` định nghĩa `Users` và `UserRoles`, là nguồn dữ liệu profile/role được hiển thị.
- `auth-guard.js` expose `window.idtsCurrentUser()` và `window.idtsLogout()` cho module này dùng.
- `index.html` cung cấp host element và CSS link mà module này cần.

### Checklist sửa file an toàn

- Không đọc hoặc hiển thị bearer token.
- Không query internal generated control ID của Fiori Elements.
- Chỉ hiển thị public profile fields an toàn: name, email, role, session expiry.
- Nếu `srv/auth.js publicUser()` đổi shape, cập nhật cách dùng `currentUser()` và browser smoke.
- Nếu đổi logout behavior, verify refresh sau logout không vào được protected app content.

## Symbol walkthrough / Walkthrough theo symbol (2026-07-18)

**English.** `Component.init()` calls `ProfileShell.init()` → it finds the stable HTML host and safe current user → `render()` places the button → `createProfileButton()` creates avatar/popover/sign-out → sign-out calls `window.idtsLogout()` from auth guard. `safeUserText`, `initialsFrom`, and `formatExpiry` only prepare display values. Watch `host`, safe `user`, and the rendered flag; backend authorization remains independent of this display.

**Tiếng Việt.** `Component.init()` gọi `ProfileShell.init()` → tìm HTML host ổn định và current user an toàn → `render()` đặt button → `createProfileButton()` tạo avatar/popover/sign-out → sign-out gọi `window.idtsLogout()` từ auth guard. `safeUserText`, `initialsFrom`, `formatExpiry` chỉ chuẩn bị dữ liệu hiển thị. Quan sát `host`, safe `user` và cờ đã render; authorization backend hoàn toàn độc lập với phần hiển thị này.

## IDTS-113 update - platform-managed session label

### English

When the current safe profile reports `authMode: "xsuaa"`, the popover shows
`Session managed by SAP BTP` instead of a custom token expiry. Sign Out still
calls `window.idtsLogout()`, but the guard routes it to AppRouter `/do/logout`.
The shell never reads or displays a JWT.

### Vietnamese

Khi safe profile co `authMode: "xsuaa"`, popover hien `Session managed by SAP
BTP` thay vi custom token expiry. Sign Out van goi `window.idtsLogout()`, nhung
guard se chuyen den AppRouter `/do/logout`. Profile shell khong doc hay hien
JWT.
