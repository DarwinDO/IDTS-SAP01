# DonHV Learning Ownership

## English

Primary files: `db/schema.cds` and all runtime files under `srv/`. Primary flows: authentication, create/lifecycle, notifications/email, and AI assistance. Backup role: SangVN's collaboration flow and NhanT's QA diagnosis.

Before PASS, DonHV must trace a request through service definition, handler wiring, helper/action module, transaction side effect, and the relevant entity or external boundary.

## Vietnamese

File primary của DonHV: `db/schema.cds` và mọi runtime file trong `srv/`. Flow primary: authentication, create/lifecycle, notifications/email và AI assistance. Vai trò backup: collaboration của SangVN và QA diagnosis của NhanT.

Trước khi PASS, DonHV phải trace được request qua service definition, handler wiring, helper/action module, transaction side effect và entity hoặc external boundary liên quan.
