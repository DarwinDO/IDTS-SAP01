# My Notifications compact Bug title row — 2026-09-03

## English

DonHV confirmed `BUG-0017` has a stored title and description, then selected a compact notification contract that displays only Bug number and title. The earlier popover showed only event metadata because the caller-only DTO did not carry Bug context; the old row also rendered a redundant category icon and allowed horizontal overflow.

The fix removes the row icon, adds bounded `bugNumber`/`bugTitle` fields to `NotificationSummary`, hydrates them in the existing single bounded source query, renders `BUG-number — title` as the Bug primary line, omits Bug description, uses compact native margins, and disables horizontal scrolling. Access notifications retain their localized title/summary. No custom CSS or dependency is added.

TDD evidence: icon dependency RED then GREEN; DTO field RED then GREEN; horizontal-scroll/layout RED then GREEN. My Notifications UI/service/model/backfill/events/scheduled/digest, User Access notification, auth foundation/XSUAA `39/39`, cross-app UI contracts, configured UI lint/build, CAP EDMX/HANA compile, secret/rules/depth gates, AI DevKit lint, and `git diff --check` passed. CAP emitted only the existing attachment annotation warning. Codex Security diff scan `a9f4249f-6de2-4a08-91ab-0b089943b2a3` completed with zero findings across nine reviewed surfaces; TAC was unavailable and the parent-only fallback was used. PR, merge, CAP/UI rollout, live Browser Control acceptance, and cleanup evidence will be appended at their actual boundaries.

Source PR [#381](https://github.com/DarwinDO/IDTS-SAP01/pull/381) passed `qa-depth-gate` and merged into `dev` at `391e500f9f180dd3039bc6a2a276d45472d76a63`. The deployed CAP artifact was staged as package `fb4a7dbd-9f79-4842-9407-b0089e096f7e`, assigned as droplet `2382e503-e645-4ba2-bd03-e1b2c2b2ff5f`, and preserves rollback droplet `250a8a7e-4b57-4f58-a8ce-f808abc3bb43`. The dedicated UI content MTAR SHA-256 is `8838D2BA5CD0EB5A7A1CA0B60C000427D76EFE6C1AE005F42A30042796BF1087`; operation `4d78c12b-a78e-11f1-b693-eeee0a88f756` finished successfully. Final readback reports CAP/AppRouter `1/1`, health/readiness/Web `200`, protected anonymous API `401`, no active MTA operation, and `DEMO READY`.

One attempted selective deployment of a full MTAR, operation `82ad0357-a78d-11f1-866f-eeee0a815b5e`, aborted during HTML5 content validation with `manifest.json not found`. It did not replace the running CAP droplet. Recovery used the bounded CAP package/stage/droplet path and a dedicated one-module UI MTAR; no DB/HDI/schema/data/provider/user/role/email/scheduler mutation occurred. The generated CAP artifact was corrected locally from `fast-xml-parser` `5.9.3` to `5.11.1`, reducing its production audit from one high plus seven moderate findings to seven moderate and zero high/critical findings, without changing repository manifests or lockfiles. Cloud Foundry also warned that `cflinuxfs4` is scheduled for future retirement in May 2027; it is follow-up platform maintenance, not a blocker for this release.

Signed-in Edge Browser Control accepted the live empty state without mutating data: the `My Notifications` dialog measured `clientWidth=480` and `scrollWidth=480`, proving no horizontal overflow. The available DonHV inbox contained zero notifications, so no synthetic notification was created and no live claim is made for a populated Bug row. The exact deployed UI artifact independently contains Bug UI version `0.0.10`, `bugTitle`, no `sap/ui/core/Icon` dependency, and disabled horizontal scrolling; deployed CAP metadata exposes `bugNumber`/`bugTitle` and deliberately no Bug description. Therefore an existing Bug notification renders compactly as `BUG-0017 — test notification bug` when viewed by its recipient, while the current DonHV session truthfully exercises the empty state only.

After the docs closure merged, the final independent check found HANA readiness temporarily at HTTP 503 while CAP/AppRouter remained `1/1`. One supported `btp:demo:prepare` requested the existing HANA service start and restarted CAP once after the database returned HTTP 200. A separate check-only rerun then returned `DEMO READY` with the same current CAP droplet `2382e503-e645-4ba2-bd03-e1b2c2b2ff5f`; no redeploy, schema, seed or business-data mutation occurred.

## Tiếng Việt

DonHV xác nhận `BUG-0017` có title và description đã lưu, sau đó chọn contract notification gọn chỉ hiển thị Bug number và title. Popover trước chỉ hiện metadata event vì DTO caller-only chưa mang context Bug; row cũ còn có icon category dư và cho phép overflow ngang.

Fix xóa icon row, thêm field có giới hạn `bugNumber`/`bugTitle` vào `NotificationSummary`, hydrate trong cùng source query bounded hiện có, render `BUG-number — title` làm dòng chính, không đưa Bug description, dùng margin compact native và tắt horizontal scrolling. Notification Access giữ title/summary đã localize. Không thêm custom CSS hoặc dependency.

Evidence TDD: dependency icon RED rồi GREEN; field DTO RED rồi GREEN; horizontal-scroll/layout RED rồi GREEN. Contract My Notifications UI/service/model/backfill/events/scheduled/digest, User Access notification, auth foundation/XSUAA `39/39`, cross-app UI, UI lint/build theo config, CAP EDMX/HANA compile, gate secret/rules/depth, AI DevKit lint và `git diff --check` đều PASS. CAP chỉ có warning attachment annotation đã tồn tại. Codex Security diff scan `a9f4249f-6de2-4a08-91ab-0b089943b2a3` hoàn tất với zero finding trên chín surface; TAC không khả dụng và dùng fallback parent-only. Evidence PR, merge, rollout CAP/UI, acceptance Browser Control live và cleanup sẽ được bổ sung đúng từng boundary thực tế.

Source PR [#381](https://github.com/DarwinDO/IDTS-SAP01/pull/381) PASS `qa-depth-gate` và merge vào `dev` tại `391e500f9f180dd3039bc6a2a276d45472d76a63`. Artifact CAP được stage thành package `fb4a7dbd-9f79-4842-9407-b0089e096f7e`, assign thành droplet `2382e503-e645-4ba2-bd03-e1b2c2b2ff5f`, đồng thời giữ droplet rollback `250a8a7e-4b57-4f58-a8ce-f808abc3bb43`. MTAR UI content riêng có SHA-256 `8838D2BA5CD0EB5A7A1CA0B60C000427D76EFE6C1AE005F42A30042796BF1087`; operation `4d78c12b-a78e-11f1-b693-eeee0a88f756` hoàn tất. Readback cuối cho CAP/AppRouter `1/1`, health/readiness/Web `200`, anonymous protected API `401`, không còn MTA operation active và `DEMO READY`.

Một lần deploy chọn lọc từ full MTAR, operation `82ad0357-a78d-11f1-866f-eeee0a815b5e`, đã abort ở bước HTML5 content validation với lỗi `manifest.json not found`; lần này không thay droplet CAP đang chạy. Recovery dùng đường CAP package/stage/droplet có giới hạn và MTAR UI một module riêng; không mutation DB/HDI/schema/data/provider/user/role/email/scheduler. Artifact CAP generated được sửa cục bộ từ `fast-xml-parser` `5.9.3` lên `5.11.1`, hạ production audit từ một high cộng bảy moderate xuống bảy moderate và zero high/critical, không sửa manifest hay lockfile repository. Cloud Foundry cũng cảnh báo `cflinuxfs4` dự kiến ngừng trong tháng 05/2027; đây là maintenance platform sau này, không phải blocker release này.

Edge Browser Control đã đăng nhập acceptance empty state live mà không đổi dữ liệu: dialog `My Notifications` đo được `clientWidth=480` và `scrollWidth=480`, chứng minh không overflow ngang. Inbox DonHV hiện có zero notification nên không tạo notification giả và không claim visual live cho Bug row có dữ liệu. Artifact UI deploy chính xác chứa Bug UI `0.0.10`, có `bugTitle`, không còn dependency `sap/ui/core/Icon` và đã tắt horizontal scrolling; CAP metadata deploy có `bugNumber`/`bugTitle` và chủ ý không có Bug description. Vì vậy notification Bug hiện có sẽ render gọn thành `BUG-0017 — test notification bug` khi đúng recipient xem, còn session DonHV hiện tại chỉ chứng minh trung thực empty state.

Sau khi docs closure merge, check độc lập cuối phát hiện HANA readiness tạm thời là HTTP 503 trong khi CAP/AppRouter vẫn `1/1`. Một lần `btp:demo:prepare` được hỗ trợ đã yêu cầu start HANA service hiện có và restart CAP đúng một lần sau khi database trả HTTP 200. Check-only độc lập sau đó trả `DEMO READY` với cùng CAP droplet hiện tại `2382e503-e645-4ba2-bd03-e1b2c2b2ff5f`; không redeploy, đổi schema, seed hoặc business data.
