# DonHV Status - Leader / BA-PM / Cross-Workstream Support

Last updated: 2026-06-17

Vietnamese: Trạng thái của DonHV - Leader / BA-PM / hỗ trợ các workstream khác.

## Member Identity

| Field | Value |
| --- | --- |
| Member | DonHV |
| Team role | Leader |
| Primary responsibility | Leader, Backend CAP lead for Sprint 02, backend bug fixing, project documentation, scope alignment, weekly consolidation |
| Support responsibility | May support BA/PM consolidation, Fiori/UI5, and QA/Verification when needed |

Vietnamese:

| Trường | Giá trị |
| --- | --- |
| Thành viên | DonHV |
| Vai trò nhóm | Leader |
| Trách nhiệm chính | Leader, Backend CAP lead cho Sprint 02, backend bug fixing, tài liệu dự án, thống nhất scope, tổng hợp hàng tuần |
| Trách nhiệm hỗ trợ | Có thể hỗ trợ BA/PM consolidation, Fiori/UI5 và QA/Verification khi cần |

## Current Focus

Prepare Sprint 02 delivery after mentor feedback: lead Backend CAP work, own backend bug fixing, coordinate Jira/PM handover, and keep project documentation aligned when business meaning changes.

Vietnamese: Chuẩn bị delivery Sprint 02 sau feedback mentor: lead Backend CAP, chịu trách nhiệm backend bug fixing, điều phối Jira/PM handover và giữ tài liệu dự án đồng bộ khi nghiệp vụ thay đổi.

## Done

- BA documentation pack created under `docs/ba/`.
- PM documentation structure created under `docs/pm/`.
- BRD/SRS/FRS Markdown and DOCX deliverables created and maintained.
- SAP490 Google Workspace sync workflow, `gws` scripts, team setup guide, and template demo upload were prepared.
- MVP role baseline clarified as Tester, Developer, and PM.
- Optional Learning Recap / Mentor Mode was added for post-task teaching and teach-back.
- Sprint 02 backend tasks `IDTS-2` and `IDTS-4` completed and moved to Done in Jira.
- `srv/service.js` now enforces developer processing permission when request user is known, keeps team-visible discussion open, and records nextProcessor history/notification side effects.

Vietnamese:

- Đã tạo bộ tài liệu BA trong `docs/ba/`.
- Đã tạo cấu trúc tài liệu PM trong `docs/pm/`.
- Đã tạo và duy trì các deliverable BRD/SRS/FRS dạng Markdown và DOCX.
- Đã chuẩn bị workflow sync SAP490 Google Workspace, script `gws`, hướng dẫn setup team và demo upload template.
- Đã chốt baseline role MVP là Tester, Developer và PM.
- Đã thêm Learning Recap / Mentor Mode tùy chọn để dạy lại và kiểm tra hiểu sau task.

## In Progress

- Sprint 02 mentor feedback and happy-flow plan prepared, Jira backlog created, and Jira assignees aligned with the balanced team allocation.
- Jira `IDTS-5` remains In Progress as the backend QA bug-fix bucket for additional Sprint 02 issues reported by NhanT or the team.
- Local shell-based verification for comments, attachments, lifecycle, and audit behavior remains active under `IDTS-5`, but the attachment persistence hardening itself is now complete.

Vietnamese:

- Đã chuẩn bị feedback mentor Sprint 02, tạo Jira backlog và assign Jira theo phân công team đã cân bằng.

## Next

- Start Sprint 02 implementation from the assigned Jira backlog.
- Lead backend rule and handler changes for developer visibility, mandatory note/reason validation, history, nextProcessor, and backend bug fixing.
- Consolidate weekly updates from `sangvn.md`, `datdt.md`, and `nhant.md`.
- Keep risks and decisions updated in `docs/pm/risk-decision-log.md`.

Vietnamese:

- Bắt đầu implementation Sprint 02 từ Jira backlog đã được assign.
- Lead backend rule và handler cho developer visibility, mandatory note/reason validation, history, nextProcessor và backend bug fixing.
- Tổng hợp cập nhật hằng tuần từ `sangvn.md`, `datdt.md` và `nhant.md`.
- Duy trì risk/decision trong `docs/pm/risk-decision-log.md`.

## Blockers

- None.

Vietnamese: Chưa có blocker.

## Session Log

Each work session should add a row here.

Vietnamese: Mỗi phiên làm việc cần thêm một dòng vào bảng này.

| Date | Task/WP | What was done | Completed part | Issues/Bugs found | Fix status | Evidence/Commands | Next handoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-06-17 | IDTS-5 / Branch push and PR handoff | Cleaned temporary QA artifacts from the working tree, added ignore rules for `db.sqlite-shm` and `db.sqlite-wal`, committed the current backend/QA/docs state on branch `fix/idts-5-resubmit-workflow-donhv`, and pushed it to `origin`. | Remote branch is published and ready for review/merge into `dev`. | GitHub PR automation is still blocked on this machine because no GitHub CLI is installed and no `GITHUB_TOKEN` / `GH_TOKEN` is available in the environment. The correct compare URL must explicitly target `dev`. | Branch push completed; PR creation pending manual GitHub submission or a later authenticated CLI/API step | `git status --short`; `git commit -m \"fix(idts-5): harden backend happy flow and audit flow\"`; `git push -u origin fix/idts-5-resubmit-workflow-donhv`; branch tip `404bdcc` | Open PR from `https://github.com/DarwinDO/IDTS-SAP01/compare/dev...fix/idts-5-resubmit-workflow-donhv?expand=1`, then merge to `dev` after review |
| 2026-06-17 | IDTS-5 / Status-entry alignment + attachment history hardening | Finalized the backend/product decision that the current MVP create flow persists `Assigned` or `Pending Assignment` instead of persisting `New`, then synced the canonical docs/diagrams to mark `New` as legacy/import-only. Expanded backend audit tracking so generic edits such as `title` and `description` now write `HistoryLogs`, and replaced the unreliable attachment-history hook with a CAP draft `SAVE` wrapper on `Bugs.drafts` so attachment uploads write history only when the root draft is activated. Also cleaned the QA scripts: `test-idts6-programmatic.js` now verifies 30 scenarios, and the auxiliary programmatic comments/attachments script no longer reports a false failure for CAP's intentionally blocked direct child draft writes. | Backend behavior, canonical docs, and repeatable verification are complete for the status-entry decision, generic edit history, and real draft attachment history. | One non-product QA-script issue was found and fixed: `scripts/qa/test-comments-attachments-programmatic.js` previously failed with `DRAFT_MODIFICATION_ONLY_VIA_ROOT` because it tried to create a draft child attachment directly instead of via the root draft flow. No remaining blocker was found in the backend paths verified in this pass. | Fixed and verified | `node --check srv/service.js`; `npx cds compile srv/service.cds app/bug-management-ui/annotations.cds --to edmx > .tmp-history-status.edmx`; `node scripts/qa/test-idts6-programmatic.js` => 30 PASS / 0 FAIL; `powershell -ExecutionPolicy Bypass -File scripts/qa/test-comments-attachments.ps1 -BaseUrl http://localhost:4014/odata/v4/bug` => PASS end-to-end for comment history + attachment upload/download/history; `node scripts/qa/test-comments-attachments-programmatic.js` => PASS with CAP draft limitation documented as INFO | FE/UI follow-up can now focus on product UX gaps only: attachment visibility on create page, action/button clarity, and any remaining role-specific Object Page behavior. SAP490 Functional Specification and Test and Fix Bug workbooks should be updated from this verified baseline when the next document sync window opens. |
| 2026-06-17 | IDTS-5 / Need More Information resubmit workflow | Implemented an explicit Tester/PM recovery path for `Need More Information`. Added bound action `resubmitToDeveloper(note)` in `srv/service.cds`, handler logic in `srv/service.js`, and Fiori annotation exposure in `app/bug-management-ui/annotations.cds`. The new action returns the bug to `Assigned`, resets `nextProcessor` to the assigned Developer, requires an update summary, writes grouped history changes, creates a follow-up comment, and sends an in-app notification to the Developer. Also tightened the backend transition matrix so `Rejected` no longer routes back through `Need More Information` in the current MVP. | Backend logic, service metadata, and repeatable QA script coverage are complete for the resubmit path. Canonical business docs and PM docs were synced in the same work item. | The earlier workflow gap in `Need More Information` / `Rejected` was real and could confuse demo users because forward movement relied on edit/comment plus another action instead of a single CTA. No new blocker was introduced in this fix pass. | Fixed and verified for MVP | `node --check srv/service.js`; `npx cds compile srv/service.cds app/bug-management-ui/annotations.cds --to edmx > .tmp-resubmit-workflow.edmx`; `node scripts/qa/test-idts6-programmatic.js` => 26 PASS / 0 FAIL, including `SC-05c`, `SC-05d`, `SC-05e`, and `SC-06c` | FE/UI retest can now validate the new `Resubmit to Developer` action and confirm the button placement/behavior in Object Page without changing the backend contract |
| 2026-06-17 | IDTS-5 / Lifecycle and audit gap verification | Repaired corrupted `ProcessorRoleValues` names in the live `db.sqlite` so `nextProcessorRoleName` stops rendering strings like `Developerrrr`, then verified live OData capability flags for `NEW`, `PENDING_ASSIGNMENT`, `IN_REVIEW`, `NEED_MORE_INFORMATION`, and `REJECTED` under Tester, Developer, and PM personas. Also ran the backend happy-flow suite again and executed two isolated in-memory service tests to verify the audit behavior for generic bug-detail edits versus comments. | Local master-data drift is fixed and re-verified in live payloads; backend transition suite still passes 22/22. Audit-gap evidence is now confirmed, not assumed. | Three actionable gaps found during verification: 1. current create logic skips persisted `NEW` and writes `ASSIGNED` or `PENDING_ASSIGNMENT` directly, so the documented lifecycle and actual product entry state are not fully aligned; 2. `Need More Information` and `Rejected` are technically transitionable, but the Tester-side product flow lacks an explicit `resubmit` action and relies on edit/comment plus assign or developer follow-up; 3. generic edits to fields like `title` and `description` do not create meaningful `HistoryLogs`, while `addComment` does. | Partially fixed: role-name drift fixed; lifecycle/audit gaps only diagnosed and documented | Live OData `GET /odata/v4/bug/Bugs?...$select=bugNumber,status_code,nextProcessorRole_code,nextProcessorRoleName` returned HTTP 200 with `Developer`/`Tester` restored; `node scripts/qa/test-idts6-programmatic.js` => 22 PASS / 0 FAIL; in-memory `UPDATE Bugs` test showed one history row only for derived `nextProcessorUser`, with no `title`/`description` history rows; in-memory `addComment` test created `HistoryLogs.fieldName = comment` | Decide whether Sprint 02 should keep current status model for demo or align implementation/documentation now: either reintroduce persisted `NEW` properly and add a clear tester `resubmit` pattern, or document the current simplified flow and defer UX refinement to a later FE/UI5 batch |
| 2026-06-17 | IDTS-5 / Draft projection runtime fix | Converted the draft-sensitive display fields on `BugService.Bugs` to `virtual` service properties backed by `after READ` enrichment and then corrected the enrichment logic so the virtual fields are always written into the response payload, even when CAP does not materialize those properties on the raw DB row. Restarted a clean CAP backend and re-verified both list and single-entity OData reads. | The preview-blocking SQLite draft error is fixed, and the four display-name fields now return correctly in runtime payloads. | No new product bug found in this fix pass. One verification-command issue remained non-product only: earlier PowerShell test URLs escaped `$select` incorrectly and produced 400 parser noise in the server log. | Fixed and verified | `node --check srv/service.js`; `npx cds compile srv/service.cds app/bug-management-ui/annotations.cds --to edmx`; Node `fetch` to `GET /odata/v4/bug/Bugs?$top=3&$select=bugNumber,title,reporterDisplayName,assigneeDisplayName,nextProcessorUserDisplayName,nextProcessorRoleName,status_code` returned HTTP 200 with populated display fields; Node `fetch` to `GET /odata/v4/bug/Bugs(ID=90000000-0000-0000-0000-000000000001,IsActiveEntity=true)?$select=bugNumber,reporterDisplayName,assigneeDisplayName,nextProcessorUserDisplayName,nextProcessorRoleName,reporter_ID,assignee_ID,nextProcessorUser_ID,nextProcessorRole_code,status_code` returned HTTP 200 with populated display fields; `.tmp\\cds-serve-4004.log` shows normal GETs and no `no such column: $d.nextProcessorRoleName` after restart | Ask Claude to browser-retest the Fiori screen that originally raised the popup and confirm the preview no longer crashes, but no Playwright work was run by Codex in this session |
| 2026-06-17 | IDTS-5 / Draft projection runtime error diagnosis | Diagnosed the new List Report startup crash shown from the Fiori preview app. Confirmed the error is not a generic UI issue but a CAP SQLite draft query failure after the recent display-field hardening change. Live runtime SQL tries to read `sd.nextProcessorRoleName` from `BugService_Bugs_drafts`, but that draft shadow table does not physically contain the flattened projection column. | Root cause is identified with runtime evidence. | New actionable bug found: the recently introduced flattened display properties on draft-enabled `BugService.Bugs` (`reporterDisplayName`, `assigneeDisplayName`, `nextProcessorUserDisplayName`, `nextProcessorRoleName`) are safe for metadata/readability but at least `nextProcessorRoleName` breaks draft query generation on SQLite preview runtime. Symptom: List Report fails to load with `no such column: $d.nextProcessorRoleName`. | Open (diagnosed, not fixed) | User screenshot from `localhost:51769`; source review of `srv/service.cds` and `annotations.cds`; live preview error text; CAP docs lookup on virtual elements in projections; runtime metadata/query inspection in current session | Recommended next fix: convert the flattened display properties on `BugService.Bugs` from inline projection expressions to `virtual` fields populated in `after READ` for both `Bugs` and `Bugs.drafts`, while keeping the underlying FK fields read-only. |
| 2026-06-17 | IDTS-5 / FE runtime metadata sanity check | Compared source annotations, compiled EDMX, and live runtime `$metadata` on `localhost:4004` to explain why `Status` is read-only but `Reporter` / `Assignee` / `Next Processor` may still appear editable in the browser. Confirmed the runtime service already exposes `Common.FieldControl/ReadOnly` on `reporter_ID`, `assignee_ID`, `nextProcessorUser_ID`, and `nextProcessorRole_code`, plus computed display fields for `reporterDisplayName`, `assigneeDisplayName`, `nextProcessorUserDisplayName`, and `nextProcessorRoleName`. | Root-cause direction clarified: source/runtime metadata is aligned; remaining mismatch is likely in the running FE session rather than missing backend annotations. | New tooling/runtime issue found: `.cds-watch.log` shows repeated `EADDRINUSE` errors on livereload ports `35752` and `35753`. This can leave multiple local watcher instances or stale FE reload sessions active, which may cause the browser to keep old metadata/control state and make editability checks unreliable. | Open (environment hygiene issue, no product-code fix applied yet) | `curl.exe -s -u DonHV: \"http://localhost:4004/odata/v4/bug/$metadata\"`; `rg` against runtime metadata; `.cds-watch.log` tail showing `EADDRINUSE` on ports `35752/35753` | Clean the local runtime before the next UI retest: stop duplicate `cds/node` watcher processes, restart one clean server, then re-check edit mode with cache disabled. |
| 2026-06-17 | IDTS-5 / Edit-mode governance hardening | Implemented a defensive fix for edit-mode corruption and display safety. Added flattened display fields on `BugService.Bugs` for reporter, assignee, next processor user, and next processor role; rewired Object Page fields to those safe display properties instead of direct navigation-path bindings; and added backend read-only guards for master data and system-managed entities (`Users`, `DeveloperProfiles`, code lists, `HistoryEvents`, `HistoryLogs`, `Notifications`, `DuplicateLinks`, and related views). Restored the corrupted local `NhanT` display name in `db.sqlite`. | Code patch and local data repair completed. Service metadata compiles, flattened properties exist in CSN, and `BugService.Users` now rejects write attempts with HTTP 405 semantics. | One tooling issue found during verification: `npm run cds-compile` does not exist in `package.json`, so verification had to switch to direct `npx cds compile ...`. Architectural residual note: audit actor/comment author names still depend on immutable user-master assumptions rather than stored snapshot names, but the immediate corruption path is now blocked by service-layer guards. | Fixed for the current local/demo path; residual snapshot-design discussion remains optional follow-up | `npx cds compile srv/service.cds app/bug-management-ui/annotations.cds --to edmx`; `npx cds compile srv/service.cds --to json | Select-String 'reporterDisplayName|nextProcessorUserDisplayName|nextProcessorRoleName'`; Node/CDS query confirmed `NhanT` restored in `idts.cap.Users`; programmatic CAP service verification returned `READONLY_GUARD=405:BugService.Users is read-only in BugService.` | Ask Claude to re-run focused UI retest for edit mode only: verify Reporter, Assignee, Next Processor User, Next Processor Role, Rejected Follow-up, Planning, Comments, History, and Notifications are no longer editable from Object Page edit mode. |
| 2026-06-17 | SP2 / History actor-name corruption diagnosis | Investigated the leader concern that `History.Actor` now shows `NhanTTestUserTesting User` instead of `NhanT`. Verified directly against local `db.sqlite` that the `Users` master record for `NhanT` was actually mutated at runtime, not just rendered incorrectly in one table. Also confirmed that `HistoryEvents`, `Comments`, and bug-level reporter/next-processor names all read the current `Users.displayName`, so the mutation propagates retroactively across old bug reports. | Root cause evidence is now confirmed at data-model level. | New actionable bug found: local master user `NhanT` (`10000000-0000-0000-0000-000000000004`) currently has `displayName = NhanTTestUserTesting User`, `modifiedBy = nhant`, `modifiedAt = 2026-06-17T12:05:42.302Z`. Because `BugService.HistoryEvents.actorDisplayName`, `Comments.authorDisplayName`, and reporter/next-processor displays are projections from `Users.displayName`, old bug history now shows the corrupted tester name everywhere. This strongly supports the earlier edit-mode governance concern around writable navigation-based user fields. | Open (diagnosed, not fixed) | Node/CDS DB queries against `db.sqlite`: `SELECT` from `idts.cap.Users`, `BugService.HistoryEvents`, `BugService.Comments`, and `idts.cap.Bugs`; local seed file `db/data/idts.cap-Users.csv` still shows original `NhanT`, proving the runtime DB drift is not from seed data. | Approve a targeted fix plan: restore the corrupted local user display name, lock user-facing bug fields so bug edit mode cannot mutate `Users`, and decide whether history/comment read models should keep live user names or store immutable snapshot names for audit stability. |
| 2026-06-17 | SP2 / Edit-mode governance QA UI retest | Conducted a complete QA UI retest of the edit-mode field and section governance using the browser subagent (Playwright CLI/Interactive). Retested BUG-0001 (New) and BUG-0004 (Rejected) Object Pages in edit mode as PM user DonHV. | Verified all requested fields and section visibilities, took screenshots of the general edit mode, assignment section, and rejected follow-up case, and confirmed multiple governance bypasses. | Found that: 1. Reporter field remains editable in edit mode (deep-updating Users table directly). 2. Assignee field is editable in the form container instead of read-only. 3. Next Processor User/Role fields are editable. 4. Latest Rejection Reason and Next Processor fields in Rejected Follow-up section are editable. 5. Planning section is visible and editable. 6. Rejected Follow-up is visible on non-rejected BUG-0001. 7. Inline Assign Developer button is hidden during draft edit mode. | Retest completed, findings reported | browser_subagent runs; screenshots: edit_mode_general_*.png, edit_mode_assignment_*.png, rejected_bug_case_*.png; webp recordings | Hand over findings to DatDT/SangVN for fixing annotations (hiding Planning/Rejected Follow-up, locking assignee/reporter FKs as read-only). |
| 2026-06-17 | SP2 / Edit-mode governance fix pass | Implemented the annotation-side governance fix for Object Page edit mode. Locked `assignee`, `reporter`, `nextProcessorUser`, `nextProcessorRole`, `rejectionReason`, `plannedCompletionDate`, `dueDate`, and `estimatedEffortHours` at property level and/or DataField level. Added `Assign Developer` as a form-level action inside the `Assignment` section so users can still assign from the page without using generic field editing. Also tightened `Rejected Follow-up` visibility so it only appears for rejected bugs, and hid `Planning` from the current MVP Object Page flow. | Annotation/metadata fix is complete and compiles successfully. | No new product bug found during this pass. Remaining risk: UI browser retest is still required to confirm Fiori no longer renders editable controls for `Reporter` and `Next Processor` and that the new section-level assign action behaves cleanly beside the header action. | Partially fixed (code-side fixed, UI retest pending) | `cds compile srv app/bug-management-ui --to edmx`; EDMX verification against `.tmp-edit-governance.edmx` confirmed `Common.FieldControl/ReadOnly` for `reporter_ID`, `assignee_ID`, `nextProcessorUser_ID`, `nextProcessorRole_code`, `rejectionReason`, `plannedCompletionDate`, `dueDate`, `estimatedEffortHours`, and confirmed `Assign Developer` action inside `UI.FieldGroup#Assignment` | Browser-retest edit mode as DonHV/DatDT/NhanT; verify Reporter and Next Processor are not editable, Assignee is read-only, and assignment now goes through action only |
| 2026-06-17 | SP2 / Edit-mode field audit | Audited Object Page edit-mode annotations to identify fields that are still exposed for generic editing even though the current workflow expects action-driven or system-managed behavior. Confirmed that `Assignee` remains directly editable in `UI.FieldGroup#Assignment`, and also identified `Rejected Follow-up` / `Planning` fields that stay editable for any existing bug although the current MVP flow does not use generic inline edit for rejection handling or PM planning. | Root cause and affected UI groups are identified and documented for the edit-mode audit. | New actionable bug found: edit mode still exposes workflow-sensitive fields (`rejectionReason`, `plannedCompletionDate`, `dueDate`, `estimatedEffortHours`) in addition to the previously logged `Assignee` bypass. This can let users perform generic edits that do not match the intended lifecycle actions and can confuse the happy-flow demo. | Open (diagnosed, not fixed) | `app/bug-management-ui/annotations.cds` lines 182-193, 291-307, 313-334; `rg -n "RejectedFollowUp|Planning|assignee_ID|rejectionReason|dueDate|estimatedEffortHours|FieldControl" app/bug-management-ui/annotations.cds`; Fiori MCP `search_docs` for `@Common.FieldControl : #ReadOnly` and `@UI.Hidden` guidance | Approve a fix plan: lock or hide workflow-sensitive fields in edit mode, keep assignment action-driven, and only re-open generic editing later if the backend is intentionally taught to mirror the workflow side effects |
| 2026-06-17 | SP2 / Assignment flow diagnosis for BUG-0001 | Investigated why `BUG-0001` could still show `NEW` and no in-app notification after DonHV assigned `DatDT`. Traced the backend assignment paths and the Object Page annotations. Confirmed that the Object Page still exposes editable field `assignee_ID` inside `UI.FieldGroup#Assignment`, while the real business flow is the bound action `assignToDeveloper`. The generic update path records assignee changes in history, but it does not automatically force `status_code = ASSIGNED` on update and only writes notifications when a tracked status change exists. | Root cause is identified and documented. | New actionable bug found: editing the `Assignee` field directly can bypass the intended assign workflow, leaving the bug in `NEW` and producing no `ASSIGNED` notification. This creates confusing user behavior and can make the header action and editable field conflict. | Open (diagnosed, not fixed) | `rg -n "assignToDeveloper|assignee_ID|status_code|writeNotificationForStatus|recordUpdateSideEffects" srv app -S`; inspection of `srv/service.js` around `prepareBugWrite`, `assignToDeveloper`, `transitionBug`, `recordUpdateSideEffects`, and `writeNotificationForStatus`; inspection of `app/bug-management-ui/annotations.cds` `UI.FieldGroup#Assignment` | Decide the fix path: either make `assignee` read-only in the form and force all assignment through the header action, or teach generic assignee edits to synchronize status/nextProcessor/notification like the bound assign flow |
| 2026-06-17 | SP2 / FE extension batching decision | Recorded the current execution decision for the remaining `IDTS-9` selected-text gap: do not start a one-off targeted FE/UI5 custom extension now. Keep the current annotation-first path for demo, and batch future FE/UI5 custom work into one later implementation wave if multiple custom UX gaps accumulate. | Delivery sequencing is clarified in the leader handover file. | No new product bug. This is an execution/planning note: the remaining open FE gap is still `TMP-BUG-14`, but the team is intentionally deferring isolated custom FE work for now. | Fixed at handover level | `Get-Content docs/pm/status/donhv.md`; `rg -n "TMP-BUG-14|IDTS-9|happy flow" docs/pm/status/donhv.md docs/pm/tasks/wp4-walkthrough.md docs/pm/current-status.md` | Continue with annotation-driven/demo-safe baseline first. If later there are multiple FE/UI5 custom needs, implement them together in one controlled extension batch |
| 2026-06-17 | PM handover / IDTS-9 status alignment | Cleaned up `docs/pm/status/datdt.md` and `docs/pm/status/sangvn.md` so the member-role, current focus, blocker state, and `IDTS-9` retest wording match the actual Sprint 02 FE handover. Removed stale `not started` positioning and softened the root-cause wording from an absolute `standard limitation` claim to a still-reviewable `likely` FE limitation/handling gap. | Cross-member PM handover docs are aligned again for the current FE issue. | No new product bug was introduced. The documentation issue was that both member files still carried outdated lane/focus metadata and overly strong wording around the root cause of the UUID symptom. | Fixed | `Get-Content` review of `docs/project-context.md`, `docs/pm/current-status.md`, `docs/pm/task-board.md`, `docs/pm/status/datdt.md`, `docs/pm/status/sangvn.md`; `git diff --check -- docs/pm/status/donhv.md docs/pm/status/datdt.md docs/pm/status/sangvn.md` | Use the cleaned status files as the current reference while deciding between demo acceptance and targeted FE extension for `IDTS-9` |
| 2026-06-17 | SP2 / Assign Developer selected-text follow-up | Investigated the last open FE usability gap where the `Assign Developer` action dialog still showed developer UUID after selection. Confirmed from OData metadata that the action parameter itself only had `Common.ValueList` while the value-help key `BugService.AssignableDevelopers/developerProfileID` had no business text. Added `@Common.Text : developerName` and `@Common.TextArrangement : #TextOnly` on that value-help property in `app/bug-management-ui/annotations.cds`, recompiled metadata successfully, and reopened Jira `IDTS-9` with comment `10137` so DatDT/SangVN can run browser retest. | Annotation-only candidate fix prepared and handed over. PM/Jira tracking is aligned again: `IDTS-9` moved back to In Progress because the issue is still not browser-verified closed. | One process/tooling issue occurred during local retest support: a quick Playwright-style browser probe from Node could not find the value-help button selector reliably in the action dialog, so I stopped there and did not use that as verification evidence. Product bug status itself remains unchanged until DatDT/SangVN complete browser retest. | Partially fixed. Code-side candidate is in place; actual UI closure still open pending FE browser verification. | `npx cds compile srv app/bug-management-ui --to edmx`; EDMX inspection confirmed `BugService.AssignableDevelopers/developerProfileID` now emits `Common.Text -> developerName` with `UI.TextArrangement TextOnly`; Jira `IDTS-9` transitioned to `In Progress` and comment `10137` added. | DatDT/SangVN should browser-test the Assign Developer dialog on the latest metadata. If the input still shows UUID, decide between accepting the standard FE limitation for demo or implementing a targeted FE extension. |
| 2026-06-17 | SP2 / FE happy-flow rerun and priority fixes | Re-ran the Fiori happy flow on `localhost:4012` after the side-effect and dependent value-help fixes. Verified create page open, dependent Application Component -> Defect Category filtering, real attachment upload during create, active-page attachment visibility after save, readable comment author, assignment value-help rows, developer-only actions for DatDT, and PM-safe view for DonHV. Updated PM handover docs to close the FE refresh gaps that are now verified. | P1 attachment visibility after create is verified fixed. P1 action-bar refresh is verified fixed at workflow level. P2 classification pair filtering is verified fixed. WP7 can move back to completed. | One UX bug remains open: the Assign Developer action dialog now uses the business label `Assignee` and business-friendly rows, but after selecting DatDT the input still shows developer profile UUID `20000000-0000-0000-0000-000000000002` instead of the developer name. One transient command issue also occurred during verification: an initial `curl` query for `Comments/Attachments` used the wrong OData GUID filter syntax and failed even though the product behavior was healthy. | Partially fixed. TMP-BUG-11, TMP-BUG-12, and TMP-BUG-13 are closed by fresh evidence; TMP-BUG-14 remains open for UI polish. The OData command issue was fixed immediately by switching to direct bug-entity verification plus browser evidence. | `mcp__node_repl.js` browser reruns on `http://localhost:4012/idts.bugmanagementui/index.html?sap-ui-xx-viewCache=false`; `curl.exe -s -u NhanT: 'http://localhost:4012/odata/v4/bug/Bugs(ID=3d3e32ef-e53c-4b65-a61c-6aa11f444fee,IsActiveEntity=true)?$select=bugNumber,status_code,assignee_ID,nextProcessorRole_code&$expand=nextProcessorRole($select=name),status($select=name)'`; fresh body/button inspection for NhanT, DatDT, and DonHV | DatDT/SangVN should polish Assign Developer selected-text behavior. DonHV should sync the verified fixes into SAP490 Functional Specification and Test and Fix Bug deliverables. |
| 2026-06-17 | SP2 / Demo script hardening | Consolidated the temporary SAP490 bug staging register in this file, confirmed Option A for create-time attachments in annotations, and upgraded the Sprint 02 walkthrough into a step-by-step happy-flow checklist with demo safety rules and fix priority | Bug staging is now centralized for later SAP490 transfer, and the mentor demo script now has concrete HF-01 to HF-11 steps with expected results and known-risk watch points | No new product bug beyond TMP-BUG-11 to TMP-BUG-14; one documentation quality gap was addressed because the older walkthrough was too narrative and not explicit enough for repeatable team demo execution | Fixed at documentation/coordination level; product follow-up still remains on the four open UI/UX gaps | `git diff --check -- app/bug-management-ui/annotations.cds docs/pm/status/donhv.md docs/pm/current-status.md docs/pm/task-board.md docs/pm/tasks/wp4-walkthrough.md docs/pm/tasks/wp7-notification-attachments.md`; walkthrough + status review in current session | Re-run browser retest against HF-01 to HF-11 after the next FE refresh fix pass, then transfer stable bug/test evidence into SAP490 Functional Specification and Test And Fix Bug workbooks |
| 2026-06-17 | SP2 / Happy Flow retest for mentor demo | Re-tested the browser happy flow with real draft create, valid classification selection, real attachment upload, comment creation, developer assignment, and developer status transition. Verified create draft upload visibility, active bug creation, DB/API attachment persistence, comment creation, assign-to-developer, and `Mark In Review` backend transition. | Core happy flow is partially verified: create/save works with a valid component pair, comment flow works, assignment works, and backend status transitions work. | Found four important UI/UX gaps: invalid component/category combinations are still selectable in value help and fail only on save; attachment row can disappear immediately after draft activation even though DB/API persistence is correct; header action buttons do not refresh immediately after lifecycle actions; Assign Developer parameter dialog still shows technical `assigneeID`/UUID values. | Open follow-up required. Backend state is correct for tested actions, but final mentor-safe demo still needs Fiori refresh/UX fixes. | Playwright browser flow on `localhost:4004`; OData API checks with `curl.exe -u NhanT:` and `curl.exe -u DatDT:`; DB verification against `idts_cap_Attachments`; CAP log tail in `.tmp/cds-ui-4004.log` | Reopen WP7/WP4 follow-up, add Fiori side-effect fix plan, then re-run browser demo smoke before locking SAP490 bug/test artifacts |
| 2026-06-17 | WP7 / QA UI & E2E Verification | Performed E2E browser UI testing for comments and attachments under users DonHV and NhanT; verified comment add action, attachment upload, draft save, and persistence after reopen; checked read-only locking of sub-tables in edit mode. | E2E UI QA verification completed with passing results for both Comment and Attachment flows. | None. All tested UI flows passed and did not trigger 404 errors or generic error popups. | Fixed & Verified (All previously identified bugs are resolved, and the custom attachment flow works without fallback). | browser_subagent execution; screenshots: bug_0003_details_*.png, comment_added_*.png, attachment_saved_*.png, comments_and_attachments_*.png, nhant_comments_table_*.png; curl.exe retrieval of attachment content. | Finalize the Sprint 2 demo script under IDTS-12 and prepare for the Sprint 2 review session. |
| 2026-06-17 | AGENTS / WP7 attachment re-verification | Tightened `AGENTS.md` so every nontrivial final response must explicitly report `AI DevKit flow used`; then re-verified the custom comment + attachment backend flow with fresh evidence in the current session instead of relying on earlier runs | Confirmed again that the custom attachment flow still works on file-based SQLite: `srv/service.js` syntax check passes, programmatic comment persistence passes, HTTP draft attachment upload/download passes, and both comment plus attachment content still survive CAP stop/start on port `4011` | No new product bug reopened. One transient command issue occurred while stopping the QA server because a PowerShell loop used reserved variable name `$PID`; fixed immediately by renaming the loop variable to `$procId` | Fixed. No backend code change was needed in this session beyond the already-present custom attachment fix; custom flow remains stable enough that switching to `@cap-js/attachments` is not currently required | `node --check srv/service.js`; `node scripts/qa/test-comments-attachments-programmatic.js`; `powershell -ExecutionPolicy Bypass -File scripts/qa/test-comments-attachments.ps1 -BaseUrl http://localhost:4011/odata/v4/bug`; post-restart HTTP checks for `QA shell comment 1781678032164` and attachment `7131d67f-dbe4-4e7b-af55-f4fcade1dd7f` on port `4011`; `netstat -ano | Select-String ':4011'` | Hand over to Claude/browser QA for end-to-end UI retest of comment + attachment on the Object Page; if UI-only issues remain, investigate Fiori/UI transport/rendering rather than backend persistence |
| 2026-06-17 | WP7 / IDTS-5 local persistence and shell QA | Switched local development DB from in-memory SQLite to file-based `db.sqlite`, added shell/programmatic QA scripts for comment and attachment verification, reproduced the custom attachment stream bug, then fixed backend handling so media body is copied into `req.data.content` for `PUT .../content` requests | Confirmed that comment rows, attachment metadata, and attachment binary content persist after CAP restart when using `db.sqlite`; `qa:comments-attachments:http` and `qa:comments-attachments:programmatic` now both pass | Initial shell QA found that uploaded attachment `content` stayed `null` in SQLite and `GET /Attachments(...)/content` returned empty/invalid results after draft activation; root cause was that CAP draft child media requests were not materializing the incoming body into `req.data.content` in the custom flow | Fixed in `srv/service.js`; custom attachment flow now passes shell QA and restart persistence checks; Jira `IDTS-5` updated with both the bug report and fix evidence | `npx cds env requires.db`; `node scripts/qa/test-comments-attachments-programmatic.js`; `powershell -ExecutionPolicy Bypass -File scripts/qa/test-comments-attachments.ps1 -BaseUrl http://localhost:4011/odata/v4/bug`; direct Node DB queries against `db.sqlite`; CAP restart on port `4011`; post-restart HTTP GET on comment + attachment content; Jira MCP comments added to `IDTS-5` | Hand over to Claude/browser QA for end-to-end UI retest of comment + attachment on Object Page; if UI still fails, investigate Fiori/UI transport only, not backend persistence |
| 2026-06-17 | WP7 attachment upload metadata fix | Applied the standards-first Fiori fix for attachment upload by exposing the `content` stream field in `service.Attachments @UI.LineItem`; verified the emitted OData metadata instead of jumping to a controller extension | Attachment metadata now matches Fiori OData V4 upload-table guidance: `UI.LineItem` contains `content` and `UI.MediaResource` still points to `content` | Found a tooling limitation while checking with CAP MCP: `mcp__cap.search_model` fails at repo root because untracked `scratch/test.cds` duplicates `BugService.Bugs` annotations; also confirmed the earlier upload bug was caused by missing stream field in `Attachments @UI.LineItem` | Metadata fix applied and verified; browser/UI retest still open; CAP MCP scratch conflict remains open but non-blocking for targeted `cds compile srv app/bug-management-ui --to edmx` checks | `mcp__fiori.search_docs`; `mcp__cap.search_model` (failed due scratch duplicate annotation); `npx cds compile srv app/bug-management-ui --to edmx`; EDMX check confirmed `BugService.Attachments` `UI.LineItem` now includes `Label=\"Attachment\"` + `Path=\"content\"` and `UI.MediaResource Stream=\"content\"`; `node --check srv/service.js` | Hand over to Claude/browser QA for attachment upload retest on the Object Page; if 404 persists, only then investigate controller extension or runtime path handling |
| 2026-06-17 | WP5/WP7 UI fix follow-up | Removed direct `Bugs.historyLogs` ownership to avoid draft duplicate conflicts and re-enabled attachment create in Fiori annotations | History now uses grouped `HistoryEvents` only; Attachments upload is visible again in the Object Page | Draft edit hit a UNIQUE constraint when both Bugs.historyLogs and HistoryEvents.logs owned the same draft child rows; Attachments remained hidden/insert-locked in UI | Fixed | `node --check srv/service.js`; `npx cds compile srv app/bug-management-ui --to edmx`; `node scripts/qa/test-idts6-programmatic.js` = 22 PASS / 0 FAIL | Hand over to Claude for UI retest of draft edit and attachment upload |
| 2026-06-17 | WP5/WP7 QA UI & Verification | Conducted UI QA verification for child tables and history events | Verified display name rendering, notifications, and history summaries; reported three open bugs (draft unique constraint, locked attachments, and missing Reason column) | Found UNIQUE constraint failed on draft edit, Attachments upload locked out in UI, and missing Reason column in History table | Open (reported) | browser_subagent execution, screenshots captured: bug_comments_table_1781663750263.png, bug_history_notifications_table_1781663758404.png, attachments_edit_mode_1781663873590.png | Hand over bug report to DonHV/SangVN/DatDT for fixing |
| 2026-06-16 | WP5/WP7 sub-tables deep fix | Root-cause fix: replaced navigation property paths (author.displayName, actorRole.name, etc.) in LineItem with FK property paths; added @Common.Text + @Common.TextArrangement #TextOnly on associations for human-readable display; added @Common.FieldControl #ReadOnly on EVERY visible field in Comments, HistoryLogs, Attachments, and Notifications; locked Attachments with CreateHidden/DeleteHidden/InsertRestrictions; removed @readonly from service projections (conflicts with FK annotation discovery) and rely on Capabilities restrictions | All four sub-tables now render pure read-only text in Edit mode; navigation properties show display names via Common.Text instead of editable inputs | Root cause: @Common.FieldControl on association (author) only locks FK (author_ID) but not navigation property (author.displayName) rendered by Fiori LineItem; @readonly on service projection hides FK from CDS compiler so @Common.Text annotation was silently dropped | Fixed by using Common.Text on association elements and switching LineItem Value from nav paths to FK paths | CDS compile zero warnings; EDMX shows Common.Text + FieldControl ReadOnly on all FK properties; backend 21/21 PASS | Anh DonHV cần verify bằng browser (refresh trang đang mở) |
| 2026-06-16 | WP5/WP7 sub-tables locking | Added UI.CreateHidden and UI.DeleteHidden to Comments, HistoryLogs, and Notifications projections, and set Comments content to #ReadOnly; verified in browser using browser_subagent | Successfully locked all three sub-tables as completely read-only on the Object Page in Edit mode, hiding Create/Delete buttons and input boxes | Fiori Elements OData V4 displays the Create button on draft-enabled composition tables (Notifications) even when Capabilities.InsertRestrictions is false | Fixed by adding explicit UI.CreateHidden and UI.DeleteHidden annotations and locking Comments content | browser_subagent verification, cds compile, backend tests 21/21 PASS | Sprint 2 final demo is ready |
| 2026-06-16 | WP5/WP7 QA UI & Verification | Conducted direct browser QA UI verification for comments and attachments under user `donhv` (PM role) | Verified that Add Comment action dialog, comment display, history logging, and attachment upload/download stream work correctly and persist on Save | None (all previously identified gaps/issues are verified as fixed) | Fixed & Verified | browser_subagent execution, HTTP OData query validation, attachment content stream retrieval | Sprint 2 final demo preparation |
| 2026-06-16 | WP5/WP7 comments and attachments | Implemented bound comment action, disabled broken inline comment creation, and established the draft attachment upload path in CAP/Fiori | Comments now add through `addComment`; attachment UI and metadata path were available for draft upload testing | Inline child-create for comments was not usable with draft entities; attachment uploads initially rejected FE multipart envelope as unsupported media type; action-level refresh behavior needed side-effect tuning. Later shell QA on 2026-06-17 showed binary `content` persistence and `fileName` persistence were still not complete. | Partially fixed; superseded by the 2026-06-17 shell-QA findings for binary attachment persistence | `cds compile srv app/bug-management-ui --to edmx`; OData POST `BugService.addComment`; OData GET `Comments` and `Attachments`; browser smoke on localhost:4004 | Continue WP7 backend investigation for attachment stream persistence before claiming full happy flow |
| 2026-06-16 | WP4 role action visibility | Fixed backend capability enrichment so Fiori action booleans still compute when FE omits `status_code` and `assignee_ID`; explicitly locked Object Page `Status` through read-only field metadata instead of leaving it editable; restarted stale local server on port 4004 for clean auth-based testing; added case-insensitive request-user fallback so lowercase mock login names like `donhv` still map to the correct IDTS user | Backend/UI fix implemented on `fix/idts-5-role-action-visibility-donhv` and fast-forwarded into `dev`; follow-up metadata hardening for `Status` also completed locally | Found four causes behind confusing action/status behavior: stale `cds watch` on port 4004 allowed old behavior, FE requests selecting only `canXxx` flags caused false capability results, lowercase mock login names (`donhv`) did not map to displayName `DonHV`, and `status` remained editable because only the display path changed while read-only metadata was incomplete | Fixed in code and local runtime; fresh 4004 returns `401` for anonymous, computes role-based `canXxx` correctly, resolves lowercase member logins, and exposes read-only `Status` metadata in live `$metadata` | `mcp__fiori.search_docs`, `mcp__cap.search_model`, `mcp__node_repl.js`, OData fetch on `localhost:4004/4006/4010`, live `$metadata` inspection, `git reflog`, `git show --stat`, `git diff -- app/bug-management-ui/annotations.cds`, `git diff --check` | Re-test Object Page as `donhv`, `sangvn`, and `nhant`; then decide whether action-bar grouping needs another WP4 UX pass |
| 2026-06-03 | PM setup | Renamed status model from workstream files to member files and kept DonHV as leader/BA-PM owner | Team status ownership reset | None | Fixed | `rg`, `git diff --check` | Team members update their own status files |
| 2026-06-03 | SAP490 docs | Created SAP490 template inventory and usage guide | Template purpose/fill guidance documented | None | Fixed | `openpyxl`, `rg`, `git diff --check` | Use inventory before filling SAP490 templates |
| 2026-06-04 | Agent learning workflow | Added optional Learning Recap / Mentor Mode in AGENTS.md and `.agents/skills/learning-recap` | Mentor-style teach-back workflow integrated | None | Fixed | `rg`, `git diff --check`, `npx ai-devkit@latest lint --json` | Use after nontrivial tasks when requested or useful |
| 2026-06-04 | BA/database readiness | Extracted SRS/FRS diagrams into `docs/diagrams`, installed database modeling support, and wrote current database model review | Diagram pack and database review baseline completed | DBML CLI trial timed out and increased audit noise | Fixed by removing DBML CLI and keeping database skills only | `npx skills add`, `npm uninstall @dbml/cli`, `cds compile srv --to edmx`, `rg`, `git diff --check` | Confirm DB open questions, then start WP1 Data Model Foundation |
| 2026-06-04 | WP1 database baseline | Converted DB-Q01 to DB-Q08 from open questions into explicit WP1 database decisions and synced them to canonical docs | Database decision baseline completed | None | Fixed | `rg`, `cds compile srv --to edmx`, `npx ai-devkit@latest lint --json`, `git diff --check` | Backend can start WP1 after reviewing `docs/ba/09-database-model-review.md` |
| 2026-06-04 | MCP troubleshooting | Investigated why CAP MCP tools were not visible and documented the lazy-load fix using `tool_search` | CAP MCP exposure workflow documented | CAP MCP namespace was deferred in current Codex session | Fixed by `tool_search` discovery; CAP `search_docs` and `search_model` verified | `tool_search`, `mcp__cap.search_docs`, `mcp__cap.search_model`, `npx -y @cap-js/mcp-server --help` | Use `tool_search` first if MCP namespace is missing |
| 2026-06-04 | WP1 Data Model Foundation | Implemented CAP CDS model, service projections, and seed data as DonHV cross-workstream support for Backend CAP | WP1 model foundation completed | Business handlers are not implemented yet | Open for WP3, not a WP1 bug | `cds compile srv --to edmx`, `cds deploy --to sqlite::memory:`, `git diff --check`, `mcp__cap.search_docs`, `mcp__cap.search_model` | SangVN can continue with WP2/WP3; NhanT can prepare QA checks |
| 2026-06-04 | WP4 Fiori Elements UX | Reviewed DatDT `Sap_FE` repo and integrated useful List Report/Object Page ideas into current CAP/Fiori app | Initial annotation-driven UI pass and demo bug seed completed | Fiori MCP tool not exposed; DatDT attachment fragment is static and not service-backed | Partially fixed; annotations/build/browser smoke pass, attachment upload remains WP7 | `tool_search`, `mcp__cap.search_model`, `cds compile srv app/bug-management-ui --to edmx`, `cds deploy --to sqlite::memory:`, `npx ui5 build --config ui5.yaml --clean-dest --dest %TEMP%/idts-ui5-build`, Playwright browser smoke, `git diff --check` | DatDT can continue WP4 after WP2/WP3 value-help/action support |
| 2026-06-04 | WP2/WP3/WP4 continuation | Implemented service value helps, bound bug lifecycle actions, CAP handler validations, nextProcessor automation, history/notification side effects, and Fiori Object Page actions | WP2 and WP3 completed for MVP; WP4 core screens updated | Browser MCP has snapshot/screenshot but no click/type in this session; real attachment upload and deeper QA remain | Fixed for MVP; remaining items handed to WP4/WP7/QA | `tool_search`, `mcp__cap.search_docs`, `mcp__cap.search_model`, `mcp__fiori.search_docs`, `cds compile srv app/bug-management-ui --to edmx`, `cds deploy --to sqlite::memory:`, OData action smoke calls, `npx ui5 build --config ui5.yaml --clean-dest --dest $env:TEMP/idts-ui5-build`, Playwright browser smoke | SangVN can review WP2/WP3 backend; DatDT can continue UI usability QA; NhanT can prepare regression scenarios |
| 2026-06-04 | WP4 UI refinement | Adjusted List Report UX after visual review: GridTable layout, draft-enabled standard Create dialog, and semantic status display without default icons | UI refinement implemented and compile/build/browser verified | Toolbar standard action label remains `Create`; dialog title is `Create Bug`. Full manual create-save QA still needed | Fixed for MVP; manual UI QA remains | `mcp__fiori.search_docs`, `mcp__cap.search_docs`, `cds compile srv app/bug-management-ui --to edmx`, `npx ui5 build --config ui5.yaml --clean-dest --dest $env:TEMP/idts-ui-fix-build`, Playwright browser click/snapshot | DatDT should manually verify Create Bug save flow and table behavior in browser |
| 2026-06-05 | Sprint 02 planning | Synced mentor feedback into canonical docs, created Sprint 02 plan, updated PM handover, created Jira backlog, and added Jira bug-tracking rule to AGENTS.md | Sprint 02 planning baseline, Jira backlog, and agent bug-tracking rule completed | `IDTS-SUMMARY.md` append initially failed because console showed mojibake while file content was UTF-8 clean | Fixed by verifying the real line text with `Select-String` and patching using clean UTF-8 text | `Get-Content`, `Select-String`, `apply_patch`, Atlassian Rovo MCP `_createjiraissue`, `_searchjiraissuesusingjql`; Jira issues `IDTS-1` to `IDTS-12`; verified later with `git diff --check` | Start backend handler updates for Sprint 02 |
| 2026-06-06 | Sprint 02 Jira assignment | Assigned Jira issues `IDTS-1` to `IDTS-12` to DonHV, NhanT, DatDT, and SangVN using the balanced Sprint 02 mapping; fixed mismatched Jira owner descriptions for `IDTS-3`, `IDTS-10`, and `IDTS-12`; updated `docs/pm/07-sprint-2-plan.md` to match Jira | Jira assignment and sprint plan alignment completed | Initial Jira descriptions for `IDTS-3`, `IDTS-10`, and `IDTS-12` still showed old owners after assignee changes | Fixed by updating those Jira descriptions through Atlassian Rovo MCP | `tool_search`, Atlassian Rovo MCP `_editjiraissue`, `_searchjiraissuesusingjql`, `apply_patch`, `rg`, `git diff --check` passed with LF-to-CRLF warnings | Each member starts assigned Jira tasks and updates their own status file after work sessions |
| 2026-06-10 | Sprint 02 backend - IDTS-2/IDTS-4/IDTS-5 | Added branch naming rule to AGENTS.md, implemented developer visibility vs processing permission, added comment author handling, added nextProcessor history logging, added resolved/retest/reopened notifications, and updated Jira | `IDTS-2` and `IDTS-4` completed and moved to Done; `IDTS-5` moved to In Progress as QA bug-fix bucket | Backend gaps found: request-user processing permission was missing; bound actions did not log nextProcessor changes; resolved/retest/reopened did not create notification records; `cds serve all` logs a non-blocking `cds-plugin-ui5` welcome-page injection warning | Fixed the backend gaps in `srv/service.js`; UI5 welcome-page warning remains open/non-blocking and not a backend blocker | `node --check srv/service.js`; `cds compile srv app/bug-management-ui --to edmx`; `cds deploy --to sqlite::memory:`; OData permission smoke; OData side-effect smoke; Atlassian Rovo Jira comments/transitions | NhanT can continue IDTS-3 validation and QA checks; DatDT/SangVN can continue FE tasks; DonHV monitors IDTS-5 for new backend bugs |
| 2026-06-10 | Business rule cleanup | Removed redundant BR-48 and moved its mentor-confirmed content into the original affected rules and canonical docs | BR-02, BR-18, BR-25, BR-27, Summary, Scope, and Project Context now state the final rule directly | Documentation clarity issue: keeping BR-48 as a late refinement could make readers debate the older BR wording before reaching the correction | Fixed by rewriting affected original rules and deleting BR-48 | `rg`, `apply_patch`, `git diff --check` | Use the original BR numbers as the single source of truth for developer visibility, developer note, status note/reason, and comment rules |
| 2026-06-10 | SAP490 artifact review | Checked DOCX/XLSX files under `docs/sap490` after Sprint 02 backend and business-rule updates | Confirmed templates remain source templates only; the only generated SAP490 workbook is a demo artifact from 2026-06-03 and is not a current submission file | No tool/runtime bug found; artifact gap found: no generated SAP490 DOCX and no updated generated Sprint 02 test/report workbook exist yet | Open as follow-up, not a code bug | `Get-ChildItem docs/sap490 -Recurse`; `openpyxl` read-only workbook inspection | If mentor needs SAP490-format files, generate new copies from templates for Test/Fix Bug, Test Report, and optionally Functional/Technical Specification instead of editing template originals |
| 2026-06-10 | SAP490 Blueprint draft | Created initial Blueprint v0.1 content as Markdown source and Pandoc-generated DOCX | Initial files were `docs/sap490/generated/Blueprint_IDTS_SAP01_v0.1.md` and `.docx`, but they were later removed after review | User found the Pandoc-generated DOCX did not preserve the SAP490 template form/layout well enough | Fixed by deleting the Markdown/Pandoc artifacts and recreating the Blueprint as a direct template-filled DOCX copy | `python -m pip install python-docx`; `pandoc ... --reference-doc ...`; user visual review; `Remove-Item` for the two generated artifacts | Use only the template-filled DOCX for Blueprint review |
| 2026-06-10 | SAP490 Blueprint template-fill correction | Recreated Blueprint by copying `Blueprint_Template.docx` and filling the copied DOCX directly with IDTS content | `docs/sap490/generated/Blueprint_IDTS_SAP01_TemplateFilled_v0.1.docx` created; `docs/pm/current-status.md` updated | First template-fill script attempt failed because newly inserted tables changed `doc.tables` indices before the original template tables were referenced | Fixed by capturing original table references immediately after opening the copied template, then rerunning generation from a fresh template copy | `python-docx` inspection showed 66 paragraphs, 14 tables, 3 sections; sample `SD-MD-01 Customer Master Data` absent; IDTS title/process/nextProcessor present; `soffice --headless --convert-to pdf` created PDF length 638042; `git diff --check`; `npx ai-devkit@latest lint --json` | Review the template-filled DOCX visually in Word/LibreOffice before mentor submission |
| 2026-06-10 | SAP490 documentation rules | Updated `AGENTS.md` with stricter SAP490 language split, template-copy-fill, formatting preservation, and verification rules | Agent rules now require separate English/Vietnamese SAP490 files and require non-Markdown SAP490 files to be copied from templates before filling | Process issue found: prior SAP490 wording could still be interpreted as allowing final DOCX/XLSX/PPTX generation from Markdown/Pandoc | Fixed in `AGENTS.md`; existing SAP490 artifacts should follow the new EN/VI split and template-fill rule when recreated | `apply_patch`; `rg`; `git diff --check`; `npx ai-devkit@latest lint --json` | Future SAP490 Blueprint/Test Report/Specification files should be recreated as separate EN and VI template-filled copies |
| 2026-06-10 | SAP490 Blueprint EN/VI correction | Recreated the SAP490 Blueprint as separate English and Vietnamese DOCX files by copying and filling `Blueprint_Template.docx`; added a repeatable generator script and aligned SAP490 markdown docs with the EN/VI split rule | `docs/sap490/generated/Blueprint_IDTS_SAP01_en_v0.1.docx` and `docs/sap490/generated/Blueprint_IDTS_SAP01_vi_v0.1.docx` created; old mixed-language single Blueprint file removed; SAP490 workflow/setup/inventory markdown split into `.en.md` and `.vi.md` files | Prior Blueprint artifact still violated the new EN/VI split rule; SAP490 workflow docs still implied Markdown/Pandoc-style generation for final files; template inventory split initially failed because it used interleaved bilingual sections instead of a single marker | Fixed by generating two template-filled DOCX files with the same table dimensions as the template, splitting SAP490 markdown docs by language, repairing Vietnamese headings, and updating PM/status references | `python scripts/sap490/generate-blueprint-docx.py`; `python-docx` structure/content check; LibreOffice PDF conversion; `rg` old-reference/mojibake checks; `git diff --check`; `npx ai-devkit@latest lint --json` | Open both DOCX files in Word/LibreOffice for final visual review before mentor submission |
| 2026-06-15 | Jira and pull-request audit | Queried Jira `IDTS` through Atlassian Rovo, fetched GitHub branches and PRs, and synchronized PM handover summaries | Confirmed 6 Done, 4 In Progress, 2 To Do; confirmed 2 open PRs and 0 merged PRs before integration | PM docs were stale; GitHub CLI `gh` is not installed on PATH | PM docs updated; GitHub REST API used as a successful fallback. Missing `gh` remains non-blocking | Atlassian Rovo `_searchjiraissuesusingjql`; `git fetch --prune origin`; GitHub REST API; `git diff --check` | Review and integrate PR #1, PR #2, and completed DonHV branches |
| 2026-06-15 | DatDT UI branch pre-merge audit | Inspected all local/remote branches, commit authors, UI diffs, PR history, and simulated combining DatDT's `Remake_UI` with SangVN's PR #2 branch | Confirmed `Remake_UI` is DatDT's only separate remote UI branch and contains one commit ahead of `dev` | `Remake_UI` has no PR and conflicts with PR #2 in `app/bug-management-ui/annotations.cds`; it also relaxes Create required fields and changes CreationDialog to NewPage | Open merge risk; do not merge wholesale. Review or cherry-pick only accepted changes after PR #2 review | `git for-each-ref`; GitHub branches/PR API; `git log --all`; `git diff`; `git merge-tree --write-tree` | Review PR #2 first, then compare and selectively integrate DatDT changes |
| 2026-06-15 | Sprint 02 branch integration | Consolidated PR #1, PR #2, DonHV backend branch, and DonHV SAP490 documentation branch on an integration branch from `origin/dev`; resolved PM handover conflicts by preserving both implementation and SAP490 history | Four requested sources integrated and pushed to `dev` at `eb1df70` | SAP490 branch conflicted with newer PM audit updates in `current-status.md` and `status/donhv.md` | Fixed by preserving SAP490 deliverables and merging the newer Jira/PR audit content | GitHub confirms PR #1 and PR #2 merged; CAP compile/deploy passed; backend suite passed 21/21; UI5 build passed; `git diff --check` clean | Review DatDT `Remake_UI` selectively; do not merge it wholesale |
| 2026-06-15 | IDTS-5 integration QA fix | Reproduced and fixed the SC-01a Create failure in the merged IDTS-6 programmatic QA script | Full happy-flow suite now passes 21/21 | Direct CAP CREATE dispatch had no `INSERT` query and no explicit test UUID, causing HTTP 501 and then a NOT NULL ID error during diagnosis | Fixed in the test harness only; production handlers unchanged. Jira `IDTS-5` comment `10134` records the issue and evidence | Pre-fix `node scripts/qa/test-idts6-programmatic.js` = 20 PASS / 1 FAIL; post-fix = 21 PASS / 0 FAIL; CAP MCP `search_docs` | Continue final integration verification and push to `dev` |
| 2026-06-15 | SAP490 backend test deliverables | Copied and filled the school Test Scenario, Unit Test, and Test and Fix Bug templates as separate EN/VI workbooks; added a repeatable generator | Six v0.1 XLSX files document 12 scenarios, 21 passing cases, and the fixed `IDTS-5` SC-01a issue | Spreadsheet artifact tool was unavailable (`ERR_MODULE_NOT_FOUND`); initial PDF preview stack lacked `pypdfium2` and Pillow; first render exposed clipped Fix text, an orphan Unit Test row, and unused matrix columns; work initially started on `dev` before the branch rule was rechecked | Fixed by using the previously approved `openpyxl` template-copy fallback, installing `pypdfium2`/Pillow, shortening text, balancing the page break, setting the scenario matrix print area/fit-to-page, and moving all uncommitted work to a dedicated branch before commit | `python scripts/sap490/generate-test-deliverables.py`; openpyxl structure/formula/language scan; LibreOffice PDF conversion for all six files; PDF visual review; backend suite 21/21 | Mentor/team reviews the six v0.1 workbooks before Drive upload or school submission |
| 2026-06-15 | WP4 Create Flow Option B | Completed Option B: removed `InsertRestrictions.RequiredProperties`, added `@Common.FieldControl: #Mandatory` for 9 fields, added the `New Bug` i18n title, and removed unused `FieldGroup#CreateBug` | Create navigates directly to the full draft Object Page, required indicators remain visible, empty activation is blocked, and complete activation succeeds | Port 4004 was occupied by a leftover CDS process; `cds-plugin-ui5` welcome-page error remains non-blocking; initial status update accidentally shortened three earlier session-log rows | Port conflict fixed; earlier session-log details restored; welcome-page error remains a known non-blocking local warning | EDMX has 9 `FieldControl/Mandatory` and 0 `RequiredProperties`; SQLite deploy pass; QA 21/21 PASS; empty draft activation HTTP 400; complete draft activation HTTP 201; browser smoke pass; Fiori MCP and CAP MCP | Push corrected Option B documentation, then implement Option C on a separate branch |
| 2026-06-16 | WP4 Create Flow Option B | Fixed Assignee value help, related value-list labels, and dynamic lookup filtering (via draft PATCH handler, SideEffects, and AssignableDevelopers); hardened backend reporter_ID; documented C-prime as the target UI refinement baseline | Assignee popup shows business columns, filters candidates dynamically, and displays developer display name; C-prime layout is recorded as the target next refinement, not yet a separate verified implementation pass | None | Fixed for value help; C-prime layout implementation remains a WP4 refinement follow-up | Playwright browser smoke test, OData response, cds compile srv app/bug-management-ui --to edmx, node scripts/qa/test-idts6-programmatic.js | Hand over to DatDT/SangVN for final verification and C-prime layout implementation decision |
| 2026-06-16 | SAP490 Functional/Test deliverables for WP4 | Generated Functional Specification v0.1 and Test and Fix Bug v0.2 as separate EN/VI template-filled workbooks; added repeatable generator script | Four workbook artifacts created under `docs/sap490/generated/` and PM/WP4 docs updated | Issues found: first generator run failed on `MergedCell` writes; PowerShell rejected bash-style `python - <<`; Python console hit `UnicodeEncodeError` when printing Vietnamese through default code page; one verification attempt read a workbook while generator was still writing because dependent commands were accidentally run in parallel | Fixed by resolving merged-cell anchors, using PowerShell here-string syntax, setting `PYTHONIOENCODING=utf-8`, and rerunning generator/inspection sequentially | `python scripts/sap490/generate-functional-and-bugfix-deliverables.py`; `python -m py_compile scripts/sap490/generate-functional-and-bugfix-deliverables.py`; `openpyxl` workbook inspection; `soffice --headless --convert-to pdf` for all four workbooks | Review Functional Spec v0.1/Test and Fix Bug v0.2 visually in Excel/LibreOffice before mentor submission; decide separately whether to generate Technical Specification |
| 2026-06-16 | WP4 UI & Action Fix | Fixed UUID display on Object Page by adding @Common.Text; corrected assignToDeveloper action parameter to use AssignableDevelopers | Resolved UUID classification issue and action value help parameter | None | Fixed | cds compile, backend tests 21/21 PASS, browser subagent smoke test | Ready for team review |
| 2026-06-16 | WP4 Actions Visibility | Implemented dynamic Object Page header actions visibility using backend capabilities and disabled physical delete | Hiding/disabling standard delete and dynamic visibility logic completed and visually verified | Invalid CDS 'not canXxx' syntax initially caused compilation error | Fixed by using $edmJson $Not logical negation | cds compile, backend tests 21/21 PASS, browser subagent smoke test | Ready for Sprint 2 Demo |
| 2026-06-16 | WP4 Demo Safety & Performance | Configured mock users for local login, separated developer and coordinator action visibility, optimized calculations to O(1) query, and relocated walkthrough to repository | Mock login, dynamic action bar, walkthrough document, and performance optimized | None | Fixed | package.json, service.js, docs/pm/tasks/wp4-walkthrough.md, node scripts/qa/test-idts6-programmatic.js | Sprint 2 Demo ready |

Vietnamese note: Ngày 2026-06-10, DonHV đã hoàn thành `IDTS-2` và `IDTS-4`, chuyển hai Jira issue này sang Done, đồng thời mở `IDTS-5` ở trạng thái In Progress để tiếp tục nhận bug backend từ QA. Các gap backend đã fix gồm: thiếu kiểm soát quyền xử lý theo request user, bound action chưa ghi history cho thay đổi `nextProcessor`, và các trạng thái resolved/retest/reopened chưa tạo notification record. Warning `cds-plugin-ui5` về welcome page vẫn còn nhưng không chặn compile, deploy hoặc OData smoke test.

Vietnamese note: Ngày 2026-06-10, DonHV đã cleanup business rules theo hướng single source of truth: bỏ BR-48 vì nội dung refine bị dư thừa, đưa rule cuối cùng về BR-02, BR-18, BR-25 và BR-27, đồng thời sync wording liên quan trong `IDTS-SUMMARY.md`, `IDTS-PROJECT-SCOPE-SAP01.md` và `docs/project-context.md`.

Vietnamese note: Ngày 2026-06-10, DonHV đã cập nhật `AGENTS.md` để rule SAP490 rõ hơn: mọi artifact SAP490 do project viết phải tách file tiếng Anh và tiếng Việt riêng; file DOCX/XLSX/PPTX chính thức phải copy từ template rồi fill, không generate từ Markdown; khi fill phải giữ font, cỡ chữ, màu, bảng, formula, header/footer, cover page và kiểm tra lỗi tiếng Việt trước khi báo hoàn tất.

Vietnamese note: Ngày 2026-06-10, DonHV đã sửa lại SAP490 Blueprint theo rule mới: tạo hai file riêng `Blueprint_IDTS_SAP01_en_v0.1.docx` và `Blueprint_IDTS_SAP01_vi_v0.1.docx` bằng cách copy/fill `Blueprint_Template.docx`, xóa bản single-file trộn ngôn ngữ cũ, thêm script generate lại, đồng thời tách các markdown SAP490 đang được cập nhật thành file `.en.md` và `.vi.md` để không còn hiểu nhầm dùng Markdown/Pandoc cho file SAP490 chính thức.

Vietnamese:

| Ngày | Task/WP | Đã làm gì | Phần đã xong | Khó khăn/Bug phát hiện | Trạng thái fix | Bằng chứng/Lệnh đã chạy | Handoff tiếp theo |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-06-16 | Khóa bảng phụ WP5/WP7 | Thêm UI.CreateHidden, UI.DeleteHidden vào các projection Comments, HistoryLogs, Notifications, và chuyển trường content của Comments sang ReadOnly; verify bằng browser_subagent | Khóa cứng các bảng Comments, History, Notifications dạng read-only trong Edit mode, ẩn hoàn toàn nút Create/Delete và ô nhập liệu | Fiori Elements hiển thị nút Create trên Notifications table do cấu trúc draft composition dù Insertable: false | Sửa bằng cách thêm các annotation explicit UI.CreateHidden, UI.DeleteHidden và khóa trường content | browser_subagent, cds compile, 21/21 backend tests pass | Sẵn sàng demo Sprint 2 |
| 2026-06-03 | PM setup | Đổi mô hình status từ file theo workstream sang file theo thành viên và giữ DonHV là leader/BA-PM owner | Đã reset ownership status của team | Không có | Đã xử lý | `rg`, `git diff --check` | Thành viên cập nhật file status của mình |
| 2026-06-03 | SAP490 docs | Tạo inventory và hướng dẫn sử dụng template SAP490 | Đã document mục đích và cách fill template | Không có | Đã xử lý | `openpyxl`, `rg`, `git diff --check` | Dùng inventory trước khi điền template SAP490 |
| 2026-06-04 | Agent learning workflow | Thêm Learning Recap / Mentor Mode tùy chọn trong AGENTS.md và `.agents/skills/learning-recap` | Đã tích hợp workflow dạy lại và teach-back | Không có | Đã xử lý | `rg`, `git diff --check`, `npx ai-devkit@latest lint --json` | Dùng sau task không tầm thường khi user yêu cầu hoặc khi hữu ích |
| 2026-06-05 | Sprint 02 planning | Sync feedback mentor vào canonical docs, tạo Sprint 02 plan, cập nhật PM handover, tạo Jira backlog và thêm rule track bug bằng Jira vào AGENTS.md | Đã hoàn thành baseline planning Sprint 02, Jira backlog và rule agent về bug tracking | Lần append `IDTS-SUMMARY.md` đầu tiên fail vì console hiển thị mojibake dù file là UTF-8 sạch | Đã fix bằng cách kiểm tra text thật bằng `Select-String` và patch lại bằng UTF-8 đúng | `Get-Content`, `Select-String`, `apply_patch`, Atlassian Rovo MCP `_createjiraissue`, `_searchjiraissuesusingjql`; Jira issues `IDTS-1` đến `IDTS-12`; đã verify sau bằng `git diff --check` | Bắt đầu backend handler updates cho Sprint 02 |
| 2026-06-06 | Sprint 02 Jira assignment | Assign Jira issues `IDTS-1` đến `IDTS-12` cho DonHV, NhanT, DatDT và SangVN theo mapping Sprint 02 đã cân bằng; sửa description bị lệch owner cho `IDTS-3`, `IDTS-10`, `IDTS-12`; cập nhật `docs/pm/07-sprint-2-plan.md` cho khớp Jira | Đã hoàn tất assign Jira và đồng bộ sprint plan | Description ban đầu của `IDTS-3`, `IDTS-10`, `IDTS-12` vẫn ghi owner cũ sau khi đổi assignee | Đã fix bằng cách cập nhật description qua Atlassian Rovo MCP | `tool_search`, Atlassian Rovo MCP `_editjiraissue`, `_searchjiraissuesusingjql`, `apply_patch`, `rg`, `git diff --check` pass với warning LF-to-CRLF | Mỗi thành viên bắt đầu task Jira được assign và cập nhật status file của mình sau phiên làm việc |
| 2026-06-15 | Deliverable kiểm thử SAP490 backend | Copy và fill template Test Scenario, Unit Test và Test and Fix Bug thành các workbook EN/VI riêng; thêm generator chạy lặp lại | Sáu file XLSX v0.1 ghi 12 scenario, 21 case pass và lỗi SC-01a `IDTS-5` đã sửa | Spreadsheet artifact tool không khả dụng (`ERR_MODULE_NOT_FOUND`); preview PDF ban đầu thiếu `pypdfium2` và Pillow; render lần đầu phát hiện nội dung Fix bị cắt, một dòng Unit Test bị tách riêng và matrix còn cột in trống; task ban đầu được làm trên `dev` trước khi kiểm tra lại branch rule | Đã dùng fallback `openpyxl` đã được duyệt để fill bản copy template, cài renderer, rút gọn text, cân bằng page break, thiết lập print area/fit-to-page và chuyển toàn bộ thay đổi chưa commit sang branch riêng trước khi commit | `python scripts/sap490/generate-test-deliverables.py`; scan cấu trúc/formula/ngôn ngữ bằng openpyxl; LibreOffice convert PDF cả sáu file; visual review PDF; backend suite 21/21 | Team/mentor review sáu workbook v0.1 trước khi upload Drive hoặc nộp |

| 2026-06-04 | BA/database readiness | Tách diagram trong SRS/FRS vào `docs/diagrams`, cài hỗ trợ database modeling và viết review database model hiện tại | Đã hoàn tất diagram pack và baseline review database | DBML CLI thử nghiệm bị timeout và làm tăng audit noise | Đã xử lý bằng cách gỡ DBML CLI và chỉ giữ database skills | `npx skills add`, `npm uninstall @dbml/cli`, `cds compile srv --to edmx`, `rg`, `git diff --check` | Chốt các câu hỏi DB còn mở, rồi bắt đầu WP1 Data Model Foundation |
| 2026-06-04 | WP1 database baseline | Chuyển DB-Q01 đến DB-Q08 từ câu hỏi mở thành quyết định database rõ ràng cho WP1 và sync vào canonical docs | Đã hoàn tất baseline quyết định database | Không có | Đã xử lý | `rg`, `cds compile srv --to edmx`, `npx ai-devkit@latest lint --json`, `git diff --check` | Backend có thể bắt đầu WP1 sau khi đọc `docs/ba/09-database-model-review.md` |
| 2026-06-04 | MCP troubleshooting | Kiểm tra vì sao CAP MCP tools chưa hiện và document cách lazy-load bằng `tool_search` | Đã document workflow expose CAP MCP | Namespace CAP MCP bị deferred trong Codex session hiện tại | Đã xử lý bằng `tool_search`; đã verify CAP `search_docs` và `search_model` | `tool_search`, `mcp__cap.search_docs`, `mcp__cap.search_model`, `npx -y @cap-js/mcp-server --help` | Nếu namespace MCP thiếu, dùng `tool_search` trước |
| 2026-06-16 | WP4 Create Flow Option B | Tinh chỉnh annotations để ẩn động các section/field bằng HasActiveEntity; sửa lỗi filter Value Help Assignee bằng draft PATCH handler ở backend và SideEffects ở frontend; thắt chặt backend ghi đè reporter_ID; ghi nhận C-prime là target UI refinement baseline | Hoàn thành phần Value Help Assignee, SideEffects và bảo mật reporter_ID; C-prime là hướng refinement tiếp theo, chưa phải một pass implement/verify riêng | Không có | Đã xử lý phần value help; C-prime layout còn là follow-up WP4 | Playwright browser smoke test, OData response verify, npx cds compile srv app/bug-management-ui --to edmx | Bàn giao cho DatDT/SangVN kiểm tra giao diện cuối cùng và quyết định implement C-prime |
| 2026-06-16 | SAP490 Functional/Test deliverables for WP4 | Tạo tài liệu Functional Specification v0.1 và Test and Fix Bug v0.2 bằng cách điền từ template tiếng Anh/tiếng Việt | Đã tạo 4 workbook trong docs/sap490/generated/ và cập nhật PM/WP4 | Lỗi merge-cells trong openpyxl, lỗi Unicode tiếng Việt khi in log PowerShell | Đã sửa bằng cách điều chỉnh code generator | python scripts/sap490/generate-functional-and-bugfix-deliverables.py, openpyxl check, soffice convert PDF | Review trước khi nộp; cân nhắc tạo Technical Specification |
| 2026-06-16 | WP4 UI & Action Fix | Sửa lỗi hiện UUID ở phần Classification bằng Common.Text; sửa parameter của action assignToDeveloper dùng AssignableDevelopers | Đã hiển thị tên thật thay vì UUID trên UI; đã sửa value help cho action | Không có | Đã xử lý | cds compile, test suite 21/21 PASS, verify bằng browser subagent thành công | Bàn giao cho team review |
| 2026-06-16 | WP4 Actions Visibility | Implement ẩn hiện động action trên Object Page header bằng virtual boolean và cấm xóa vật lý | Hoàn tất ẩn nút delete tiêu chuẩn và ẩn hiện động các custom actions theo status/role | Lỗi biên dịch cú pháp 'not canXxx' trong CDS | Sửa bằng cách dùng biểu thức phủ định logic $edmJson $Not | cds compile, test suite 21/21 PASS, verify bằng browser subagent thành công | Sẵn sàng cho demo Sprint 2 |
| 2026-06-16 | WP4 Demo Safety & Performance | Cấu hình mock users cho local login, phân tách quyền hiển thị action bar của Developer và Coordinator, tối ưu hóa database query thành O(1) và đưa walkthrough vào repository | Mock login, dynamic action bar, walkthrough document và hiệu năng tối ưu hóa | Không có | Đã xử lý | package.json, service.js, docs/pm/tasks/wp4-walkthrough.md, node scripts/qa/test-idts6-programmatic.js | Sẵn sàng demo Sprint 2 |

## SAP490 Bug Staging Register

Temporary consolidated bug list for later transfer into SAP490 test / fix deliverables.

Vietnamese: Danh sách bug tạm thời được gom lại để chuẩn bị điền vào các file SAP490 test / fix bug.

| Temp ID | Area | Bug / Symptom | Current status | Root cause or note | Suggested SAP490 target |
| --- | --- | --- | --- | --- | --- |
| TMP-BUG-01 | Backend / Attachments | Attachment upload created metadata but `content` stayed null after draft activation; download/content endpoint then returned empty or invalid result | Fixed | Custom child-media flow did not materialize `PUT .../content` body into `req.data.content` | Test And Fix Bug, Functional Specification |
| TMP-BUG-02 | Fiori / Value Help | Assignee value help showed UUID-style fields and selection could end with `Not Found` / technical values instead of business names | Fixed | Value help and display text were bound to technical IDs / incomplete text handling | Test And Fix Bug, Functional Specification |
| TMP-BUG-03 | UI / Object Page | Status was editable manually on Bug Detail although workflow should be action-driven and role-controlled | Fixed | UI field metadata was not fully locked to read-only | Test And Fix Bug, Functional Specification |
| TMP-BUG-04 | Backend / Authorization | Role-based header actions disappeared unexpectedly for valid users or showed wrong behavior after login | Fixed | Mixed causes: stale server, omitted FE fields, case-sensitive user mapping, capability enrichment gaps | Test And Fix Bug, Technical Specification |
| TMP-BUG-05 | UI / Child Tables | Comments, History, Notifications remained editable in bug Edit mode when they should be read-only | Fixed | Missing create/delete hidden restrictions and incomplete field locking | Test And Fix Bug, Functional Specification |
| TMP-BUG-06 | Backend / Draft | Draft edit could fail with unique constraint conflict around history ownership | Fixed | `Bugs.historyLogs` ownership conflicted with grouped `HistoryEvents.logs` during draft handling | Test And Fix Bug, Technical Specification |
| TMP-BUG-07 | UI / Attachments | Attachments section was hidden or locked out in some UI states even when backend path existed | Fixed | Annotation/facet visibility and stream-table configuration were incomplete during intermediate iterations | Test And Fix Bug, Functional Specification |
| TMP-BUG-08 | UI / Readability | Comments/History tables displayed UUIDs instead of human-readable user names | Fixed | Missing flattened display fields / `Common.Text` usage for FE display | Test And Fix Bug, Functional Specification |
| TMP-BUG-09 | UI / History UX | History showed too many low-level rows like `nextProcessorRole`, `nextProcessorUser`, raw status deltas, making the audit table noisy | Partially improved; follow-up review may still be needed | Required grouped `HistoryEvents` read model instead of only raw `HistoryLogs` | Functional Specification, possibly Technical Specification |
| TMP-BUG-10 | UI / Create Flow | Create Bug page did not expose attachment evidence even though attachments are valid at bug-report creation time | Fixed | Attachment facet had been intentionally hidden for brand-new draft create mode; Option A reopened it | Functional Specification, demo script update after fix |
| TMP-BUG-11 | Data / UX | Create page allows selecting invalid Application Component and Defect Category combinations that later fail on save | Fixed | `ValidDefectCategories` + `Common.ValueListParameterIn` now filter defect-category rows by selected component. Browser rerun confirmed `IDTS Fiori UI` only exposes `SAP Fiori UI5`, while `IDTS Bug Report` exposes only its valid categories. | Test And Fix Bug, Functional Specification, seed-data review |
| TMP-BUG-12 | UI / Attachment Refresh | Attachment uploaded during create draft disappears on the active Object Page right after save | Fixed | Fresh browser rerun on `localhost:4012` confirmed the uploaded file row remains visible immediately after create on the active Object Page. | Test And Fix Bug, Functional Specification, Technical Specification |
| TMP-BUG-13 | UI / Action Refresh | Object Page action buttons remain stale after lifecycle actions even when backend state changed | Fixed | Bound-action side effects now refresh capability/action state correctly. Browser rerun showed tester/developer/PM action sets changing to the right state during the happy flow. | Test And Fix Bug, Functional Specification |
| TMP-BUG-14 | UI / Action Parameter UX | `Assign Developer` action dialog still shows the selected developer as a UUID in the input after selection | Open | The parameter label is now business-friendly (`Assignee`) and value-help rows are readable, but the selected field value still renders the developer profile UUID instead of `DatDT`. | Functional Specification, UX backlog |
| TMP-BUG-15 | UI / Assignment Workflow Consistency | Editing the `Assignee` field directly on Object Page can bypass the intended `Assign Developer` action flow, so the bug may keep `NEW` status and create no assignment notification | Code-side fixed, UI retest pending | `prepareBugWrite` only auto-derives `ASSIGNED` on create, not on update; generic update side effects only send notifications when a status change exists; the form exposed editable `assignee_ID` in `UI.FieldGroup#Assignment`. Current fix locks `assignee` read-only and adds a section-level `Assign Developer` action instead. | Test And Fix Bug, Functional Specification, Technical Specification |
| TMP-BUG-16 | UI / Edit Mode Governance | Generic Object Page edit mode still exposes workflow-sensitive fields such as `rejectionReason`, `plannedCompletionDate`, `dueDate`, and `estimatedEffortHours`, although the current MVP flow expects rejection to go through dedicated actions and planning is not part of the current happy-flow edit path | Code-side fixed, UI retest pending | `RejectedFollowUp` and `Planning` facets were previously only hidden for brand-new create draft, not for existing-bug edit mode; the underlying fields had no read-only or hide rule that matched the current business flow. Current fix makes the fields read-only, restricts `Rejected Follow-up` to rejected bugs, and hides `Planning` from the MVP Object Page. | Test And Fix Bug, Functional Specification, Technical Specification |
| TMP-BUG-17 | Data / Audit Integrity | A bug-edit governance gap allowed the local `Users.displayName` master data for `NhanT` to drift to `NhanTTestUserTesting User`, which then retroactively changed actor/reporter/comment names across old bug reports | Fixed for the current local/demo path | Root cause was a combination of writable user-facing bug fields/navigation-path display bindings and an unguarded service projection for master data. The local `db.sqlite` row was restored to `NhanT`, bug read-model display fields were flattened on `BugService.Bugs`, and `BugService` now rejects `CREATE/UPDATE/PATCH/DELETE` on `Users`, `DeveloperProfiles`, code lists, and system-managed audit/notification entities. Remaining optional improvement: snapshot actor/author names into history/comment records if the project later wants audit text to stay immutable even when user master data legitimately changes. | Test And Fix Bug, Technical Specification, possibly Functional Specification if audit-display behavior is clarified |
| TMP-BUG-18 | Backend / CAP Draft Projection | Fiori preview List Report crashed on startup with SQLite error `no such column: $d.nextProcessorRoleName` after introducing flattened display fields on draft-enabled `BugService.Bugs` | Fixed | Root cause was two-layered: 1. inline projection expressions on a draft-enabled service entity caused CAP SQLite draft runtime to look for a non-physical draft-table column; 2. after converting them to `virtual` fields, the `after READ` enrichment still depended on `hasOwnProperty`, so CAP rows that omitted the virtual properties never received the display values. The final fix uses `virtual` service fields plus unconditional `after READ` enrichment. | Test And Fix Bug, Technical Specification |
| TMP-BUG-19 | Data / Master Data Drift | `nextProcessorRoleName` rendered corrupted values such as `Developerrrr` and `TesterTesting Role` because the local `db.sqlite` rows in `ProcessorRoleValues` had drifted away from the seed CSV. | Fixed | Seed file `db/data/idts.cap-ProcessorRoleValues.csv` was already correct; the live local database had mutated role names. Restoring the five rows to the seed values fixed the display immediately in OData payloads. | Test And Fix Bug, Technical Specification |
| TMP-BUG-20 | Audit / History Coverage Gap | Editing business fields like `title` and `description` does not create meaningful history rows, while adding a comment does. | Open | `importantChanges()` currently tracks only `status`, `assignee`, `sapModule`, `applicationComponent`, `defectCategory`, `componentCategory`, `nextProcessorUser`, `nextProcessorRole`, and `rejectionReason`. An isolated in-memory service update proved that changing `title` and `description` generated no `title`/`description` history rows; only a derived `nextProcessorUser` row was written. Separate in-memory `addComment` verification produced `HistoryLogs.fieldName = comment` correctly. | Test And Fix Bug, Functional Specification, Technical Specification |
| TMP-BUG-21 | Workflow / Status UX Gap | `Need More Information` and `Rejected` were technically transitionable, but the product lacked a clean Tester-facing `resubmit` happy flow. | Fixed for MVP | Root cause was that the backend transition matrix allowed the status concepts, but the product exposed no explicit Tester/PM action to return a `Need More Information` bug to the assigned Developer. The fix added `resubmitToDeveloper(note)` with required update summary, grouped history, follow-up comment, notification, and `nextProcessor` reset to the assigned Developer; `Rejected` was also tightened so it no longer routes back through `Need More Information` in MVP. | Functional Specification, UX backlog, Technical Specification |
| TMP-BUG-22 | Workflow / Status Model Mismatch | The documented lifecycle still presents `New` as the initial persisted status, but the current create handler writes `ASSIGNED` or `PENDING_ASSIGNMENT` directly. | Open | `prepareBugWrite()` sets `req.data.status_code = finalData.assignee_ID ? STATUS.ASSIGNED : STATUS.PENDING_ASSIGNMENT` on create, so fresh bug creation does not currently persist a stable `NEW` state. Existing `NEW` examples in the live database are legacy/dirty records rather than the result of the current create flow. | Functional Specification, Technical Specification, canonical docs after design decision |

Vietnamese note: DonHV đã hỗ trợ WP1 Data Model Foundation cho Backend CAP. Kết quả: `db/schema.cds`, `srv/service.cds` và seed data trong `db/data/` đã compile/deploy SQLite in-memory pass. Handler nghiệp vụ vẫn để mở cho WP3.

Vietnamese note: DonHV đã hỗ trợ WP4 Fiori Elements UX bằng cách review repo `Sap_FE` của DatDT, tích hợp các ý tưởng List Report/Object Page phù hợp vào app CAP/Fiori hiện tại, và thêm bốn bug demo để List Report có dữ liệu khi review local. CAP compile, SQLite deploy, UI5 build, và Playwright browser smoke đã pass; dependent value help/action hiện đã được cập nhật ở mức MVP trong lượt WP2/WP3/WP4 tiếp theo, upload attachment thật vẫn chờ WP7.

Vietnamese note: DonHV đã hỗ trợ tiếp WP2/WP3/WP4 bằng cách thêm value help, bound actions, `srv/service.js` handlers, nextProcessor automation, history/notification side effects và Object Page actions. WP2/WP3 đã hoàn thành ở mức MVP; WP4 core screens đã cập nhật nhưng vẫn cần QA sâu, usability tuning và upload attachment thật ở WP7.

Vietnamese note: DonHV đã tinh chỉnh tiếp WP4 UI sau khi review giao diện: đổi List Report sang `GridTable`, bật draft trên `BugService.Bugs` để standard Create button xuất hiện, đổi Create thành `CreationDialog` với `UI.FieldGroup#CreateBug`, và bỏ icon mặc định khỏi Status/Priority/Severity bằng `CriticalityRepresentation : #WithoutIcon`. CAP compile, UI5 build và browser click mở dialog đã pass; DatDT/NhanT vẫn nên kiểm tra thủ công save flow của Create Bug trên browser.

## 2026-06-15 Selective Fiori UI Integration Log

English:

| Field | Detail |
| --- | --- |
| Task/WP | WP4 Fiori Elements selective `Remake_UI` integration |
| What was done | Reviewed DatDT's `Remake_UI` changes after PR #1, PR #2, and DonHV branches were merged to `dev`; applied only Assignment-first Object Page ordering, read-only History table capability annotations, and `NewPage` create flow evaluation. |
| Completed part | CAP/Fiori annotations compile, backend QA suite passes, UI5 build passes, and browser smoke showed List Report rows, Object Page section order, Assignment fields, History table, and Create flow behavior. |
| Issues/Bugs found | `CreationDialog` was too crowded for IDTS because the create form has many required fields; local Fiori dev server logs non-blocking 404s for `Component-preload.js` and `/sap/bc/lrep/flex/*`; `cds-plugin-ui5` logs a non-blocking welcome-page injection warning; GridTable text click did not navigate during smoke testing and the Details icon was used instead; after stopping the visible server PIDs, an npm/cds child process still held `.tmp/cds-watch-ui.log`; Atlassian MCP rejected the short site string `fpt-team-jvtyghn.atlassian.net` with tenant 404. |
| Fix status | Create flow changed to `NewPage`; History read-only and Assignment-first layout fixed. Local dev 404/welcome-page noise is open but non-blocking because direct app URL works and UI5 build creates preload assets. GridTable text navigation remains an open UX note for DatDT/SangVN manual QA. The locked `.tmp` log was fixed by locating and stopping the remaining npm/cds child PIDs, then deleting `.tmp`. Jira lookup was fixed by using Rovo search to find cloud ID `a22b9679-1f1f-45dd-bee7-be63304bc233`. |
| Evidence/Commands | `mcp__fiori.search_docs`; `mcp__cap.search_model`; `cds compile srv app/bug-management-ui --to edmx`; `cds deploy --to sqlite::memory:`; `node scripts/qa/test-idts6-programmatic.js` = 21 PASS / 0 FAIL; `npx ui5 build --config ui5.yaml --clean-dest --dest $env:TEMP\\idts-ui-selective-build`; browser smoke on `http://localhost:4004/idts.bugmanagementui/index.html?sap-ui-xx-viewCache=false`; Atlassian Rovo comment `IDTS-10` / `10135`. |
| Next handoff | Selective branch merged into `dev` and verified. DatDT/SangVN should manually verify full create-save behavior and Bug Detail usability; DonHV keeps backend happy-flow suite passing as the merge baseline. |

Vietnamese:

| Trường | Chi tiết |
| --- | --- |
| Task/WP | WP4 Fiori Elements tích hợp chọn lọc `Remake_UI` |
| Đã làm gì | Review thay đổi `Remake_UI` của DatDT sau khi PR #1, PR #2 và các branch của DonHV đã merge vào `dev`; chỉ lấy các phần đưa Assignment lên đầu Object Page, đặt History table read-only, và đánh giá create flow bằng `NewPage`. |
| Phần đã xong | CAP/Fiori annotations compile pass, backend QA suite pass, UI5 build pass, browser smoke thấy List Report rows, thứ tự Object Page section, Assignment fields, History table và Create flow. |
| Bug/lỗi phát hiện | `CreationDialog` quá chật với IDTS vì form tạo bug có nhiều required fields; dev server Fiori local có 404 không chặn cho `Component-preload.js` và `/sap/bc/lrep/flex/*`; `cds-plugin-ui5` có warning không chặn khi inject welcome page; khi smoke test GridTable, click vào text không navigate nên phải dùng icon Details; sau khi dừng visible server PID, vẫn còn npm/cds child process giữ `.tmp/cds-watch-ui.log`; Atlassian MCP báo tenant 404 khi dùng site string ngắn `fpt-team-jvtyghn.atlassian.net`. |
| Trạng thái fix | Đã đổi create flow sang `NewPage`; đã fix History read-only và Assignment-first layout. 404/welcome-page noise của local dev vẫn mở nhưng không chặn vì direct app URL chạy được và UI5 build có tạo preload assets. GridTable text navigation là UX note còn mở cho DatDT/SangVN manual QA. Lỗi lock `.tmp` đã fix bằng cách tìm và dừng các npm/cds child PID còn lại rồi xóa `.tmp`. Lỗi Jira lookup đã fix bằng Rovo search để lấy cloud ID `a22b9679-1f1f-45dd-bee7-be63304bc233`. |
| Bằng chứng/lệnh | `mcp__fiori.search_docs`; `mcp__cap.search_model`; `cds compile srv app/bug-management-ui --to edmx`; `cds deploy --to sqlite::memory:`; `node scripts/qa/test-idts6-programmatic.js` = 21 PASS / 0 FAIL; `npx ui5 build --config ui5.yaml --clean-dest --dest $env:TEMP\\idts-ui-selective-build`; browser smoke tại `http://localhost:4004/idts.bugmanagementui/index.html?sap-ui-xx-viewCache=false`; Atlassian Rovo comment `IDTS-10` / `10135`. |
| Handoff tiếp theo | Nhánh selective đã merge vào `dev` và đã verify. DatDT/SangVN cần kiểm tra thủ công full create-save behavior và usability Bug Detail; DonHV giữ backend happy-flow suite pass làm baseline sau merge. |

Vietnamese note: Ngày 2026-06-16, DonHV đã hoàn thiện phần comment và mở được đường đi upload draft attachment trong CAP/Fiori bằng cách chuyển comment sang bound action `addComment`, tắt inline comment create bị lỗi trên draft child entity, và xác định đúng path `Attachments(ID=...,IsActiveEntity=false)/content`. Tuy nhiên, kết quả shell QA ngày 2026-06-17 cho thấy binary `content` của attachment vẫn chưa persist đúng vào SQLite sau draft activation, nên chưa được phép kết luận real attachment upload happy flow đã hoàn tất.

Vietnamese note: NgÃ y 2026-06-17, DonHV Ä‘Ã£ gá»¡ bá» ownership trÃ¡c tiáº¿p cá»§a `Bugs.historyLogs` Ä‘á»ƒ trá» vá» grouped `HistoryEvents`, tá»« Ä‘Ã³ váº¡ch ra lÃ½ do draft conflict vÃ  báº­t láº¡i upload attachment trÃªn Fiori annotations. CÃ²n cáº§n Claude retest UI cho draft edit vÃ  attachment upload.

## Update Rule

- DonHV updates this file for leader decisions, BA/PM work, SAP490 deliverables, weekly consolidation, and cross-workstream support.
- When DonHV supports another member's task, record the support here and mention the affected member/task file.
- Other members should not overwrite this file except when explicitly asked by DonHV.

Vietnamese:

- DonHV cập nhật file này cho quyết định của leader, việc BA/PM, deliverable SAP490, tổng hợp hằng tuần và hỗ trợ cross-workstream.
- Khi DonHV hỗ trợ task của thành viên khác, ghi phần hỗ trợ ở đây và nêu rõ file member/task bị ảnh hưởng.
- Thành viên khác không ghi đè file này trừ khi được DonHV yêu cầu rõ.
