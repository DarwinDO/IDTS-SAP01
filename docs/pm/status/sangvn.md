# SangVN Status - Fiori/UI5 Support

Last updated: 2026-07-06

Vietnamese: Trạng thái của SangVN - hỗ trợ Fiori/UI5 cho Sprint 02.

## Member Identity

| Field | Value |
| --- | --- |
| Member | SangVN |
| Primary lane | Fiori/UI5 Support |
| Shared delivery responsibility | May receive Backend CAP, Fiori/UI5, or QA/Verification tasks as assigned, but Sprint 02 priority is Fiori/UI5 support and shared delivery assistance |
| Leader support | DonHV can support or unblock this lane when needed |

Vietnamese:

| Trường | Giá trị |
| --- | --- |
| Thành viên | SangVN |
| Mảng chính | Hỗ trợ Fiori/UI5 |
| Trách nhiệm delivery chung | Có thể nhận task Backend CAP, Fiori/UI5 hoặc QA/Verification khi được phân công, nhưng ưu tiên của Sprint 02 là hỗ trợ Fiori/UI5 và hỗ trợ delivery chung khi cần |
| Leader hỗ trợ | DonHV có thể hỗ trợ hoặc gỡ blocker cho mảng này khi cần |

## Current Focus

Sprint 03 Fiori/UI5 support work: IDTS-19 grouped history timeline remains the main implementation task. IDTS-32 is now narrowed to SangVN's manual browser UAT scope for workflow/history, lifecycle, and role/action visibility coverage after DatDT received the companion FE-shell UAT task IDTS-33.

Vietnamese: Trọng tâm hiện tại là phần Fiori/UI5 của Sprint 03: IDTS-20 chỉnh label ownership đang thực hiện. Task tiếp theo là IDTS-19 grouped history timeline với UI5 extension.

## Done

- Initial repo baseline and BA implementation references were reviewed.
- Sprint 02 UI annotations for editable status behavior, supporting information positioning, and multiline action-note usability were updated.
- Shared FE support work is aligned with the current Sprint 02 handover instead of the old Backend CAP placeholder.

Vietnamese:

- Đã review baseline ban đầu của repo và các tài liệu BA liên quan đến implementation.
- Đã cập nhật các UI annotations của Sprint 02 cho hành vi status editable, vị trí supporting information, và usability của multiline action note.
- Đã đồng bộ lại vai trò hỗ trợ FE theo handover Sprint 02 hiện tại, không dùng placeholder Backend CAP cũ nữa.

## In Progress

- Jira IDTS-37 is verified. Fiori Notification UI shows human-readable values.
- `IDTS-9` is no longer an open FE blocker; keep it only as a regression check during the final browser rerun.

Vietnamese:

- Đã verify IDTS-37: UI Notifications hiển thị các trường dữ liệu dễ đọc (không bị hiện UUID).
- Đang hỗ trợ retest và phân tích tiếp cho Jira `IDTS-9`.
- Đang hỗ trợ delivery Fiori/UI5 chung để chốt trạng thái sẵn sàng cho demo với mentor.

## Next

- Support DatDT in the final browser rerun and watch for any FE regression in Assign Developer, Comments CTA, or Object Page action visibility.
- If a new FE gap appears during the final rerun, help evaluate whether it should be fixed now or deferred from the demo baseline.
- Continue using Fiori MCP and SAP Fiori Guidelines before touching annotations, manifest configuration, or custom UI decisions.

Vietnamese:

- Hỗ trợ DatDT verify lại trên browser candidate fix annotation-only mới nhất của dialog Assign Developer.
- Nếu sau khi chọn vẫn còn hiện UUID, cùng team đánh giá nên chấp nhận gap đó cho demo hay làm targeted FE extension.
- Tiếp tục dùng Fiori MCP và SAP Fiori Guidelines trước khi đụng vào annotations, manifest config, hoặc quyết định custom UI.

## Blockers

- None at execution level. The remaining blocker is a product decision on whether the last Assign Developer selected-text gap deserves custom FE work.

Vietnamese: Hiện không có blocker về execution. Blocker còn lại là quyết định sản phẩm: gap cuối cùng ở Assign Developer có đáng để đầu tư custom FE hay không.

## Session Log

| Date | Task/WP | What was done | Completed part | Issues/Bugs found | Fix status | Evidence/Commands | Next handoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-06 | IDTS-56 Smart assignment UI | Completed Smart Assign developer dialog/dropdown for Bug Object Page, added QA scripts/evidence, updated PM docs and knowledge mirrors, pushed branch, opened PR, and commented Jira. | Implementation and verification completed; PR is open. | Product defect found and fixed in-session: Smart Assign initially treated hidden `componentCategory_ID` as missing when Object Page had not selected it. Test-harness/tooling issues found and mostly fixed in-session: QA result reporting, browser readiness, SQLite fixture boolean binding, auth fixture `issuedAt`, local preview console allow-list, and Playwright hidden-button selector. Root-level ESLint config for `scripts/qa/*.js` remains an open tooling baseline gap. | Fixed for product/UI and IDTS-56 test harness; root-level ESLint gap remains open and documented. | PR #79 `https://github.com/DarwinDO/IDTS-SAP01/pull/79`; Jira comment `10352`; `npx eslint webapp/ext/actions/SmartAssignDeveloper.js` -> pass; `npm run qa:idts56:programmatic` -> 10 PASS / 0 FAIL; `npm run qa:idts56:browser` on `localhost:4004` -> PASS; screenshots in `docs/pm/evidence/idts-56/`. | Ready for DonHV/reviewer. Move Jira IDTS-56 to Done after PR review/merge decision. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Inspected the post-click screenshot for the Smart Assign dialog failure. | Root cause found in the Smart Assign handler. | **Product defect:** Smart Assign showed `Choose Application Component and Defect Category before assigning a developer` even when the Object Page displayed valid classification. The handler assumed `componentCategory_ID` was already selected in the Fiori Object Page context; hidden technical fields may be absent unless explicitly requested. | Under fix in-session; request required assignment properties (`componentCategory_ID`, `sapModule_ID`, `canAssign`, `assigneeDisplayName`) before opening the dialog. | Screenshot `docs/pm/evidence/idts-56/debug_after_smart_assign_click.png` shows classified bug plus the incorrect missing-classification toast. | Patch SmartAssignDeveloper.js and rerun programmatic + browser QA. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Reran browser QA with visible Smart Assign selector. | Action click reached, but the dialog did not appear within timeout. | **Under investigation:** clicking Smart Assign did not open the expected dialog. Need capture browser console/SAP error state immediately after click to distinguish UI handler runtime error, disabled/no-op action, or selector still clicking a non-action element. | Under investigation in-session; add post-click harness assertion and screenshot before the dialog wait. | `npm run qa:idts56:browser` showed Object Page/Smart Assign action PASS, then timed out waiting for `Smart Assign Developer` dialog. | Add post-click diagnostics, rerun, then fix root cause. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Reran browser QA after restarting server 4004. | Object Page loaded and Smart Assign action was discoverable, but click failed due to selector ambiguity. | **Test-harness issue:** Playwright `getByRole('button', { name: /Smart Assign/ }).first()` matched a hidden Fiori Dynamic Page header focus span before the real visible UI5 button, causing a click timeout. | Under fix in-session; use a visible UI5 button selector/text locator for the Smart Assign action. | `npm run qa:idts56:browser` showed `PASS fixture Object Page loaded with Smart Assign action`, then failed clicking hidden `sapFDynamicPageTitleFocusSpan`. | Patch selector and rerun browser QA. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Reran browser QA after patching local preview console allow-list. | Fixture cleanup verified after failure. | **Environment blocker:** the active `localhost:4004` server stopped responding between browser runs; Playwright failed with `net::ERR_CONNECTION_REFUSED`. This is a local server availability issue, not a Smart Assign product failure. | Under fix in-session; restart the CAP/Fiori server on port 4004 and rerun. Verified fixture cleanup restored SangVN availability to `AVAILABLE`. | `npm run qa:idts56:browser` exit 1 with `ERR_CONNECTION_REFUSED`; `Get-NetTCPConnection -LocalPort 4004` returned no listener; direct DB check returned SangVN `availabilityStatus_code=AVAILABLE`. | Restart server on 4004 with logs and rerun browser QA. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Reran browser QA against the user's active `localhost:4004` server. | Object Page loaded far enough for harness checks. | **Test-harness issue:** shared browser harness already allows local preview `Component-preload.js` 404 responses, but did not allow the matching UI5 console message `failed to load JavaScript resource: ... Component-preload.js`; this stopped the Smart Assign check before interacting with the dialog. | Under fix in-session; add this known local preview console pattern to `scripts/qa/lib/browser-harness.js`. | `npm run qa:idts56:browser` exit 1; screenshot `docs/pm/evidence/idts-56/error_object_page_load.png`; console error text references `idts/bugmanagementui/Component-preload.js`. | Patch browser harness noise allow-list and rerun browser QA. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Ran browser QA after fixing auth-session fixture. User clarified the active local server should be `localhost:4004`, not the temporary `4026` server. | Browser script reached the Object Page but failed on an overly strict visibility wait. | **Test-harness issue:** Fiori rendered the generated Object Page title text in a hidden span while the page was still usable, so waiting for the title text to be visible timed out. The browser QA should wait for the user-facing `Smart Assign` action instead. | Under fix in-session; change default browser QA base URL to `http://localhost:4004` and wait on visible Smart Assign action. Stop the temporary 4026 server started by the agent. | `npm run qa:idts56:browser` exit 1 after 90s: title locator resolved to hidden span for `QA-IDTS56-*`; user confirmed server should be `localhost:4004`. | Patch browser QA wait/default URL and rerun against `localhost:4004`. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Reran browser QA after fixing SQLite boolean binding in the fixture. | Auth-session fixture gap identified before browser opened. | **Test-harness issue:** the browser QA script inserted an `AuthSessions` row without required `issuedAt`, so SQLite rejected the row with a NOT NULL constraint error. | Under fix in-session; align test session insert with `srv/auth.js` by setting `issuedAt`, `expiresAt`, and `userAgent`. | `npm run qa:idts56:browser` exit 1 with `NOT NULL constraint failed: idts_cap_AuthSessions.issuedAt`; checked `db/schema.cds` and `srv/auth.js` for expected fields. | Fix auth-session fixture and rerun browser QA. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Ran first browser QA attempt for Smart Assign after starting local server. | Failure isolated to fixture setup before browser opened. | **Test-harness/data issue:** browser fixture inserted `DeveloperResponsibilities.active` as boolean `true`; direct SQLite binding accepts only number/string/bigint/buffer/null in this code path. Because the fixture updated SangVN availability to `BUSY` before the failed insert, local SQLite needed an immediate restore. | Under fix in-session; change fixture boolean to numeric/string-safe value and harden cleanup around partial fixture setup. Restore SangVN availability to `AVAILABLE`. | `npm run qa:idts56:browser` exit 1 with `SQLite3 can only bind numbers, strings, bigints, buffers, and null`. | Restore local fixture data, fix browser QA script, rerun browser evidence. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Started local CAP/Fiori server on port 4026 for browser verification and ran a readiness probe. | Server process was confirmed running; readiness probe issue diagnosed. | **Test-harness issue:** initial readiness check probed protected `/odata/v4/bug/$metadata` without a Bearer token, so `Invoke-WebRequest` treated the auth response as failure and timed out even though the server was listening and root `/` returned 200. | Fixed in-session by treating root `/`/port listen as the readiness signal before token-based browser QA. | `Start-Process npx.cmd cds watch --port 4026 --livereload false` returned PID 4556; first metadata probe timed out; `Get-NetTCPConnection -LocalPort 4026` showed Listen; `Invoke-WebRequest http://localhost:4026/` returned 200. | Run `qa:idts56:browser` against the active local server. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Tried to lint the new root-level QA scripts with `npx eslint` from the repository root. | Tooling limitation identified; script syntax was still checked with Node. | **Tooling issue:** root-level ESLint cannot run because the repository root has no `eslint.config.(js|mjs|cjs)` for ESLint 10. App-level ESLint works under `app/bug-management-ui`, but root `scripts/qa/*.js` do not currently have a configured lint lane. | Open as tooling baseline; not fixed in IDTS-56 to avoid broad lint configuration changes. Use `node --check` and actual QA execution for the new root QA scripts. | `npx eslint scripts/qa/test-idts56-smart-assign.js scripts/qa/test-idts56-smart-assign-browser.js` exit 1 with missing root ESLint config; `node --check scripts/qa/test-idts56-smart-assign-browser.js` exit 0. | Continue with browser execution and capture evidence; root ESLint config can be handled separately if team wants script linting. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Added first version of `qa:idts56:programmatic` and ran it against the new Smart Assign contract/backend validation. | Failure diagnosed before continuing implementation. | **Test-harness issue:** the new QA script printed FAIL for checks that had already passed because `rec(label)` was called without the required boolean `true` argument. This made the verification output unreliable even though the contract assertions reached those points. | Under fix in-session; update the script to call `rec(label, true)` for successful checkpoints and rerun. | `npm run qa:idts56:programmatic` exit 1; output showed 0 PASS / 10 FAIL with repeated labels, indicating harness reporting failure rather than a single product assertion failure. | Fix QA script reporting, rerun IDTS-56 verification, then continue UI/browser evidence. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Started task setup after reading Jira IDTS-56 and project handover. Attempted to create the required task branch. | Jira IDTS-56 moved to In Progress; branch setup later completed. | **Tooling issue:** sandbox initially blocked `git switch -c feature/idts-56-smart-assign-developer-sangvn` because Git could not create the branch lock file under `.git/refs/heads/feature/` (`Permission denied`). | Fixed in-session after permissions changed; work continued on `feature/idts-56-smart-assign-developer-sangvn`. | Atlassian Rovo read IDTS-56 and transitioned it to In Progress; initial `git switch -c ...` exit 1; later branch confirmed by `git status --short --branch`. | Continue implementation on the task branch. |
| 2026-07-02 | IDTS-32 Manual browser UAT | Checked history timeline rendering with many records. | History limit observation recorded. | **UX/product follow-up:** History timeline is not limited and shows all records, making the Object Page extremely long when there is a lot of history. | Open for DonHV review. | Jira `IDTS-32` comment added. | DonHV to review and decide if pagination or a display limit is needed. |
| 2026-07-01 | IDTS-37 Notification UI verification | Ran Playwright subagent to verify Fiori Notification UI on `localhost:4004`. Checked BUG-0005 Notifications section. | UI verification completed. | No UI issues found. All columns (Event Type, Channel, Delivery Status, Sent Time, Recipient, Message) show human-readable text instead of raw UUIDs. | Verified | Browser screenshots in `docs/pm/evidence/idts-37/`. | Push branch `feature/idts-37-notification-ui-sangvn` and request review. |
| 2026-07-01 | IDTS-32 Manual browser UAT retest | Re-tested IDTS-32 findings on latest `dev` after `npm install` fixed the missing nodemailer dependency. Ran programmatic QA validations for IDTS-41 (code list validation) and IDTS-43 (Fiori UX). | Retest completed. | All previous IDTS-32 findings are now fixed in `dev`. Priority/Severity validation gap fixed by IDTS-41 (18/18 pass). History UI duplicate fixed by IDTS-43 (raw table removed). Developer-create UX fixed by IDTS-43 (Create action hidden). Reopen wording improved by IDTS-43. | Fixed in latest `dev`. | `npm install`; `node scripts/qa/test-idts41-code-list-validation.js` -> 18 PASS; `node scripts/qa/test-idts43-fiori-ux.js` -> 11 PASS. | UAT IDTS-32 is complete. Ready to proceed to IDTS-37 Notification UI. |
| 2026-07-01 | IDTS-32 Manual browser UAT | Added interim Jira summary after the current manual browser UAT pass. Confirmed that, apart from the logged findings/observations, the main tested workflow paths look stable enough for DonHV review. | Interim manual UAT summary completed and handed off. | No new product bug in this summary step. Existing open items remain: Priority/Severity validation gap, History vs History Timeline UX decision, Developer-create entry UX follow-up, and optional wording around Reopen from Resolved. | Open items are logged for DonHV/team triage; no additional fix made in this docs/evidence branch. | Jira `IDTS-32` comment `10265`; repo evidence folder `docs/pm/evidence/idts-32/`. | DonHV to review Jira comments and repo evidence, then decide which findings need separate fix tickets versus acceptance for current Sprint 3 manual UAT baseline. |
| 2026-07-01 | IDTS-32 Manual browser UAT | Checked bug create input validation for required Priority and Severity fields. Confirmed from catalog data that valid Priority codes are `LOW`, `MEDIUM`, `HIGH`, `CRITICAL`, and valid Severity codes are `MINOR`, `MAJOR`, `CRITICAL`, `BLOCKER`. Added Jira comment and repo screenshot evidence. | Priority/Severity validation finding recorded. | **Product defect / validation gap:** entering invalid free-text value `1` into Priority and Severity still allowed bug creation. This can create invalid master-data codes and affect filtering/reporting/semantic coloring. | Open; likely needs FE value-help/input validation and backend code-list existence validation before save/activation. | Browser screenshot: `docs/pm/evidence/idts-32/priority-severity-invalid-1-accepted.png`; Jira `IDTS-32` comment `10262`; local code/catalog check: `db/data/idts.cap-PriorityValues.csv`, `db/data/idts.cap-SeverityValues.csv`, `srv/bug-service/bug-write.js`. | DonHV/FE/backend team to decide fix owner and whether to track as separate defect under Sprint 3 QA. |
| 2026-07-01 | IDTS-32 Manual browser UAT | Checked Object Page history presentation on `BUG-0005` and raised the question whether `History` and `History Timeline` should both remain visible. Added Jira comment and repo screenshot evidence for DonHV review. | History presentation observation recorded for PM/FE decision. | **UX/product follow-up:** `History` raw table and `History Timeline` event-first view both appear at the same navigation level, which may feel duplicated/confusing for normal Tester/Developer/PM users. | Open for DonHV/team decision; recommended direction is to keep `History Timeline` as primary user-facing history and keep/hide raw `History` based on audit/debug/admin need. | Browser screenshot: `docs/pm/evidence/idts-32/history-and-history-timeline-duplicate-ux.png`; Jira `IDTS-32` comment `10260`. | DonHV/FE team to decide whether raw `History` stays visible for Sprint 3 or is moved/hidden as advanced audit view. |
| 2026-07-01 | IDTS-32 Manual browser UAT | Started SangVN's manual UAT on role/action visibility using Developer account `sangvn`. Confirmed that attempting to create a bug as Developer is blocked on submit with `Only Tester or PM users can create bug reports.` Added Jira evidence comments and stored screenshot evidence in repo. | First role/action visibility check completed: backend/business-rule enforcement is PASS because only Tester and PM may create bug reports. | **UX follow-up observation:** Developer can still enter and fill the new bug draft/create form before being blocked at `Create`; this is not a core permission defect, but may be FE polish if the team wants the Create entry hidden/disabled earlier for Developer users. | Open as optional UX follow-up; no product rule fix needed for permission enforcement. | Browser screenshot: `docs/pm/evidence/idts-32/sangvn-dev-create-blocked-assignee-empty.png`; Jira `IDTS-32` comments `10256`, `10257`. | Continue IDTS-32 manual UAT with Tester/PM create flow and remaining lifecycle/history checks. |
| 2026-07-01 | Local project open/debug | Investigated why the local CAP/Fiori project does not open for SangVN. Checked environment, dependency status, SQLite service views, CAP compile, and `npm run watch-bug-management-ui`. | Root cause identified. | **Environment/tooling issue:** local `node_modules` is stale after recent SMTP/outbox merge; `nodemailer` and `smtp-server` are declared in `package.json` but missing locally. CAP server start fails in `srv/email/sender.js` with `Cannot find module 'nodemailer'`. **Tooling issue:** AI DevKit memory search was blocked by sandbox/network access to npm registry. | Open pending dependency install. | `npx cds --version` reported missing `nodemailer@^9.0.1` and `smtp-server@^3.15.0`; `npm run dev:sqlite:refresh-views` exit 0; `npx cds compile srv app/bug-management-ui --to edmx -s all` exit 0 with known attachment warning; `npm run watch-bug-management-ui` reproduced server start failure; `npx ai-devkit@latest memory search ...` failed with npm registry `EACCES`. | Run `npm install`, then rerun `npm run watch-bug-management-ui` and browser open check. |
| 2026-06-28 | IDTS-19 / PR #18 DonHV merge review | DonHV re-reviewed PR #18, found that the branch had moved the verified PM monitoring List Report tabs from page-level `views.paths` into table-level `quickVariantSelection`, then restored the `views.paths` configuration while keeping the new `HistoryTimeline` Object Page custom section. Updated the `manifest.json` knowledge mirror to explain why this app must keep the verified OData V4 tab configuration. | PR #18 fix completed on SangVN branch. | **Fiori configuration/process issue:** branch was Git-mergeable but would have changed the previously verified PM monitoring tab mechanism. This was not a backend defect. | Fixed in this session. | Fiori MCP `search_docs` confirmed OData V4 multiple List Report views use `views.paths`; UI5 manifest validation passed; `npx cds compile srv app/bug-management-ui --to edmx` pass with known attachment warning; `npx ui5 build --config ui5.yaml` pass; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `npm run qa:history-events:programmatic` -> 13 PASS / 0 FAIL; `git diff --check origin/dev HEAD` pass. | Push branch update, merge PR #18 into `dev`, then update Jira IDTS-19. |
| 2026-06-27 | IDTS-19 / Jira comment fix (Codex) | Continued from Codex state. Ran CDS compile, UI5 build, and checked git diff for spacing. Server started properly on port 4004. Playwright sandbox prevented automated UI capture, but server is fully up with the Timeline and i18n configs intact. | Compiled and built successfully. | Test-harness issue: `browser_subagent` encountered sandbox restrictions reading allowed paths. | Left for SangVN's manual UAT (IDTS-32). | `npx cds compile srv app/bug-management-ui --to edmx` exit 0; `npm run watch-bug-management-ui` started server at port 4004; `git diff --check` clean. | SangVN to do manual browser check and push branch. |
| 2026-06-30 | IDTS-32 roadmap lookup | Read local PM handover and attempted to retrieve Jira `IDTS-32` through Atlassian Rovo before giving the task roadmap. | Local roadmap source confirmed from `task-board.md`, `sangvn.md`, `wp5-comments-history.md`, and `wp6-pm-monitoring.md`. | Tooling issue: Jira MCP lookup failed for cloud ID `darwindo.atlassian.net` with tenant-info 404, and Rovo natural search returned no `IDTS-32` results in this session. | Open for connector/session access; local PM docs remain usable. | `tool_search` loaded Atlassian Rovo; `_searchjiraissuesusingjql` returned tenant-info 404; `_search` returned empty results for `Find Jira issue IDTS-32`. | SangVN can use the local PM roadmap now; Jira details should be rechecked when connector access recovers. |
| 2026-06-30 | Pull latest `dev` request | Pulled latest `origin/dev` into `feature/idts-19-history-timeline-sangvn` before IDTS-32 work. | Branch is now fast-forwarded to `92aaa12`. Local SangVN status edits were restored after the pull. | Process/tooling issue: first merge attempt was blocked because local `docs/pm/status/sangvn.md` edits would be overwritten. | Fixed in-session by stashing only the local status-file edit, pulling `dev`, then restoring the status edit without conflict. | First `git pull origin dev` fetched but aborted with "local changes would be overwritten"; second `git pull origin dev` fast-forwarded to `92aaa12`; `git stash pop` auto-merged cleanly; `git diff --check -- docs/pm/status/sangvn.md` pass. | Continue IDTS-32 work on latest `dev`; keep untracked `diff.patch` untouched unless the owner clarifies it. |
| 2026-06-27 | IDTS-19 / Manual Browser UAT (IDTS-32) | SangVN manually verified the Fiori UI locally and provided screenshots. Verified PM monitoring tabs still exist and History Timeline renders, expands, and shows translated i18n texts properly. | Browser verification completed. | None | Fixed | Provided UI screenshot: `History Timeline` section renders successfully with i18n keys and expandable details on Object Page. | Push branch and request DonHV to re-review PR #18. |
| 2026-06-27 | IDTS-19 / Browser verification Playwright setup | Started local CAP/Fiori server and attempted Playwright browser verification for PM tabs and Object Page History Timeline. | Local server started on port 4004; OData auth/data probe worked with mock user `DonHV`. | Environment blocker: Playwright package is installed but its managed Chromium binary is missing, so `chromium.launch()` failed and suggested `npx playwright install`. | Under investigation; try local Edge/Chrome executable before considering browser download. | `Start-Process npx.cmd cds watch --port 4004 ...` returned PID 12628; OData query for `HistoryEvents` returned grouped history rows; Playwright failed: missing `chromium_headless_shell-1228`. | Retry Playwright with installed system browser executable. |
| 2026-06-27 | IDTS-19 / Browser verification setup | Refreshed local SQLite service views, then tried to start local CAP/Fiori server for browser verification. | `dev:sqlite:refresh-views` completed. | Environment blocker: sandbox blocked `Start-Process` for launching the local `npx cds watch` server (`Access is denied`). | Under investigation; retry requires escalated local process permission. | `npm run dev:sqlite:refresh-views` exit 0; `Start-Process ... npx cds watch --port 4004 ...` failed with Access denied. | Retry server startup with approved escalation, then run Playwright browser checks. |
| 2026-06-27 | IDTS-19 / PR #18 review fix verification | Ran available FE lint after restoring PM monitoring tabs and i18n/knowledge mirror updates. | CAP compile and UI5 build passed; lint result captured for review transparency. | Test-harness/tooling issue: `npx eslint webapp` fails on pre-existing CRLF linebreak-style errors in `webapp/Component.js` and warnings in existing OPA test files; the current IDTS-19 change did not modify those JavaScript files. | Open as tooling/style baseline issue; not fixed in this PR to avoid broad unrelated line-ending churn. | `npx eslint webapp` exit 1: 244 problems, 11 errors in `webapp/Component.js` for CRLF linebreak-style and 233 warnings in existing test files. | Continue browser verification for the timeline and PM monitoring tabs; decide separately whether to normalize FE JS line endings repo-wide. |
| 2026-06-27 | IDTS-19 / Jira comment handoff | Tried to read the current task review comment for branch `feature/idts-19-history-timeline-sangvn` before making code changes. Jira/Rovo connector and Jira/Atlassian CLI were not available in this Codex session; GitHub CLI exists but network access for `gh pr view --comments` was blocked and the escalation request was not approved. | Context and blocker captured before code changes. | Tooling issue: direct Jira comment retrieval is unavailable in the current tool/session setup, and fallback PR comment retrieval cannot run without network approval. | Open; needs user-provided Jira comment text or connector/network access. | `tool_search` exposed CAP/Fiori/Memory MCP only; `list_available_plugins_to_install` listed no Jira plugin; `Get-Command jira/acli/rovo` returned no tools; `gh pr view --comments --json ...` failed with socket permission error; escalated retry was rejected. | User to paste the Jira comment or enable Jira/GitHub access, then SangVN can apply the requested code fix. |
| 2026-06-27 | IDTS-19 (Sprint 03) | Redesigned timeline from `sap.suite.ui.commons.Timeline` back to `sap.m.List` + `CustomListItem` with timeline-style layout. Root cause of logs not rendering: `sap.suite.ui.commons.TimelineItem.embeddedControl` drops nested OData V4 composition binding in FPM custom sections. New design uses standard `sap.m` controls where `$expand: 'logs'` works natively. Removed `sap.suite.ui.commons` from manifest. Fixed merge conflict markers in `sangvn.md`. | Fragment rewritten, manifest cleaned, status file fixed | 1) Product defect: nested Table inside `sap.suite.ui.commons.TimelineItem.embeddedControl` does not bind OData V4 composition data in FPM custom sections. 2) Process issue: `sangvn.md` had residual merge conflict markers from previous rebase. | 1) Fixed by switching to `sap.m.List` + `CustomListItem`. 2) Fixed by rewriting file. | `npx cds compile srv app/bug-management-ui --to edmx` (exit 0, only known attachment warning) | Browser verification needed. Push and update PR. |
| 2026-06-27 | IDTS-32 / IDTS-33 manual UAT split | DonHV split the original broad IDTS-32 manual browser UAT scope. IDTS-32 now stays with SangVN and covers workflow/history-heavy testing: Developer lifecycle, Need More Information/resubmit, reject follow-up, retest/close/reopen, grouped history timeline, and role/action visibility. | IDTS-32 Jira description updated and linked to IDTS-33. | No product bug found in this planning step. Process change: FE-shell-heavy UAT was moved out to DatDT under IDTS-33 to reduce overload and make ownership clearer. | Not applicable. | Jira IDTS-32 edited; Jira IDTS-33 created and linked with `Relates`; comments added on both Jira issues. | SangVN to execute IDTS-32 workflow/history UAT and record findings/evidence in this file. |
| 2026-06-24 | IDTS-32 Manual browser UAT planning | DonHV created Jira task IDTS-32 for SangVN to manually test Sprint 3 browser flows: PM monitoring slices, bug create, assignment/reassignment, developer lifecycle, Need More Information/resubmit, reject follow-up, retest/close/reopen, grouped history timeline, comments/attachments/notifications, and role/action visibility. | Jira task created and assigned to SangVN. | No product bug found in this planning step. Process note: manual UAT must classify any later finding as product defect, environment blocker, tooling issue, test-harness issue, data issue, documentation issue, or process issue. | Not applicable. | Jira IDTS-32 created through Atlassian Rovo; PM board updated. | SangVN to execute manual browser UAT on latest `dev` and update this status file with evidence/findings. |
| 2026-06-23 | IDTS-20 PR #14 DonHV support fix | DonHV reviewed the pushed PR #14 branch against SangVN's Jira comment, merged the current `origin/dev` documentation baseline into the branch, restored the missing `package-lock.json` package root name, replaced the remaining value-help labels `Next Processor User` and `Next Processor Role` with `Current Action Owner` and `Action Owner Role`, and updated the matching knowledge mirror for `value-helps.cds`. | PR #14 cleanup support completed on SangVN branch. | Process/documentation mismatch: Jira comment said `package-lock.json` was reverted, but the PR still removed `packages[""].name`. UI wording issue: `value-helps.cds` still exposed `Next Processor User/Role` labels in metadata-driven value-help surfaces. | Fixed in this support commit. | `git diff --check` PASS; targeted `rg "Next Processor User|Next Processor Role" app/bug-management-ui/annotations` returned no matches; `npx cds compile srv app/bug-management-ui --to edmx` exit 0 with only the known `@cap-js/attachments` warning; `npx ui5 build --config ui5.yaml` in `app/bug-management-ui` succeeded; `npx ai-devkit@latest lint --json` PASS; secret scan returned no matches. | DonHV to push this branch, merge PR #14, then continue DatDT PR/branch review. |
| 2026-06-23 | IDTS-20 (Sprint 03) | Fixed PR #14 review findings from DonHV. 1) Removed invalid `_ID` and `_code` annotations on foreign keys in `labels.cds` to fix compiler warnings. 2) Fixed DataPoint `Title` in `object-page.cds` to 'Assignee (Technical Owner)'. 3) Replaced `nextProcessorRoleName` and `nextProcessorUserDisplayName` with `currentActionOwnerDisplayName` in List Report and Assignment field groups to remove ambiguity. 4) Reverted unintended `package-lock.json` changes. | Cleaned up all PR #14 findings and compiler warnings | `npx cds compile srv app/bug-management-ui --to edmx` was throwing `has not been found` warnings for foreign keys. `package-lock.json` was mistakenly modified. | Fixed | `git checkout package-lock.json`; `npx cds compile srv app/bug-management-ui --to edmx` (0 warnings on UI); `git diff --check` (pass) | SangVN to commit and push/re-create PR for DonHV |
| 2026-06-22 | IDTS-20 (Sprint 03) | Refined UI labels and added helper texts (QuickInfo and DataPoints) for ownership semantics per DEC-022. Updated 4 annotation CDS files: `labels.cds` (Assignee → Assignee (Technical Owner) + QuickInfo, Next Processor User → Current Action Owner + QuickInfo, Next Processor Role → Action Owner Role + QuickInfo, Pending Assignment → Awaiting Assignment, Rejected Follow-up → Rejected — Needs Follow-up), `ownership-assignment.cds` (Assignment and Rejected Follow-up FieldGroup labels), `list-report.cds` (Removed hardcoded column headers to inherit from labels.cds consistently), `object-page.cds` (added Current Action Owner to General Info section; **added Assignee and Current Action Owner to UI.HeaderFacets as DataPoints so their helper descriptions are natively visible on the Object Page in display mode**). | All 4 annotation files updated, CDS compile passed with no errors, browser verification passed | Fiori Elements display mode does not natively show QuickInfo tooltips for read-only text fields. The initial ugly inline-text solution was rejected. | UI Limitation fixed: Used UI.HeaderFacets with UI.DataPoint to show the helper texts as professional header descriptions instead of inline text. | `git checkout -b feature/idts-20-ownership-labels-sangvn`; `npm install`; `npx cds compile srv --to edmx` (pass); Manual browser check (passed) | Done. Proceed to next task (IDTS-19). |
| 2026-06-17 | IDTS-9 QA UI Retest | Re-tested the Assign Developer action dialog on the latest local FE build to verify whether the selected input now resolves the developer name instead of the UUID | Confirmed that the dialog is visible, the field label is now `Assignee`, and the value-help rows are business-friendly. The remaining issue is narrower: after selecting a developer, the input still shows the raw developer-profile UUID instead of the readable developer name. | Likely a standard Fiori Elements action-parameter selected-text limitation or a remaining FE metadata handling gap for this dialog. Browser evidence confirms the symptom; root-cause classification is still under team review. | Open (investigated) | Local run via `npm run watch-bug-management-ui`; browser retest on `http://localhost:4004/...`; screenshots `dialog_before_selection_1781693070331.png`, `dialog_after_selection_1781693103672.png` | Wait for team decision on recommendation: accept for demo or build a targeted FE extension |
| 2026-06-13 | IDTS-8, IDTS-10, IDTS-11 (Sprint 02) | Updated Bug Detail UI annotations for status dropdown behavior, supporting information placement, and multiline action notes | UI annotations completed | None | Fixed | `cds compile srv --to edmx` | Manual QA and shared FE follow-up |
| 2026-06-03 | Status setup | Member status file created from previous status baseline | Ownership assigned to SangVN | None | Fixed | `rg`, `git diff --check` | Continue on assigned Sprint work |

Vietnamese:

| Ngày | Task/WP | Đã làm gì | Phần đã xong | Khó khăn/Bug phát hiện | Trạng thái fix | Bằng chứng/Lệnh đã chạy | Handoff tiếp theo |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-02 | IDTS-32 Manual browser UAT | Kiểm tra hiển thị history timeline khi có nhiều record. | Đã ghi nhận observation về giới hạn history. | **UX/product follow-up:** History timeline hiện không bị limit mà hiển thị tất cả, làm trang Object Page rất dài khi có nhiều history. | Open chờ DonHV review. | Đã comment trên Jira `IDTS-32`. | DonHV review và quyết định xem có cần phân trang hay limit không. |
| 2026-07-01 | IDTS-37 Notification UI verification | Dùng browser subagent verify giao diện Notifications trên `localhost:4004`. Đã kiểm tra BUG-0005. | Đã hoàn thành verify UI. | Không phát hiện UI lỗi. Tất cả các cột (Event Type, Channel, Delivery Status, Sent Time, Recipient, Message) đều hiển thị text dễ hiểu, không có UUID raw. | Verified | Screenshot lưu tại `docs/pm/evidence/idts-37/`. | Push nhánh `feature/idts-37-notification-ui-sangvn` và nhờ DonHV review. |
| 2026-07-01 | IDTS-32 Manual browser UAT retest | Retest lại các lỗi của IDTS-32 trên nhánh `dev` mới nhất sau khi chạy `npm install` để fix lỗi thiếu nodemailer. Chạy file QA validation tự động cho IDTS-41 và IDTS-43. | Hoàn thành retest. | Các lỗi phát hiện trước đó đã được fix trên `dev`. Lỗi nhập Priority/Severity sai đã được IDTS-41 fix (18/18 pass). Lỗi trùng lặp giao diện History đã được IDTS-43 fix (ẩn bảng thô). Lỗi Developer thấy nút Create Bug đã được IDTS-43 fix (ẩn nút). Lỗi wording của nút Reopen đã được IDTS-43 fix. | Đã fix trên `dev` mới nhất. | `npm install`; `node scripts/qa/test-idts41-code-list-validation.js` -> 18 PASS; `node scripts/qa/test-idts43-fiori-ux.js` -> 11 PASS. | Đã xong UAT cho IDTS-32. Chuẩn bị làm tiếp IDTS-37 Notification UI. |
| 2026-06-28 | IDTS-19 / PR #18 DonHV merge review | DonHV review lại PR #18 và phát hiện branch đã chuyển cấu hình tab PM monitoring của List Report từ `views.paths` ở page settings sang `quickVariantSelection` trong table settings. DonHV khôi phục lại `views.paths` đã được verify trước đó, đồng thời vẫn giữ custom section `HistoryTimeline` mới trên Object Page. Knowledge mirror của `manifest.json` cũng được cập nhật để giải thích rõ vì sao app này cần giữ cấu hình tab OData V4 đã verify. | Đã fix PR #18 trực tiếp trên branch SangVN. | **Fiori configuration/process issue:** PR merge được ở mức Git nhưng có thể làm thay đổi cơ chế tab PM monitoring đã được verify. Đây không phải lỗi backend. | Đã fix trong phiên này. | Fiori MCP `search_docs` xác nhận multiple List Report views của OData V4 dùng `views.paths`; UI5 manifest validation pass; `npx cds compile srv app/bug-management-ui --to edmx` pass với warning attachment cũ; `npx ui5 build --config ui5.yaml` pass; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `npm run qa:history-events:programmatic` -> 13 PASS / 0 FAIL; `git diff --check origin/dev HEAD` pass. | Push branch update, merge PR #18 vào `dev`, rồi cập nhật Jira IDTS-19. |
| 2026-06-27 | IDTS-19 (Sprint 03) | Redesign timeline từ `sap.suite.ui.commons.Timeline` về `sap.m.List` + `CustomListItem` kiểu timeline. Nguyên nhân logs không render: `TimelineItem.embeddedControl` drop nested OData V4 composition binding trong FPM custom sections. Design mới dùng standard `sap.m` controls. Xóa `sap.suite.ui.commons` khỏi manifest. Fix merge conflict markers trong `sangvn.md`. | Fragment viết lại, manifest dọn, status file fix | 1) Product defect: nested Table trong `TimelineItem.embeddedControl` không bind OData V4 composition. 2) Process issue: `sangvn.md` còn merge conflict markers từ rebase trước. | 1) Fix bằng cách chuyển sang `sap.m.List`. 2) Fix bằng cách viết lại file. | `npx cds compile srv app/bug-management-ui --to edmx` (exit 0) | Cần browser verify. Push và update PR. |
| 2026-06-23 | IDTS-20 PR #14 DonHV support fix | DonHV kiểm tra branch PR #14 so với comment Jira của SangVN, merge baseline tài liệu mới nhất từ `origin/dev` vào branch, khôi phục `packages[""].name` bị mất trong `package-lock.json`, đổi label value-help còn sót `Next Processor User` và `Next Processor Role` thành `Current Action Owner` và `Action Owner Role`, đồng thời cập nhật knowledge mirror cho `value-helps.cds`. | Đã cleanup PR #14 trực tiếp trên branch SangVN. | Lệch quy trình/tài liệu: comment Jira nói đã revert `package-lock.json`, nhưng PR vẫn xóa `packages[""].name`. Lỗi wording UI: `value-helps.cds` vẫn còn label `Next Processor User/Role` trong các bề mặt UI sinh từ metadata. | Đã fix trong commit support này. | `git diff --check` PASS; scan `rg "Next Processor User|Next Processor Role" app/bug-management-ui/annotations` không còn match; `npx cds compile srv app/bug-management-ui --to edmx` exit 0 và chỉ còn warning cũ của `@cap-js/attachments`; `npx ui5 build --config ui5.yaml` trong `app/bug-management-ui` succeeded; `npx ai-devkit@latest lint --json` PASS; secret scan không có match. | DonHV push branch này, merge PR #14, rồi tiếp tục kiểm tra PR/branch của DatDT. |
| 2026-06-23 | IDTS-20 (Sprint 03) | Sửa các lỗi review PR #14 từ DonHV: 1) Xóa annotation trên các foreign key `_ID`/`_code` trong `labels.cds` để hết cảnh báo compile. 2) Chỉnh DataPoint `Title` thành 'Assignee (Technical Owner)'. 3) Dùng `currentActionOwnerDisplayName` thay cho 2 trường rời rạc của Next Processor trong List Report và Assignment để hết gây nhầm lẫn. 4) Revert lại `package-lock.json`. | Đã dọn dẹp xong mọi findings của PR #14. | Lệnh compile báo lỗi "has not been found" vì annotate trực tiếp lên foreign keys sinh tự động của CAP. | Đã fix | `git checkout package-lock.json`; `npx cds compile srv app/bug-management-ui --to edmx` (hết warning UI); `git diff --check` (pass) | SangVN tự commit, push và tạo lại PR sạch cho DonHV review |
| 2026-06-22 | IDTS-20 (Sprint 03) | Chỉnh label UI và thêm helper text (QuickInfo tooltip + DataPoints) cho ownership semantics theo DEC-022. Cập nhật 4 file annotation CDS: `labels.cds` (thêm tooltip giải thích rõ Assignee và Action Owner), `ownership-assignment.cds`, `list-report.cds` (xóa hardcode label để nhất quán), `object-page.cds` (thêm Current Action Owner vào General Info; **thêm Assignee và Current Action Owner vào UI.HeaderFacets dưới dạng DataPoint để hiện Description chuyên nghiệp trên Header ở chế độ Display**). | Cả 4 file annotation đã cập nhật, CDS compile pass không lỗi, browser verify pass | Fiori Elements display mode không hiện QuickInfo cho read-only text. Giải pháp thêm text vào inline form nhìn rất xấu và bị từ chối. | Đã fix: Dùng UI.HeaderFacets với UI.DataPoint để hiển thị helper text cực chuẩn ở Header của Object Page. | `git checkout -b feature/idts-20-ownership-labels-sangvn`; `npm install`; `npx cds compile srv --to edmx` (pass); Kiểm tra tay trên Browser (pass) | Xong. Chuyển sang task tiếp theo (IDTS-19). |
| 2026-06-17 | IDTS-9 QA UI Retest | Retest dialog Assign Developer trên bản FE local mới nhất để kiểm tra ô chọn sau khi chọn developer có hiện tên hay vẫn hiện UUID | Đã xác nhận label của field là `Assignee`, các dòng trong value help đã thân thiện hơn về mặt nghiệp vụ. Vấn đề còn lại hẹp hơn: sau khi chọn developer, ô input vẫn hiện UUID của developer profile thay vì tên đọc được. | Nhiều khả năng đây là giới hạn của standard Fiori Elements với selected text của action parameter, hoặc vẫn còn một khoảng FE metadata handling chưa được resolve hết. Hiện mới nên kết luận ở mức `likely`, chưa nên khẳng định tuyệt đối. | Open (đã điều tra và giữ mở) | Chạy local bằng `npm run watch-bug-management-ui`; browser retest trên `http://localhost:4004/...`; screenshot `dialog_before_selection_1781693070331.png`, `dialog_after_selection_1781693103672.png` | Chờ team chốt hướng xử lý: chấp nhận cho demo hoặc làm targeted FE extension |
| 2026-06-13 | IDTS-8, IDTS-10, IDTS-11 (Sprint 02) | Cập nhật UI annotations cho Bug Detail liên quan status dropdown, vị trí Supporting Information, và multiline action note | Đã xong phần UI annotations | Không có | Đã xử lý | `cds compile srv --to edmx` | Manual QA và hỗ trợ FE tiếp theo |
| 2026-06-03 | Status setup | Tạo file status thành viên từ baseline status cũ | Đã giao ownership cho SangVN | Không có | Đã xử lý | `rg`, `git diff --check` | Tiếp tục theo Sprint task được assign |

## Update Rule

- SangVN updates this file after each work session.
- Record what was done, what part is complete, blockers, bugs/errors found, whether they were fixed, and verification evidence.
- Do not edit other members' status files unless coordinating with DonHV.

Vietnamese:

- SangVN cập nhật file này sau mỗi phiên làm việc.
- Ghi rõ đã làm gì, xong phần nào, blocker, bug/error phát hiện, đã fix hay chưa và bằng chứng verify.
- Không chỉnh file status của thành viên khác trừ khi phối hợp với DonHV.
