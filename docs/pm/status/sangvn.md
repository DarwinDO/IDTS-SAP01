# SangVN Status - Fiori/UI5 Support

Last updated: 2026-07-08

Vietnamese: Trạng thái của SangVN - hỗ trợ Fiori/UI5 cho Sprint 02.

## Member Identity

| Field | Value |
| --- | --- |
| Member | SangVN |
| Primary lane | Fiori/UI5 Support |
| Shared delivery responsibility | May receive Backend CAP, Fiori/UI5, or QA/Verification tasks as assigned, but Sprint 02 priority is Fiori/UI5 support and shared delivery assistance |
| Leader support | DonHV can support or unblock this lane when needed |

Vietnamese:

| Trường | Giá trị |
| --- | --- |
| Thành viên | SangVN |
| Mảng chính | Hỗ trợ Fiori/UI5 |
| Trách nhiệm delivery chung | Có thể nhận task Backend CAP, Fiori/UI5 hoặc QA/Verification khi được phân công, nhưng ưu tiên của Sprint 02 là hỗ trợ Fiori/UI5 và hỗ trợ delivery chung khi cần |
| Leader hỗ trợ | DonHV có thể hỗ trợ hoặc gỡ blocker cho mảng này khi cần |

## Current Focus

Sprint 04 follow-up: IDTS-47 is Done on Jira after the History Timeline limit PR was merged/closed. SangVN's next active Jira work is IDTS-59, a focused manual UI/UX scan of completed Sprint 4 Fiori screens.

Vietnamese: Trọng tâm hiện tại là phần Fiori/UI5 của Sprint 03: IDTS-20 chỉnh label ownership đang thực hiện. Task tiếp theo là IDTS-19 grouped history timeline với UI5 extension.

## Done

- Initial repo baseline and BA implementation references were reviewed.
- Sprint 02 UI annotations for editable status behavior, supporting information positioning, and multiline action-note usability were updated.
- Shared FE support work is aligned with the current Sprint 02 handover instead of the old Backend CAP placeholder.

Vietnamese:

- Đã review baseline ban đầu của repo và các tài liệu BA liên quan đến implementation.
- Đã cập nhật các UI annotations của Sprint 02 cho hành vi status editable, vị trí supporting information, và usability của multiline action note.
- Đã đồng bộ lại vai trò hỗ trợ FE theo handover Sprint 02 hiện tại, không dùng placeholder Backend CAP cũ nữa.

## In Progress

- Shared Fiori/UI5 delivery support for final mentor-demo readiness remains active.
- Jira IDTS-32 is ready for SangVN to run manual browser UAT across workflow lifecycle, grouped history, reject/retest follow-up, and role/action visibility.
- `IDTS-9` is no longer an open FE blocker; keep it only as a regression check during the final browser rerun.

Vietnamese:

- Đang hỗ trợ retest và phân tích tiếp cho Jira `IDTS-9`.
- Đang hỗ trợ delivery Fiori/UI5 chung để chốt trạng thái sẵn sàng cho demo với mentor.

## Next

- Start Jira `IDTS-59` by moving it to In Progress, then scan the shared QA Sprint 4 UI surfaces: login, profile/sign-out, dashboard, comments, attachments, and existing Object Page flow.
- For each target surface, capture at least one positive check and one attempted-break/edge check; classify findings as product defect, UX gap, data issue, environment blocker, tooling issue, or test-harness issue.
- If an actionable product/UX issue is found, create or link a focused Jira issue and avoid including tokens, passwords, private Render/AWS/Brevo config, or private recipient data in screenshots/comments.
- Continue using Fiori MCP and SAP Fiori Guidelines before touching annotations, manifest configuration, or custom UI decisions.

Vietnamese:

- Hỗ trợ DatDT verify lại trên browser candidate fix annotation-only mới nhất của dialog Assign Developer.
- Nếu sau khi chọn vẫn còn hiện UUID, cùng team đánh giá nên chấp nhận gap đó cho demo hay làm targeted FE extension.
- Tiếp tục dùng Fiori MCP và SAP Fiori Guidelines trước khi đụng vào annotations, manifest config, hoặc quyết định custom UI.

## Blockers

- None at execution level. The remaining blocker is a product decision on whether the last Assign Developer selected-text gap deserves custom FE work.

Vietnamese: Hiện không có blocker về execution. Blocker còn lại là quyết định sản phẩm: gap cuối cùng ở Assign Developer có đáng để đầu tư custom FE hay không.

## Session Log

| Date | Task/WP | What was done | Completed part | Issues/Bugs found | Fix status | Evidence/Commands | Next handoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-08 | IDTS-59 Sprint 4 UI/UX scan | Moved Jira `IDTS-59` to In Progress, created branch `docs/idts-59-ui-ux-scan-sangvn`, refreshed SQLite service views, started local CAP/Fiori server, ran the local Sprint 4 UI/UX scan, and addressed DonHV Jira comment `10415` by adding reviewable repo evidence. | Local `dev` scan completed for login, profile/sign-out, dashboard, Object Page, comments, attachments, Smart Assign, mobile dashboard, long comment input, invalid bug route, reload persistence, and logout guard. No product defect was reproduced locally. Shared QA public route smoke returned HTTP 200 for login and dashboard routes. | **Environment/tooling issue:** first browser run failed because Playwright Chromium runtime was missing. **Test-harness issue:** first comments/attachments HTTP run failed HTTP 401 without bearer token. **Test-harness issue:** first custom responsive probe used invalid Playwright API `context.setViewportSize`. **Environment limitation:** authenticated shared QA pass was not executed because private Render QA login credentials/session are not available in repo/session. **Tooling issue:** first `gh pr create` attempt used Bash heredoc syntax that PowerShell rejected. **Process issue:** PR #104 `qa-depth-gate` initially failed because the PR body missed required evidence sections. | Fixed/mitigated in-session: installed official Playwright Chromium, reran browser checks successfully, reran HTTP comments/attachments check with bearer token from `AuthService.login`, corrected responsive probe to `page.setViewportSize`, added repo report evidence, ran unauthenticated shared QA route smoke, reran PR creation with PowerShell-compatible input, pushed PR #104, commented Jira, and updated the PR body with QA Depth Gate sections. Authenticated shared QA remains pending on private credentials/session or DonHV acceptance of local evidence. | `npm run dev:sqlite:refresh-views` exit 0; local app probe returned 200; `npx playwright install chromium` exit 0; rerun `npm run qa:idts57:browser` pass; `npm run qa:idts60:browser` pass; `npm run qa:comments-attachments:http` with bearer token pass; custom responsive probe pass (`MOBILE_PROFILE_REFRESH_OVERLAP=false`, `BACK_AFTER_LOGOUT_GUARDED=true`, `LONG_COMMENT_INPUT_LENGTH=620`, `VISIBLE_INTERNAL_TEXT_FOUND=false`, `HTTP_5XX_COUNT=0`, `PAGE_ERROR_COUNT=0`); dashboard probe pass (`DESKTOP_HAS_DASHBOARD=true`, `MOBILE_HAS_DASHBOARD=true`); shared QA route smoke `login.html` HTTP 200 and `dashboard.html` HTTP 200; first PR #104 `qa-depth-gate` run failed with missing PR body sections. Report: `docs/qa/uat-reports/idts-59-ui-ux-scan-report.md`; PR #104 `https://github.com/DarwinDO/IDTS-SAP01/pull/104`; Jira comments `10411`, `10415`, `10416`. | DonHV review PR #104 and decide whether local evidence plus public route smoke is enough, or provide private Render QA credentials/session for authenticated shared-QA scan. |
| 2026-07-08 | Git/Jira handoff for SangVN | Diagnosed why local checkout from `feature/idts-47-history-timeline-limit-sangvn` to `dev` failed; checked Jira live state for SangVN's immediate next task. | Switched local workspace to `dev` and fast-forwarded to `origin/dev` commit `35516dc`; confirmed Jira `IDTS-47` is Done and `IDTS-59` is the only immediate next SangVN handoff task. | **Process/tooling issue:** `git switch dev` was blocked because local `package-lock.json` changes would be overwritten. The previous remote tracking branch `origin/feature/idts-47-history-timeline-limit-sangvn` is gone after cleanup, so the local IDTS-47 branch is stale. A diagnostic command `git checkout --dry-run dev` also failed because this Git version does not support that option. | Fixed/mitigated in-session by stashing only `package-lock.json` as `stash@{0}` (`pre-dev-checkout-package-lock-sangvn-20260708`), switching to `dev`, and running `git pull --ff-only`. Untracked local evidence/backup files remain and should be reviewed separately if cleanup is needed. | `git switch dev` initially failed with local `package-lock.json` overwrite warning; `git stash push -m "pre-dev-checkout-package-lock-sangvn-20260708" -- package-lock.json`; `git switch dev`; `git pull --ff-only`; `git log -1 --oneline --decorate` -> `35516dc (HEAD -> dev, origin/dev) chore: add IDTS-45 continuity proof runner (#101)`; Jira Rovo check confirmed `IDTS-59` is the immediate next task. | Move `IDTS-59` to In Progress and run the shared-QA UI/UX scan. |
| 2026-07-07 | IDTS-47 History Timeline limit | Finished the History Timeline limit implementation, reran browser evidence with a real Playwright click on the generated UI5 panel toggle, commented Jira, pushed the branch, and opened PR #87. | Implementation verified locally and PR is open for DonHV review. | Test-harness issues found in this session were resolved: missing fixture fields, too-strict Object Page wait, incomplete History anchor selector, and DOM `click()` not triggering the UI5 expandable panel. No product regression remained after rerun. | Fixed/verified. | UI5 MCP linter no findings; `npx ui5 build --config ui5.yaml --clean-dest` pass; `npx cds compile srv app/bug-management-ui --to edmx -s all` pass with known attachment warning; browser QA PASS `initialCount=5 finalCount=8 detailVisible=true`; screenshots in `docs/pm/evidence/idts-47/`; Jira comment `10366`; PR `https://github.com/DarwinDO/IDTS-SAP01/pull/87`. | DonHV review PR #87, then merge and move Jira when accepted. |
| 2026-07-07 | IDTS-47 History Timeline limit | Ran a DOM diagnostic script to inspect generated controls around `Show Details`. | Diagnostic did not reach browser because the quick fixture was incomplete. | **Test-harness/data issue:** the diagnostic fixture inserted a `Bugs` row without required fields such as `stepsToReproduce`, so SQLite rejected it with a NOT NULL constraint. | Fixed in-session by using the complete fixture shape from the passing browser QA script. | Diagnostic script failed with `SqliteError: NOT NULL constraint failed: idts_cap_Bugs.stepsToReproduce`; final browser QA later passed with full fixture. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Reran browser QA with a complete temporary bug fixture and stricter detail assertion. | The rerun did not reach the timeline assertion. | **Test-harness issue:** Playwright timed out waiting for `IDTS-47 history event 1`; the Object Page History section had to be selected before waiting for custom-section content. This was not a product regression. | Fixed in-session by explicitly clicking the Object Page `History` anchor before timeline assertions. | Inline Playwright QA failed after 90s at `locator('text=IDTS-47 history event 1').first().waitFor({ state: 'visible' })`; later rerun with History anchor passed. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Reran browser QA after explicitly clicking the Object Page `History` anchor. | Initial limit and growing checks passed, but the detail click target was still wrong. | **Test-harness issue:** the DOM-based click selected a visible `Show Details` label container (`DIV#__data402`) and later `target.click()` on the UI5 panel wrapper did not trigger the UI5 event stack. Product detail behavior was still under verification at this step. | Fixed in-session by using Playwright's real pointer click on `.sapMPanelWrappingDiv[role="button"]:has-text("Show Details")`. | Inline Playwright QA passed the 5-item initial check and 8-item growing check, then failed until the selector/click method was corrected; final rerun passed with visible detail table. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Started a DOM diagnostic for the generated `Show Details` control. | Diagnostic needed a broader anchor selector. | **Test-harness issue:** the diagnostic tried only `role=link`/button selectors for the Object Page History anchor and timed out when the runtime rendered the anchor differently. | Fixed in-session by broadening the diagnostic selector to include `role=tab` and using the same anchor coverage as the passing QA script. | Diagnostic failed with `locator('[role="link"]:has-text("History"):visible,button:has-text("History"):visible').first()` timeout; corrected diagnostic then identified `.sapMPanelWrappingDiv`. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Tried to strengthen the detail-expansion browser assertion with a `.sapMPanel` selector. | Selector needed runtime DOM inspection. | **Test-harness issue:** `.sapMPanel` did not match the generated runtime panel structure for the Fiori custom section, so the detail assertion failed before clicking. | Fixed in-session by DOM diagnostic and Playwright click on the generated `.sapMPanelWrappingDiv[role="button"]`. | Inline Playwright QA failed waiting for `.sapMPanel...locator('.sapMPanelExpandableIcon, .sapMPanelWrappingDiv')`; final browser QA later passed with visible detail table. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Visually inspected the first PASS browser screenshots. | Initial/growing evidence looked correct; detail evidence needed a stronger assertion. | **Test-harness issue:** the detail-expansion step reported PASS, but screenshot `03_history_detail_expanded.png` did not visibly show the detail table. The assertion likely matched a hidden/accessibility text node, so the evidence was not strong enough for the "preserve expandable details" criterion. | Fixed in-session by rerunning detail evidence with a visible old/new table assertion. | Visual check of the first `03_history_detail_expanded.png` showed focus on `Show Details` but no visible old/new value table; final screenshot now shows the detail table. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Reran browser QA with the Object Page wait condition fixed. | Core IDTS-47 browser behavior reached: initial timeline render and growing access to older events worked before the final detail-expansion selector failed. | **Test-harness issue:** `getByText(/Show Details/)` matched an invisible accessibility text span before the visible panel header, causing a click timeout. This is selector ambiguity, not proof that details are broken. | Fixed in-session by switching to the visible generated panel toggle selector. | Inline Playwright QA produced `01_history_initial_limited.png` and `02_history_after_growing_more.png`, then timed out clicking hidden `sapUiInvisibleText` for `Show Details`; final browser QA passed. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Reran browser QA after confirming `localhost:4004` was listening. | Browser reached the Object Page route, but the test wait condition was too strict. | **Test-harness issue:** the QA script waited for the generated bug title to be visible, but Fiori rendered the title text in a hidden span (`__title2-inner`) while the Object Page route was already loaded. This is a harness selector/wait issue, not evidence that the History UI failed. | Fixed in-session by waiting for History/timeline content instead of the generated title span. | Inline Playwright QA timed out after 90s on hidden title text; final browser QA passed after changing the wait target. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Ran first browser QA attempt with a temporary bug containing 8 history events. | Fixture setup reached browser navigation, but runtime server was unavailable. | **Environment blocker:** `localhost:4004` returned `net::ERR_CONNECTION_REFUSED` during Playwright navigation even though an earlier root probe had returned 200. The active local CAP/Fiori server stopped or was not listening by the browser run. | Fixed in-session by confirming/reusing an active local server on port 4004 and rerunning browser QA. Temporary bug fixture cleanup ran in the script finally block. | Inline Playwright browser QA failed at `page.goto` with `net::ERR_CONNECTION_REFUSED`; final browser QA later passed on `localhost:4004`. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Ran first deterministic verification for the History Timeline UI change. | UI5 linter and UI5 build passed; CAP compile needed a corrected command. | **Tooling issue:** `npx cds compile srv app/bug-management-ui --to edmx` failed because the current model has multiple services (`AuthService`, `BugService`) and CAP requires an explicit service selector. This is a verification-command issue, not a product regression. | Fixed in-session by rerunning CAP compile with `-s all`. | UI5 MCP linter for `webapp/ext/fragment/HistoryTimeline.fragment.xml` returned no findings; `npx ui5 build --config ui5.yaml --clean-dest` passed; `npx cds compile srv app/bug-management-ui --to edmx -s all` passed with known attachment warning. | No remaining action. |
| 2026-07-07 | IDTS-47 History Timeline limit | Continued IDTS-47 implementation and PM/doc verification after the session resumed. | Implementation continuation started. | **Tooling issue:** a board-inspection command used `Select-Object -Index 118..140`, which PowerShell parsed as a string instead of an integer array and rejected. | Fixed in-session by switching to `Select-Object -Skip 118 -First ...` for targeted reads. | `Get-Content docs/pm/task-board.md \| Select-Object -Index 118..140` failed with `Cannot convert value "118..140" to type "System.Int32"`. | Continue board update and verification. |
| 2026-07-06 | IDTS-47 History Timeline limit | Started Jira task setup: read Jira IDTS-47, moved Jira to In Progress, and created branch `feature/idts-47-history-timeline-limit-sangvn` from `origin/dev`. | Task setup completed; implementation analysis starting. | **Tooling/process issue:** switching from the old IDTS-56 branch to a new `origin/dev` branch was initially blocked by a pre-existing local `package-lock.json` modification. A first stash apply command also failed because PowerShell parsed `stash@{0}` unless quoted. | Fixed in-session; stashed only `package-lock.json`, created the branch from `origin/dev`, then applied the stash back with quoted ref. The package-lock change remains local/uncommitted and should not be included in IDTS-47. | `git switch -c feature/idts-47-history-timeline-limit-sangvn origin/dev` initially blocked by dirty `package-lock.json`; `git stash push -m pre-idts47-package-lock-user-change -- package-lock.json`; `git stash apply 'stash@{0}'`; branch status now `feature/idts-47-history-timeline-limit-sangvn...origin/dev`. | Review current History Timeline implementation, choose the smallest supported UI5/Fiori fix, then run browser evidence. |
| 2026-07-06 | IDTS-56 Smart assignment UI | Completed Smart Assign developer dialog/dropdown for Bug Object Page, added QA scripts/evidence, updated PM docs and knowledge mirrors, pushed branch, opened PR, and commented Jira. DonHV later resolved PR conflict, merged PR #79 into `dev`, and moved Jira to Done. | Implementation, review, merge, and Jira closure completed. | Product defect found and fixed in-session: Smart Assign initially treated hidden `componentCategory_ID` as missing when Object Page had not selected it. Test-harness/tooling issues found and mostly fixed in-session: QA result reporting, browser readiness, SQLite fixture boolean binding, auth fixture `issuedAt`, local preview console allow-list, and Playwright hidden-button selector. Root-level ESLint config for `scripts/qa/*.js` remains an open tooling baseline gap. Merge conflict with newer `dev` i18n/PM docs was resolved by keeping latest `dev` and re-adding IDTS-56 Smart Assign content. | Fixed for product/UI and IDTS-56 test harness; PR conflict resolved; root-level ESLint gap remains open and documented. | PR #79 `https://github.com/DarwinDO/IDTS-SAP01/pull/79`; merge commit `9bd846a84b07f44e58cf303318bfda91fd3d4759`; Jira comments `10352`, `10353`, `10356`; `npm run qa:idts56:programmatic` -> 10 PASS / 0 FAIL; `npm run qa:idts56:browser` -> PASS; `npx ui5 build --config ui5.yaml --clean-dest` -> PASS; `npm run qa:secret-scan` -> PASS. | IDTS-56 Done. Continue SangVN follow-up work on IDTS-59/IDTS-47 as planned. |
| 2026-07-05 | IDTS-59 manual UI/UX scan assignment | DonHV created a focused Sprint 4 manual UI/UX scan task for SangVN after checking Jira coverage. | Jira `IDTS-59` created under `IDTS-51`, assigned to SangVN, due 2026-07-11, and linked to `IDTS-58` and `IDTS-57`. | None found in this planning step. The task itself is designed to find product defects, UX gaps, data issues, environment blockers, tooling issues, or test-harness issues during manual UAT. | Ready for SangVN execution. | Jira JQL verified `IDTS-59` is in Sprint 4 id `70`, parent `IDTS-51`, status To Do, with due date and issue links. | SangVN should run the scan on shared QA and create/link separate Jira issues for actionable findings. |
| 2026-07-01 | IDTS-35 local login environment handoff | DonHV reviewed SangVN's login screenshot after updating to the authenticated `dev` baseline. | Root causes identified from the SQL error shown by the UI. | **Data/environment issue:** SangVN's source expects `Users.passwordHash`, but the local `db.sqlite` still has the older pre-IDTS-34 schema, so login fails with `SQLITE_ERROR: no such column: $U.passwordHash`. **Product/security defect:** the login UI exposes the raw internal SQL/query error instead of a safe generic sign-in error; tracked as Jira `IDTS-39`. | Local schema issue remains open on SangVN's machine; stop `cds watch`, back up and evolve/recreate SQLite, then set the local password. Raw-error disclosure is tracked separately for a focused backend error-sanitization fix and regression test. | Screenshot shows the generated SELECT failing specifically on `$U.passwordHash` and the full SQL being rendered in the login error box; current `db/schema.cds` defines `passwordHash` and `passwordChangedAt`; Jira: https://dutassociation.atlassian.net/browse/IDTS-39. | SangVN to migrate local DB and verify login; DonHV owns `IDTS-39` safe-error fix. |
| 2026-07-01 | IDTS-32 Manual browser UAT | Added interim Jira summary after the current manual browser UAT pass. Confirmed that, apart from the logged findings/observations, the main tested workflow paths look stable enough for DonHV review. | Interim manual UAT summary completed and handed off. | No new product bug in this summary step. Existing open items remain: Priority/Severity validation gap, History vs History Timeline UX decision, Developer-create entry UX follow-up, and optional wording around Reopen from Resolved. | Open items are logged for DonHV/team triage; no additional fix made in this docs/evidence branch. | Jira `IDTS-32` comment `10265`; repo evidence folder `docs/pm/evidence/idts-32/`. | DonHV to review Jira comments and repo evidence, then decide which findings need separate fix tickets versus acceptance for current Sprint 3 manual UAT baseline. |
| 2026-07-01 | IDTS-32 Manual browser UAT | Checked bug create input validation for required Priority and Severity fields. Confirmed from catalog data that valid Priority codes are `LOW`, `MEDIUM`, `HIGH`, `CRITICAL`, and valid Severity codes are `MINOR`, `MAJOR`, `CRITICAL`, `BLOCKER`. Added Jira comment and repo screenshot evidence. | Priority/Severity validation finding recorded. | **Product defect / validation gap:** entering invalid free-text value `1` into Priority and Severity still allowed bug creation. This can create invalid master-data codes and affect filtering/reporting/semantic coloring. | Open; likely needs FE value-help/input validation and backend code-list existence validation before save/activation. | Browser screenshot: `docs/pm/evidence/idts-32/priority-severity-invalid-1-accepted.png`; Jira `IDTS-32` comment `10262`; local code/catalog check: `db/data/idts.cap-PriorityValues.csv`, `db/data/idts.cap-SeverityValues.csv`, `srv/bug-service/bug-write.js`. | DonHV/FE/backend team to decide fix owner and whether to track as separate defect under Sprint 3 QA. |
| 2026-07-01 | IDTS-32 Manual browser UAT | Checked Object Page history presentation on `BUG-0005` and raised the question whether `History` and `History Timeline` should both remain visible. Added Jira comment and repo screenshot evidence for DonHV review. | History presentation observation recorded for PM/FE decision. | **UX/product follow-up:** `History` raw table and `History Timeline` event-first view both appear at the same navigation level, which may feel duplicated/confusing for normal Tester/Developer/PM users. | Open for DonHV/team decision; recommended direction is to keep `History Timeline` as primary user-facing history and keep/hide raw `History` based on audit/debug/admin need. | Browser screenshot: `docs/pm/evidence/idts-32/history-and-history-timeline-duplicate-ux.png`; Jira `IDTS-32` comment `10260`. | DonHV/FE team to decide whether raw `History` stays visible for Sprint 3 or is moved/hidden as advanced audit view. |
| 2026-07-01 | IDTS-32 Manual browser UAT | Started SangVN's manual UAT on role/action visibility using Developer account `sangvn`. Confirmed that attempting to create a bug as Developer is blocked on submit with `Only Tester or PM users can create bug reports.` Added Jira evidence comments and stored screenshot evidence in repo. | First role/action visibility check completed: backend/business-rule enforcement is PASS because only Tester and PM may create bug reports. | **UX follow-up observation:** Developer can still enter and fill the new bug draft/create form before being blocked at `Create`; this is not a core permission defect, but may be FE polish if the team wants the Create entry hidden/disabled earlier for Developer users. | Open as optional UX follow-up; no product rule fix needed for permission enforcement. | Browser screenshot: `docs/pm/evidence/idts-32/sangvn-dev-create-blocked-assignee-empty.png`; Jira `IDTS-32` comments `10256`, `10257`. | Continue IDTS-32 manual UAT with Tester/PM create flow and remaining lifecycle/history checks. |
| 2026-07-01 | Local project open/debug | Investigated why the local CAP/Fiori project does not open for SangVN. Checked environment, dependency status, SQLite service views, CAP compile, and `npm run watch-bug-management-ui`. | Root cause identified. | **Environment/tooling issue:** local `node_modules` is stale after recent SMTP/outbox merge; `nodemailer` and `smtp-server` are declared in `package.json` but missing locally. CAP server start fails in `srv/email/sender.js` with `Cannot find module 'nodemailer'`. **Tooling issue:** AI DevKit memory search was blocked by sandbox/network access to npm registry. | Open pending dependency install. | `npx cds --version` reported missing `nodemailer@^9.0.1` and `smtp-server@^3.15.0`; `npm run dev:sqlite:refresh-views` exit 0; `npx cds compile srv app/bug-management-ui --to edmx -s all` exit 0 with known attachment warning; `npm run watch-bug-management-ui` reproduced server start failure; `npx ai-devkit@latest memory search ...` failed with npm registry `EACCES`. | Run `npm install`, then rerun `npm run watch-bug-management-ui` and browser open check. |
| 2026-06-28 | IDTS-19 / PR #18 DonHV merge review | DonHV re-reviewed PR #18, found that the branch had moved the verified PM monitoring List Report tabs from page-level `views.paths` into table-level `quickVariantSelection`, then restored the `views.paths` configuration while keeping the new `HistoryTimeline` Object Page custom section. Updated the `manifest.json` knowledge mirror to explain why this app must keep the verified OData V4 tab configuration. | PR #18 fix completed on SangVN branch. | **Fiori configuration/process issue:** branch was Git-mergeable but would have changed the previously verified PM monitoring tab mechanism. This was not a backend defect. | Fixed in this session. | Fiori MCP `search_docs` confirmed OData V4 multiple List Report views use `views.paths`; UI5 manifest validation passed; `npx cds compile srv app/bug-management-ui --to edmx` pass with known attachment warning; `npx ui5 build --config ui5.yaml` pass; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `npm run qa:history-events:programmatic` -> 13 PASS / 0 FAIL; `git diff --check origin/dev HEAD` pass. | Push branch update, merge PR #18 into `dev`, then update Jira IDTS-19. |
| 2026-06-27 | IDTS-19 / Jira comment fix (Codex) | Continued from Codex state. Ran CDS compile, UI5 build, and checked git diff for spacing. Server started properly on port 4004. Playwright sandbox prevented automated UI capture, but server is fully up with the Timeline and i18n configs intact. | Compiled and built successfully. | Test-harness issue: `browser_subagent` encountered sandbox restrictions reading allowed paths. | Left for SangVN's manual UAT (IDTS-32). | `npx cds compile srv app/bug-management-ui --to edmx` exit 0; `npm run watch-bug-management-ui` started server at port 4004; `git diff --check` clean. | SangVN to do manual browser check and push branch. |
| 2026-06-30 | IDTS-32 roadmap lookup | Read local PM handover and attempted to retrieve Jira `IDTS-32` through Atlassian Rovo before giving the task roadmap. | Local roadmap source confirmed from `task-board.md`, `sangvn.md`, `wp5-comments-history.md`, and `wp6-pm-monitoring.md`. | Tooling issue: Jira MCP lookup failed for cloud ID `darwindo.atlassian.net` with tenant-info 404, and Rovo natural search returned no `IDTS-32` results in this session. | Open for connector/session access; local PM docs remain usable. | `tool_search` loaded Atlassian Rovo; `_searchjiraissuesusingjql` returned tenant-info 404; `_search` returned empty results for `Find Jira issue IDTS-32`. | SangVN can use the local PM roadmap now; Jira details should be rechecked when connector access recovers. |
| 2026-06-30 | Pull latest `dev` request | Pulled latest `origin/dev` into `feature/idts-19-history-timeline-sangvn` before IDTS-32 work. | Branch is now fast-forwarded to `92aaa12`. Local SangVN status edits were restored after the pull. | Process/tooling issue: first merge attempt was blocked because local `docs/pm/status/sangvn.md` edits would be overwritten. | Fixed in-session by stashing only the local status-file edit, pulling `dev`, then restoring the status edit without conflict. | First `git pull origin dev` fetched but aborted with "local changes would be overwritten"; second `git pull origin dev` fast-forwarded to `92aaa12`; `git stash pop` auto-merged cleanly; `git diff --check -- docs/pm/status/sangvn.md` pass. | Continue IDTS-32 work on latest `dev`; keep untracked `diff.patch` untouched unless the owner clarifies it. |
| 2026-06-27 | IDTS-19 / Manual Browser UAT (IDTS-32) | SangVN manually verified the Fiori UI locally and provided screenshots. Verified PM monitoring tabs still exist and History Timeline renders, expands, and shows translated i18n texts properly. | Browser verification completed. | None | Fixed | Provided UI screenshot: `History Timeline` section renders successfully with i18n keys and expandable details on Object Page. | Push branch and request DonHV to re-review PR #18. |
| 2026-06-27 | IDTS-19 / Browser verification Playwright setup | Started local CAP/Fiori server and attempted Playwright browser verification for PM tabs and Object Page History Timeline. | Local server started on port 4004; OData auth/data probe worked with mock user `DonHV`. | Environment blocker: Playwright package is installed but its managed Chromium binary is missing, so `chromium.launch()` failed and suggested `npx playwright install`. | Under investigation; try local Edge/Chrome executable before considering browser download. | `Start-Process npx.cmd cds watch --port 4004 ...` returned PID 12628; OData query for `HistoryEvents` returned grouped history rows; Playwright failed: missing `chromium_headless_shell-1228`. | Retry Playwright with installed system browser executable. |
| 2026-06-27 | IDTS-19 / Browser verification setup | Refreshed local SQLite service views, then tried to start local CAP/Fiori server for browser verification. | `dev:sqlite:refresh-views` completed. | Environment blocker: sandbox blocked `Start-Process` for launching the local `npx cds watch` server (`Access is denied`). | Under investigation; retry requires escalated local process permission. | `npm run dev:sqlite:refresh-views` exit 0; `Start-Process ... npx cds watch --port 4004 ...` failed with Access denied. | Retry server startup with approved escalation, then run Playwright browser checks. |
| 2026-06-27 | IDTS-19 / PR #18 review fix verification | Ran available FE lint after restoring PM monitoring tabs and i18n/knowledge mirror updates. | CAP compile and UI5 build passed; lint result captured for review transparency. | Test-harness/tooling issue: `npx eslint webapp` fails on pre-existing CRLF linebreak-style errors in `webapp/Component.js` and warnings in existing OPA test files; the current IDTS-19 change did not modify those JavaScript files. | Open as tooling/style baseline issue; not fixed in this PR to avoid broad unrelated line-ending churn. | `npx eslint webapp` exit 1: 244 problems, 11 errors in `webapp/Component.js` for CRLF linebreak-style and 233 warnings in existing test files. | Continue browser verification for the timeline and PM monitoring tabs; decide separately whether to normalize FE JS line endings repo-wide. |
| 2026-06-27 | IDTS-19 / Jira comment handoff | Tried to read the current task review comment for branch `feature/idts-19-history-timeline-sangvn` before making code changes. Jira/Rovo connector and Jira/Atlassian CLI were not available in this Codex session; GitHub CLI exists but network access for `gh pr view --comments` was blocked and the escalation request was not approved. | Context and blocker captured before code changes. | Tooling issue: direct Jira comment retrieval is unavailable in the current tool/session setup, and fallback PR comment retrieval cannot run without network approval. | Open; needs user-provided Jira comment text or connector/network access. | `tool_search` exposed CAP/Fiori/Memory MCP only; `list_available_plugins_to_install` listed no Jira plugin; `Get-Command jira/acli/rovo` returned no tools; `gh pr view --comments --json ...` failed with socket permission error; escalated retry was rejected. | User to paste the Jira comment or enable Jira/GitHub access, then SangVN can apply the requested code fix. |
| 2026-06-27 | IDTS-19 (Sprint 03) | Redesigned timeline from `sap.suite.ui.commons.Timeline` back to `sap.m.List` + `CustomListItem` with timeline-style layout. Root cause of logs not rendering: `sap.suite.ui.commons.TimelineItem.embeddedControl` drops nested OData V4 composition binding in FPM custom sections. New design uses standard `sap.m` controls where `$expand: 'logs'` works natively. Removed `sap.suite.ui.commons` from manifest. Fixed merge conflict markers in `sangvn.md`. | Fragment rewritten, manifest cleaned, status file fixed | 1) Product defect: nested Table inside `sap.suite.ui.commons.TimelineItem.embeddedControl` does not bind OData V4 composition data in FPM custom sections. 2) Process issue: `sangvn.md` had residual merge conflict markers from previous rebase. | 1) Fixed by switching to `sap.m.List` + `CustomListItem`. 2) Fixed by rewriting file. | `npx cds compile srv app/bug-management-ui --to edmx` (exit 0, only known attachment warning) | Browser verification needed. Push and update PR. |
| 2026-06-27 | IDTS-32 / IDTS-33 manual UAT split | DonHV split the original broad IDTS-32 manual browser UAT scope. IDTS-32 now stays with SangVN and covers workflow/history-heavy testing: Developer lifecycle, Need More Information/resubmit, reject follow-up, retest/close/reopen, grouped history timeline, and role/action visibility. | IDTS-32 Jira description updated and linked to IDTS-33. | No product bug found in this planning step. Process change: FE-shell-heavy UAT was moved out to DatDT under IDTS-33 to reduce overload and make ownership clearer. | Not applicable. | Jira IDTS-32 edited; Jira IDTS-33 created and linked with `Relates`; comments added on both Jira issues. | SangVN to execute IDTS-32 workflow/history UAT and record findings/evidence in this file. |
| 2026-06-24 | IDTS-32 Manual browser UAT planning | DonHV created Jira task IDTS-32 for SangVN to manually test Sprint 3 browser flows: PM monitoring slices, bug create, assignment/reassignment, developer lifecycle, Need More Information/resubmit, reject follow-up, retest/close/reopen, grouped history timeline, comments/attachments/notifications, and role/action visibility. | Jira task created and assigned to SangVN. | No product bug found in this planning step. Process note: manual UAT must classify any later finding as product defect, environment blocker, tooling issue, test-harness issue, data issue, documentation issue, or process issue. | Not applicable. | Jira IDTS-32 created through Atlassian Rovo; PM board updated. | SangVN to execute manual browser UAT on latest `dev` and update this status file with evidence/findings. |
| 2026-06-23 | IDTS-20 PR #14 DonHV support fix | DonHV reviewed the pushed PR #14 branch against SangVN's Jira comment, merged the current `origin/dev` documentation baseline into the branch, restored the missing `package-lock.json` package root name, replaced the remaining value-help labels `Next Processor User` and `Next Processor Role` with `Current Action Owner` and `Action Owner Role`, and updated the matching knowledge mirror for `value-helps.cds`. | PR #14 cleanup support completed on SangVN branch. | Process/documentation mismatch: Jira comment said `package-lock.json` was reverted, but the PR still removed `packages[""].name`. UI wording issue: `value-helps.cds` still exposed `Next Processor User/Role` labels in metadata-driven value-help surfaces. | Fixed in this support commit. | `git diff --check` PASS; targeted `rg "Next Processor User|Next Processor Role" app/bug-management-ui/annotations` returned no matches; `npx cds compile srv app/bug-management-ui --to edmx` exit 0 with only the known `@cap-js/attachments` warning; `npx ui5 build --config ui5.yaml` in `app/bug-management-ui` succeeded; `npx ai-devkit@latest lint --json` PASS; secret scan returned no matches. | DonHV to push this branch, merge PR #14, then continue DatDT PR/branch review. |
| 2026-06-23 | IDTS-20 (Sprint 03) | Fixed PR #14 review findings from DonHV. 1) Removed invalid `_ID` and `_code` annotations on foreign keys in `labels.cds` to fix compiler warnings. 2) Fixed DataPoint `Title` in `object-page.cds` to 'Assignee (Technical Owner)'. 3) Replaced `nextProcessorRoleName` and `nextProcessorUserDisplayName` with `currentActionOwnerDisplayName` in List Report and Assignment field groups to remove ambiguity. 4) Reverted unintended `package-lock.json` changes. | Cleaned up all PR #14 findings and compiler warnings | `npx cds compile srv app/bug-management-ui --to edmx` was throwing `has not been found` warnings for foreign keys. `package-lock.json` was mistakenly modified. | Fixed | `git checkout package-lock.json`; `npx cds compile srv app/bug-management-ui --to edmx` (0 warnings on UI); `git diff --check` (pass) | SangVN to commit and push/re-create PR for DonHV |
| 2026-06-22 | IDTS-20 (Sprint 03) | Refined UI labels and added helper texts (QuickInfo and DataPoints) for ownership semantics per DEC-022. Updated 4 annotation CDS files: `labels.cds` (Assignee → Assignee (Technical Owner) + QuickInfo, Next Processor User → Current Action Owner + QuickInfo, Next Processor Role → Action Owner Role + QuickInfo, Pending Assignment → Awaiting Assignment, Rejected Follow-up → Rejected — Needs Follow-up), `ownership-assignment.cds` (Assignment and Rejected Follow-up FieldGroup labels), `list-report.cds` (Removed hardcoded column headers to inherit from labels.cds consistently), `object-page.cds` (added Current Action Owner to General Info section; **added Assignee and Current Action Owner to UI.HeaderFacets as DataPoints so their helper descriptions are natively visible on the Object Page in display mode**). | All 4 annotation files updated, CDS compile passed with no errors, browser verification passed | Fiori Elements display mode does not natively show QuickInfo tooltips for read-only text fields. The initial ugly inline-text solution was rejected. | UI Limitation fixed: Used UI.HeaderFacets with UI.DataPoint to show the helper texts as professional header descriptions instead of inline text. | `git checkout -b feature/idts-20-ownership-labels-sangvn`; `npm install`; `npx cds compile srv --to edmx` (pass); Manual browser check (passed) | Done. Proceed to next task (IDTS-19). |
| 2026-06-17 | IDTS-9 QA UI Retest | Re-tested the Assign Developer action dialog on the latest local FE build to verify whether the selected input now resolves the developer name instead of the UUID | Confirmed that the dialog is visible, the field label is now `Assignee`, and the value-help rows are business-friendly. The remaining issue is narrower: after selecting a developer, the input still shows the raw developer-profile UUID instead of the readable developer name. | Likely a standard Fiori Elements action-parameter selected-text limitation or a remaining FE metadata handling gap for this dialog. Browser evidence confirms the symptom; root-cause classification is still under team review. | Open (investigated) | Local run via `npm run watch-bug-management-ui`; browser retest on `http://localhost:4004/...`; screenshots `dialog_before_selection_1781693070331.png`, `dialog_after_selection_1781693103672.png` | Wait for team decision on recommendation: accept for demo or build a targeted FE extension |
| 2026-06-13 | IDTS-8, IDTS-10, IDTS-11 (Sprint 02) | Updated Bug Detail UI annotations for status dropdown behavior, supporting information placement, and multiline action notes | UI annotations completed | None | Fixed | `cds compile srv --to edmx` | Manual QA and shared FE follow-up |
| 2026-06-03 | Status setup | Member status file created from previous status baseline | Ownership assigned to SangVN | None | Fixed | `rg`, `git diff --check` | Continue on assigned Sprint work |

Vietnamese:

| Ngày | Task/WP | Đã làm gì | Phần đã xong | Khó khăn/Bug phát hiện | Trạng thái fix | Bằng chứng/Lệnh đã chạy | Handoff tiếp theo |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-06-28 | IDTS-19 / PR #18 DonHV merge review | DonHV review lại PR #18 và phát hiện branch đã chuyển cấu hình tab PM monitoring của List Report từ `views.paths` ở page settings sang `quickVariantSelection` trong table settings. DonHV khôi phục lại `views.paths` đã được verify trước đó, đồng thời vẫn giữ custom section `HistoryTimeline` mới trên Object Page. Knowledge mirror của `manifest.json` cũng được cập nhật để giải thích rõ vì sao app này cần giữ cấu hình tab OData V4 đã verify. | Đã fix PR #18 trực tiếp trên branch SangVN. | **Fiori configuration/process issue:** PR merge được ở mức Git nhưng có thể làm thay đổi cơ chế tab PM monitoring đã được verify. Đây không phải lỗi backend. | Đã fix trong phiên này. | Fiori MCP `search_docs` xác nhận multiple List Report views của OData V4 dùng `views.paths`; UI5 manifest validation pass; `npx cds compile srv app/bug-management-ui --to edmx` pass với warning attachment cũ; `npx ui5 build --config ui5.yaml` pass; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `npm run qa:history-events:programmatic` -> 13 PASS / 0 FAIL; `git diff --check origin/dev HEAD` pass. | Push branch update, merge PR #18 vào `dev`, rồi cập nhật Jira IDTS-19. |
| 2026-06-27 | IDTS-19 (Sprint 03) | Redesign timeline từ `sap.suite.ui.commons.Timeline` về `sap.m.List` + `CustomListItem` kiểu timeline. Nguyên nhân logs không render: `TimelineItem.embeddedControl` drop nested OData V4 composition binding trong FPM custom sections. Design mới dùng standard `sap.m` controls. Xóa `sap.suite.ui.commons` khỏi manifest. Fix merge conflict markers trong `sangvn.md`. | Fragment viết lại, manifest dọn, status file fix | 1) Product defect: nested Table trong `TimelineItem.embeddedControl` không bind OData V4 composition. 2) Process issue: `sangvn.md` còn merge conflict markers từ rebase trước. | 1) Fix bằng cách chuyển sang `sap.m.List`. 2) Fix bằng cách viết lại file. | `npx cds compile srv app/bug-management-ui --to edmx` (exit 0) | Cần browser verify. Push và update PR. |
| 2026-06-23 | IDTS-20 PR #14 DonHV support fix | DonHV kiểm tra branch PR #14 so với comment Jira của SangVN, merge baseline tài liệu mới nhất từ `origin/dev` vào branch, khôi phục `packages[""].name` bị mất trong `package-lock.json`, đổi label value-help còn sót `Next Processor User` và `Next Processor Role` thành `Current Action Owner` và `Action Owner Role`, đồng thời cập nhật knowledge mirror cho `value-helps.cds`. | Đã cleanup PR #14 trực tiếp trên branch SangVN. | Lệch quy trình/tài liệu: comment Jira nói đã revert `package-lock.json`, nhưng PR vẫn xóa `packages[""].name`. Lỗi wording UI: `value-helps.cds` vẫn còn label `Next Processor User/Role` trong các bề mặt UI sinh từ metadata. | Đã fix trong commit support này. | `git diff --check` PASS; scan `rg "Next Processor User|Next Processor Role" app/bug-management-ui/annotations` không còn match; `npx cds compile srv app/bug-management-ui --to edmx` exit 0 và chỉ còn warning cũ của `@cap-js/attachments`; `npx ui5 build --config ui5.yaml` trong `app/bug-management-ui` succeeded; `npx ai-devkit@latest lint --json` PASS; secret scan không có match. | DonHV push branch này, merge PR #14, rồi tiếp tục kiểm tra PR/branch của DatDT. |
| 2026-06-23 | IDTS-20 (Sprint 03) | Sửa các lỗi review PR #14 từ DonHV: 1) Xóa annotation trên các foreign key `_ID`/`_code` trong `labels.cds` để hết cảnh báo compile. 2) Chỉnh DataPoint `Title` thành 'Assignee (Technical Owner)'. 3) Dùng `currentActionOwnerDisplayName` thay cho 2 trường rời rạc của Next Processor trong List Report và Assignment để hết gây nhầm lẫn. 4) Revert lại `package-lock.json`. | Đã dọn dẹp xong mọi findings của PR #14. | Lệnh compile báo lỗi "has not been found" vì annotate trực tiếp lên foreign keys sinh tự động của CAP. | Đã fix | `git checkout package-lock.json`; `npx cds compile srv app/bug-management-ui --to edmx` (hết warning UI); `git diff --check` (pass) | SangVN tự commit, push và tạo lại PR sạch cho DonHV review |
| 2026-06-22 | IDTS-20 (Sprint 03) | Chỉnh label UI và thêm helper text (QuickInfo tooltip + DataPoints) cho ownership semantics theo DEC-022. Cập nhật 4 file annotation CDS: `labels.cds` (thêm tooltip giải thích rõ Assignee và Action Owner), `ownership-assignment.cds`, `list-report.cds` (xóa hardcode label để nhất quán), `object-page.cds` (thêm Current Action Owner vào General Info; **thêm Assignee và Current Action Owner vào UI.HeaderFacets dưới dạng DataPoint để hiện Description chuyên nghiệp trên Header ở chế độ Display**). | Cả 4 file annotation đã cập nhật, CDS compile pass không lỗi, browser verify pass | Fiori Elements display mode không hiện QuickInfo cho read-only text. Giải pháp thêm text vào inline form nhìn rất xấu và bị từ chối. | Đã fix: Dùng UI.HeaderFacets với UI.DataPoint để hiển thị helper text cực chuẩn ở Header của Object Page. | `git checkout -b feature/idts-20-ownership-labels-sangvn`; `npm install`; `npx cds compile srv --to edmx` (pass); Kiểm tra tay trên Browser (pass) | Xong. Chuyển sang task tiếp theo (IDTS-19). |
| 2026-06-17 | IDTS-9 QA UI Retest | Retest dialog Assign Developer trên bản FE local mới nhất để kiểm tra ô chọn sau khi chọn developer có hiện tên hay vẫn hiện UUID | Đã xác nhận label của field là `Assignee`, các dòng trong value help đã thân thiện hơn về mặt nghiệp vụ. Vấn đề còn lại hẹp hơn: sau khi chọn developer, ô input vẫn hiện UUID của developer profile thay vì tên đọc được. | Nhiều khả năng đây là giới hạn của standard Fiori Elements với selected text của action parameter, hoặc vẫn còn một khoảng FE metadata handling chưa được resolve hết. Hiện mới nên kết luận ở mức `likely`, chưa nên khẳng định tuyệt đối. | Open (đã điều tra và giữ mở) | Chạy local bằng `npm run watch-bug-management-ui`; browser retest trên `http://localhost:4004/...`; screenshot `dialog_before_selection_1781693070331.png`, `dialog_after_selection_1781693103672.png` | Chờ team chốt hướng xử lý: chấp nhận cho demo hoặc làm targeted FE extension |
| 2026-06-13 | IDTS-8, IDTS-10, IDTS-11 (Sprint 02) | Cập nhật UI annotations cho Bug Detail liên quan status dropdown, vị trí Supporting Information, và multiline action note | Đã xong phần UI annotations | Không có | Đã xử lý | `cds compile srv --to edmx` | Manual QA và hỗ trợ FE tiếp theo |
| 2026-06-03 | Status setup | Tạo file status thành viên từ baseline status cũ | Đã giao ownership cho SangVN | Không có | Đã xử lý | `rg`, `git diff --check` | Tiếp tục theo Sprint task được assign |

## Update Rule

- SangVN updates this file after each work session.
- Record what was done, what part is complete, blockers, bugs/errors found, whether they were fixed, and verification evidence.
- Do not edit other members' status files unless coordinating with DonHV.

Vietnamese:

- SangVN cập nhật file này sau mỗi phiên làm việc.
- Ghi rõ đã làm gì, xong phần nào, blocker, bug/error phát hiện, đã fix hay chưa và bằng chứng verify.
- Không chỉnh file status của thành viên khác trừ khi phối hợp với DonHV.

## 2026-07-09 - IDTS-68 DonHV implementation support

English:

| Date | Task/WP | Work done | Completed part | Blockers/Bugs found | Fix status | Evidence/command | Next handoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-09 | IDTS-68 grounded bug/handoff summary | DonHV started implementation on behalf of SangVN, moved Jira to In Progress, and added backend `summarizeBugHandoff` action with grounded bug/comment/history context, provider fallback, sanitized `BUG_SUMMARY` audit, focused QA script, evidence, and knowledge mirror. | Backend draft and focused QA are complete. | Environment setup issue: fresh worktree needed `npm ci --include=dev`. Test-harness/product-safety issue: broad diagnostic scanner falsely flagged business text containing `from`. Documentation tooling issue: older knowledge mirrors contain invalid UTF-8, so `apply_patch` could not update them directly. | Fixed/mitigated in-session. | `npm run qa:idts68:programmatic` -> `28 PASS / 0 FAIL`; `npx cds compile srv --to edmx -s all` exit 0 with existing attachment warning; CAP MCP `search_model` confirms `BugService.summarizeBugHandoff`. | DonHV to run full regression/checks, open PR, attach/link evidence to Jira, and only close IDTS-68 after merge. |
| 2026-07-09 | IDTS-68 grounded bug/handoff summary | PR #115 passed `qa-depth-gate` and was squash-merged into `dev`. | Repository implementation is merged at `d5e4297`. | Local `gh pr merge` reported a cleanup error because root worktree owns local `dev`, but GitHub merge itself succeeded. | Not a product defect; confirmed `origin/dev` contains `d5e4297`. | PR #115 merged; Jira evidence comment `10431`. | DonHV to merge closure sync and transition Jira IDTS-68 to Done. |

Vietnamese:

| Ngay | Task/WP | Da lam gi | Phan da xong | Kho khan/Bug phat hien | Trang thai fix | Bang chung/Lenh da chay | Handoff tiep theo |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-09 | IDTS-68 grounded bug/handoff summary | DonHV implement thay SangVN, chuyen Jira sang In Progress, them backend action `summarizeBugHandoff` voi context bug/comment/history co can cu, provider fallback, audit `BUG_SUMMARY` da sanitize, focused QA script, evidence va knowledge mirror. | Backend draft va focused QA da xong. | Loi setup moi truong: fresh worktree can `npm ci --include=dev`. Loi test-harness/product-safety: diagnostic scanner qua rong flag nham business text co chu `from`. Loi tooling tai lieu: mot so knowledge mirror cu co invalid UTF-8 nen `apply_patch` khong sua truc tiep duoc. | Da fix/mitigate trong phien. | `npm run qa:idts68:programmatic` -> `28 PASS / 0 FAIL`; `npx cds compile srv --to edmx -s all` exit 0 voi warning attachment cu; CAP MCP `search_model` xac nhan `BugService.summarizeBugHandoff`. | DonHV chay full regression/checks, tao PR, attach/link evidence vao Jira, va chi dong IDTS-68 sau khi merge. |
| 2026-07-09 | IDTS-68 grounded bug/handoff summary | PR #115 da pass `qa-depth-gate` va squash-merge vao `dev`. | Implementation o repo da merge tai `d5e4297`. | Local `gh pr merge` bao loi cleanup vi root worktree dang giu local `dev`, nhung GitHub merge da thanh cong. | Khong phai product defect; da xac nhan `origin/dev` co `d5e4297`. | PR #115 merged; Jira evidence comment `10431`. | DonHV merge closure sync va transition Jira IDTS-68 sang Done. |

## 2026-07-09 - IDTS-69 DonHV implementation support

English:

| Date | Task/WP | Work done | Completed part | Blockers/Bugs found | Fix status | Evidence/command | Next handoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-09 | IDTS-69 smart assignment AI explanation | DonHV implemented on behalf of SangVN. Added backend `explainSmartAssignment`, AI explanation module, Smart Assign Explanation column, i18n keys, focused QA, regression updates, evidence, and knowledge notes. | Backend/UI implementation and focused local verification are complete. | Environment setup issue: fresh worktree required `npm ci --include=dev`. Documentation tooling issue: one old mirror has invalid UTF-8, so supplemental notes were created. Test-harness adjustment: IDTS-56 test timing/index updated for the new AI notice. | Fixed/mitigated in-session. | `npm run qa:idts69:programmatic` -> `6 PASS / 0 FAIL`; `npm run qa:idts56:programmatic` -> `13 PASS / 0 FAIL`; CAP compile passed with existing attachment warning. Evidence: `docs/pm/evidence/idts-69-smart-assignment-explanation/README.md`. | DonHV to run broader checks, open PR, and link Jira evidence. SangVN should review UI explanation behavior after PR/deploy. |

Vietnamese:

| Ngay | Task/WP | Da lam gi | Phan da xong | Kho khan/Bug phat hien | Trang thai fix | Bang chung/Lenh da chay | Handoff tiep theo |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-09 | IDTS-69 smart assignment AI explanation | DonHV implement thay SangVN. Da them backend `explainSmartAssignment`, AI explanation module, cot Explanation trong Smart Assign, i18n keys, focused QA, regression updates, evidence va knowledge notes. | Backend/UI implementation va focused local verification da xong. | Loi setup moi truong: fresh worktree can `npm ci --include=dev`. Loi tooling tai lieu: mot mirror cu co invalid UTF-8 nen tao supplemental notes. Dieu chinh test-harness: test IDTS-56 can sua timing/index vi co AI notice moi. | Da fix/mitigate trong phien. | `npm run qa:idts69:programmatic` -> `6 PASS / 0 FAIL`; `npm run qa:idts56:programmatic` -> `13 PASS / 0 FAIL`; CAP compile pass voi warning attachment cu. Evidence: `docs/pm/evidence/idts-69-smart-assignment-explanation/README.md`. | DonHV chay broader checks, tao PR va link Jira evidence. SangVN nen review UI explanation behavior sau khi PR/deploy. |
| 2026-07-09 | IDTS-69 smart assignment AI explanation closure | PR #117 passed `qa-depth-gate` and was squash-merged into `dev`; Jira IDTS-69 was moved to Done. | Repository and Jira handoff are complete. | Tooling issue: local `gh pr merge` cleanup reported `dev` is already used by root worktree, but GitHub merge succeeded. | Verified PR #117 state `MERGED` and `origin/dev` commit `ccaab62`. | Jira comments `10433`, `10434`; PR #117 merged. | SangVN can use the merged shared implementation as baseline when reviewing future Smart Assign UX or AI suggestion review UI. |

## 2026-07-24 - IDTS-94 review controls start

| Date | Task/WP | Work done | Completed part | Blockers/Bugs found | Fix status | Evidence/command | Next handoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-24 | IDTS-94 Handoff Summary and Smart Assign review controls | Read the Jira contract and project/PM handover context; created isolated branch `feature/idts-94-handoff-smart-assign-review-sangvn` from the open IDTS-91/92/93 dependency baseline so the dirty root worktree remains untouched. The user explicitly deferred the Knowledge Gate until implementation tasks are complete. | Intake and isolated implementation setup completed. | **Tooling issue:** mandatory OfficeCLI preflight returned `OFFICECLI_NOT_FOUND`; Markdown can still be edited as plain text, but OfficeCLI format/render validation is unavailable. **Knowledge/tooling result:** memory search for the IDTS-94 UI/review pattern returned no stored result. **Process issue:** Jira's due-date field is `2026-07-25`, while the issue description says `2026-07-29`. | OfficeCLI limitation remains an environment/tooling limitation; implementation can proceed with repo-native Markdown checks. Memory result requires relying on current source/Jira. Due-date discrepancy remains for Jira/PM clarification and does not change the feature scope. | `Get-Command officecli -ErrorAction SilentlyContinue` -> `OFFICECLI_NOT_FOUND`; memory search scope `repo:IDTS-SAP01` -> 0 results; branch base `aa621142dc81d0b313ff43e2b381843d72fb70b2`. | Inspect both existing UI flows, use Fiori/UI5 MCP, add focused tests first, then implement the smallest contextual controls. Do not merge or move Jira to Done before the deferred Knowledge Gate is completed. |
| 2026-07-24 | IDTS-94 Fiori MCP preflight | Queried Fiori MCP `list_functionality` for the existing app before modifying it. | Existing-app modification discovery was attempted. | **Environment/tooling issue:** Fiori MCP could not compile the CAP metadata because the isolated worktree does not yet have `@cap-js/attachments` installed, so `ManagedAttachments` could not be resolved. The server's separate advertised `tools/list` operation was not exposed by the current connector; tool schemas were inspected through Codex tool discovery before the call. | Under investigation; install the repository's locked dependencies in the isolated worktree, then rerun `list_functionality`. | Fiori MCP error: `Can't find package module '@cap-js/attachments'` and `No artifact ... ManagedAttachments`. | Run `npm ci --include=dev`, verify dependency resolution, and retry the Fiori MCP discovery call before editing UI files. |
| 2026-07-24 | IDTS-94 isolated dependency setup | Installed the repository's locked runtime/dev dependencies in the isolated worktree so SAP MCP and local verification can compile the CAP model. | Missing `@cap-js/attachments` dependency blocker resolved. | **Environment/security-tooling issue:** npm reports the active Node `v24.13.0` is outside the repository engine range `>=20 <23`. **Dependency audit issue:** the locked dependency graph currently reports 21 advisories (1 low, 9 moderate, 10 high, 1 critical). No automatic audit fix was run because that would be an unrelated/breaking dependency change. | Dependency installation succeeded; Node mismatch and dependency advisories remain existing repository/toolchain follow-ups, not IDTS-94 product changes. | `npm ci --include=dev` -> exit 0, 1395 packages installed; npm audit summary 21 vulnerabilities. | Retry Fiori MCP; keep IDTS-94 code change isolated and do not mutate the lockfile. |
| 2026-07-24 | IDTS-94 Jira start sync | Retrieved Jira transitions and prepared the implementation-start comment. | Confirmed transition `21` targets In Progress. | **Tooling issue:** the first composed Jira call used the wrong normalized connector method name inside the orchestration helper, so no comment or transition was executed in that failed call. | Fixed approach identified by inspecting the available normalized tool names; rerun with the exact exposed names. | Failed orchestration: `TypeError ... _addcommenttojiraissue is not a function`; available names confirmed as `mcp__codex_apps__atlassian_rovo_addcommenttojiraissue` and `..._transitionjiraissue`. | Rerun the Jira comment and transition, then verify issue state. |
| 2026-07-24 | IDTS-94 test-first baseline | Added a focused static contract test for the CAP audit ID response and both contextual review-control surfaces, then ran it before implementation. Jira start comment `10658` was added and IDTS-94 moved to In Progress. | Red test confirms the new test detects the missing IDTS-94 contract. | **Expected test failure:** the first assertion failed because `BugHandoffSummaryResult` does not yet expose `suggestionID`. This is the intended pre-implementation red state, not a regression. | Open by design; implement the minimal CAP/UI changes and rerun to green. | `npm run qa:idts94:programmatic` -> exit 1 at `handoff contract returns audit ID`; Jira comment `10658`; Jira status In Progress. | Implement response IDs and controls, then run the focused test plus behavioral regressions. |
| 2026-07-24 | IDTS-94 review-control implementation | Added the minimal audit-ID response contract and contextual review controls, then reran the focused test. | CAP/UI contract checks progressed past the new IDTS-94 assertions. | **Product/security defect:** the existing Smart Assign controller displayed caught backend `error.message` for assignment/load failures, which could expose internal diagnostics and violates IDTS-94's safe-error note. The focused test correctly failed on this existing path. | Open; replace those two display paths with existing generic localized messages, without changing backend validation. | `npm run qa:idts94:programmatic` -> exit 1 at `assignment UI hides caught backend internals`. | Apply the surgical safe-message fix, rerun focused test, and retain backend errors only for server-side diagnostics. |
| 2026-07-24 | IDTS-94 compile/UI5 verification | Replaced Smart Assign raw-error display with localized generic messages; ran focused/regression tests, CAP compile, UI5 build, syntax checks, and UI5 MCP linter. | Core implementation is green at static, backend behavior, compile, build, and UI5 lint levels. | **Existing model warning:** CAP compile still reports `NonUpdateableProperties` is not known on the attachment `@Capabilities.UpdateRestrictions` annotation in `db/schema.cds:144`; this is unrelated to IDTS-94 and was not changed. | IDTS-94 security defect fixed. Existing attachment annotation warning remains owned by the attachment/model baseline. | IDTS-94 static `32` checks pass; IDTS-68 `31/0`; IDTS-69 `8/0`; IDTS-91 `19/0`; IDTS-92 `47` checks; CAP compile exit 0 with the known attachment warning; UI5 build exit 0; Node syntax exit 0; UI5 MCP linter `[]`. | Update required knowledge mirrors/PM handover and run browser evidence for both dialogs. |
| 2026-07-24 | IDTS-94 knowledge-mirror discovery | Searched existing mirrors for every changed CAP/UI/i18n artifact and their ownership/debug links. | Relevant per-file mirrors were found under `docs/knowledge/srv/ai/` and `docs/knowledge/app/bug-management-ui/webapp/`. | **Documentation/tooling issue:** the discovery command also targeted assumed top-level files `docs/knowledge/README.md` and `docs/knowledge/ownership-map.md`, but neither path exists, so `rg` exited 1 after still returning useful matches. | Fixed by using `rg --files docs/knowledge` and the existing per-file mirrors instead of assuming a top-level ownership index. | Failed command reported `The system cannot find the file specified` for the two assumed paths; per-file matches were returned successfully. | Read and surgically update only the mirrors matching changed source files. |
| 2026-07-24 | IDTS-94 browser evidence startup | Started the local CAP/Fiori watcher as a hidden background process and polled service readiness. | Server is confirmed listening on port 4004 and is ready for authenticated browser QA. | **Transient environment issue:** the first 15-second poll ended before the watcher finished startup. **Probe issue:** a later unauthenticated metadata request returned the expected HTTP 401 because BugService is protected; PowerShell surfaced it as a WebException even though it proves the server is reachable. | Resolved; process inspection confirmed watcher PID `8580` owns port 4004. Browser harness injects a valid local test session and can proceed. | Start PID `2480`; watcher PID `8580`; `Get-NetTCPConnection` shows `::`:4004 listening; unauthenticated `$metadata` -> expected 401. | Run the authenticated Handoff and Smart Assign Playwright suites. |
| 2026-07-24 | IDTS-94 first Handoff browser run | Started the authenticated IDTS-76 Handoff browser suite against the isolated worktree server. | Browser suite reached fixture setup. | **Environment/data blocker:** the isolated worktree had no deployed local SQLite schema, so fixture insertion failed with `no such table: idts_cap_AuthSessions`. This is not an IDTS-94 product failure. | Open; stop the exact local watcher process tree, deploy the current model to the isolated `db.sqlite`, restart, and rerun. | `npm run qa:idts76:browser` -> exit 1 before browser launch at AuthSessions INSERT. | Deploy isolated development SQLite and rerun both browser suites. |
| 2026-07-24 | Ownership correction for NhanT tasks | Audited Jira ownership after the user clarified that SangVN must not implement NhanT-owned work. IDTS-96, IDTS-98, and IDTS-99 were confirmed as NhanT-owned; their SangVN support branches had no PR and clean worktrees. Remote/local branches and isolated worktrees were removed, IDTS-96/98 were returned to To Do, and corrective Jira comments were added. | Ownership/status correction and full SangVN withdrawal completed. | **Tooling issue:** the first local `git worktree remove` for IDTS-96 removed Git metadata/tracked files but could not delete the non-empty directory because ignored dependency files remained; IDTS-98 produced the same cleanup symptom. | Fixed. The registered clean worktrees were force-removed, then only the two exact residual directories—validated under `C:\Users\Admin\Desktop\DOAN_SAP`—were moved to the Windows Recycle Bin. No NhanT branch or active worktree remains. | `git status --porcelain` was empty for all three worktrees; `gh pr list` returned `[]`; remote/local branch lookup is empty; all three worktree paths report `exists=False`; Jira comments `10659`, `10660`, `10661`; IDTS-96/98 To Do and IDTS-99 remains To Do. | Continue only SangVN-owned IDTS-94/95/97. NhanT retains full ownership of IDTS-96/98/99 and may recover old commits from the SHA recorded in Jira if desired. |
| 2026-07-24 | IDTS-94 responsive browser review | Re-ran both contextual browser flows into the dedicated IDTS-94 evidence folder and visually inspected desktop/mobile screenshots. | Persistence, reviewer/time, repeat blocking, no-mutation, unavailable-ID, and both mobile layouts passed. | **Product/UI defect:** at the 390x844 viewport the Handoff Summary header row and full-width VBox plus margins caused provider/confidence and body text to overflow and clip on the right side of the dialog. | Fixed. The supported UI5 dialog now sets `stretch: Device.system.phone`; metadata is vertically stacked, the long provider/confidence copy uses wrapping `Text`, and the margin container no longer forces `100%` width. A true mobile-emulation context rerun shows wrapped content inside the dialog. | Handoff browser `7 PASS`; Smart Assign browser `7 PASS`; visually inspected `docs/pm/evidence/idts-94/handoff/idts94_handoff_summary_mobile.png` and `.../smart-assign/01c_smart_assign_explanation_mobile.png`. | Run final UI5 lint/build and full focused verification; include this fixed-in-session defect in Jira evidence. |
| 2026-07-24 | IDTS-94 UI5 responsive API lint | Ran UI5 MCP linter after the phone-layout fix. | Responsive API correction and regression verification completed. | **Product/compatibility defect:** UI5 linter initially flagged Dialog `stretchOnPhone` as deprecated (`no-deprecated-api`, line 203) and directed use of `stretch` with `Device.system.phone`. | Fixed by importing `sap/ui/Device` and setting the supported `stretch: Device.system.phone`; the linter is now clean and the Handoff browser still passes. | First UI5 MCP lint: one severity-2 finding; final `run_ui5_linter`: `results: []`; final Handoff browser `7 PASS`. | Continue final build/regression verification. |
| 2026-07-24 | IDTS-94 final Smart Assign regression | Ran the existing IDTS-56 programmatic suite after IDTS-94 browser and review-action suites passed. | Runtime/browser behavior and the legacy programmatic regression are green. | **Test-harness issue:** IDTS-56's generic control double lacked `.addStyleClass()` for the new wrapped review-button row; after that fix, its positional SearchField lookup also exposed a brittle fixed-index assumption because IDTS-94 inserted contextual controls before the search field. | Fixed only in the test harness: added the normal fluent mock method, identified `sap/m/SearchField` by control type instead of child index, and reran the complete suite. Production behavior was unchanged. | Initial runs: `7 PASS / 1 FAIL`, then `9 PASS / 1 FAIL`; final `npm run qa:idts56:programmatic` -> `13 PASS / 0 FAIL`. Browser Smart Assign remained `7 PASS`. | Continue remaining compile/build/security/diff checks. |
| 2026-07-24 | IDTS-94 final CAP/UI5 build | Re-ran CAP compile and UI5 build as part of the final verification chain. | CAP compile and the application-root UI5 build completed successfully. | **Environment/configuration issue:** the first root-level `npx ui5 build --config ui5.yaml --clean-dest` invocation failed because UI5 CLI treated the repository root package as a UI5 project and reported its `package.json` has no `name`. | Fixed by following the documented repository command from the actual UI5 project directory `app/bug-management-ui`; no package metadata was changed. | CAP compile exit 0 with the existing attachment warning; `npx ui5 build --config ui5.yaml --clean-dest` from `app/bug-management-ui` -> build succeeded in 1.14s. | Continue syntax/security/diff checks. |
| 2026-07-24 | IDTS-94 implementation handoff | Completed the contextual Handoff Summary and Smart Assign explanation review controls, audit-ID response plumbing, safe copy, responsive Handoff layout, focused/browser evidence, knowledge mirrors, and PM handover updates. Applied `ponytail-review` and removed two dead provider-state lines. | Local implementation and evidence are complete; no PR merge or Jira Done transition was performed. | All product/test/tool issues found in this session are recorded above. Remaining non-IDTS-94 items are the existing attachment annotation warning, Node engine mismatch, locked dependency advisories, OfficeCLI absence, and Jira due-date discrepancy. | IDTS-94 defects found in-session are fixed. External/baseline limitations remain documented and were not broadened into unrelated changes. | IDTS-94 `32`; IDTS-68 `31/0`; IDTS-69 `8/0`; IDTS-91 `19/0`; IDTS-92 `47`; IDTS-56 `13/0`; Handoff browser `7/7`; Smart Assign browser `7/7`; UI5 MCP lint `[]`; CAP compile exit 0; app-root UI5 build PASS; syntax, secret scan, agent rules, and `git diff --check` PASS. Evidence: `docs/pm/evidence/idts-94/`. | Commit/push a stacked PR against `feature/idts-91-93-implementation-datdt`, comment Jira with evidence, then wait for PR #167, human review, and the deferred Knowledge Gate before merge/Jira Done. |
| 2026-07-24 | IDTS-94 commit preflight | Ran the staged diff whitespace gate immediately before commit. | No commit was created in the failed attempt. | **Documentation formatting issue:** `git diff --cached --check` found trailing whitespace on the two metadata lines in `docs/pm/evidence/idts-94/README.md`; the spaces were Markdown hard-break formatting, not content corruption. | Fixed by replacing the hard-break spaces with blank lines; restage and rerun the gate before commit. | First staged check stopped at README lines 3-4; commit command was not executed. | Restage the two corrected documentation files and rerun staged diff/ownership checks. |
| 2026-07-24 | IDTS-94 Knowledge Gate OfficeCLI setup | Identified the repository-compatible OfficeCLI as the open-source `iOfficeAI/OfficeCLI` release rather than the unrelated npm package with the same name; downloaded the official Windows x64 `v1.0.141` binary and checksum asset, verified SHA-256, installed it under `%LOCALAPPDATA%\OfficeCLI`, and added that directory to user PATH. | OfficeCLI `1.0.141` is installed, resolves from the configured directory, and exposes the repository-required Office document commands. | **Tooling issues:** the first combined install command was rejected by shell safety policy because it included recursive deletion of a computed temporary path; the second attempt used unsupported `New-Item -LiteralPath`; the successful install initially returned exit 1 only because `Select-Object -First 25` closed the help-output pipe early. No checksum or installation step failed in the successful attempt. | Fixed. Re-ran `officecli help` without output truncation; it exited 0. | SHA-256 `65d119912147b47d102224715df2288813a2fea56520bfc4313b2fa0bf4672c7`; `officecli --version` -> `1.0.141`; `officecli help` -> exit 0; user PATH contains `C:\Users\Admin\AppData\Local\OfficeCLI`. | Resume the fixed three-question IDTS-94 AI Knowledge Gate. |
| 2026-07-24 | IDTS-94 Ownership Knowledge Gate | Ran the deterministic three-question AI-flow gate, taught the persisted-review fundamentals from the actual CAP/Fiori code, assessed SangVN's trace, negative case, teach-back, and controlled debug retest, then recorded safe bilingual evidence. | PASS: 3/3 (100%); critical answers, debug retest, and teach-back all PASS. | **Learning/debug observation:** the first controlled debug attempt read `ACCEPTED !== PENDING` incorrectly; mentor mode substituted concrete values and the equivalent `REJECTED` retest was correct. **Tooling issue:** the first evidence discovery filter used a slash-sensitive pattern that missed Windows backslash paths and returned exit 1 despite the example file existing. | Learning gap fixed through an equivalent retest. Discovery issue fixed by using a separator-independent `knowledge|learning` filename filter. | `npm run learning:gate -- sangvn ai 2026-07-24 -` -> 3 deterministic questions; evidence `docs/pm/evidence/idts-94/knowledge-gate-sangvn-2026-07-24.md`; OfficeCLI `1.0.141` preflight PASS with Markdown limitation recorded. | Update Draft PR #168 and Jira IDTS-94 with PASS evidence. Keep PR Draft/Jira In Progress until PR #167 dependency and human review are complete. |
| 2026-07-24 | IDTS-94 Knowledge Gate PR sync | Updated Draft PR #168 from the deferred Knowledge Gate to the assessed 3/3 PASS result and linked the repository evidence. | PR body is verified with PASS score, critical/debug/teach-back results, checked checklist, evidence path, and no remaining deferred marker; PR remains OPEN/DRAFT/CLEAN. | **Tooling issues:** the first composed `gh pr view --jq` object expression was split into multiple arguments by Windows parsing. A multiline replacement then missed GitHub's newline representation and left the old deferred block while changing single lines. | Fixed by rebuilding the complete PR body with the PASS block and verifying every required marker plus absence of `DEFERRED`. | PR #168 markers all `True`; `DeferredRemaining=False`; state `OPEN`, `isDraft=true`, `mergeStateStatus=CLEAN`. | Add the Jira PASS comment, retain In Progress/Draft until PR #167 dependency and human review are complete. |
| 2026-07-24 | IDTS-94 Knowledge Gate Jira sync | Added the required Jira comment containing the exact `Ownership Knowledge Gate: PASS` marker, score, critical/debug/teach-back results, PR link, evidence path, progress entry, and evidence commit. | Jira Knowledge Gate evidence is complete. | No new product or tooling issue observed. | Complete. | Jira comment `10665`; evidence commit `2cbd1eb`; Draft PR #168. | Keep IDTS-94 In Progress and PR #168 Draft until PR #167 dependency, human review, merge, and closure are complete. |
| 2026-07-24 | IDTS-94 dependency recheck | Rechecked PR #167 and Draft PR #168 after the Knowledge Gate sync, then used a read-only merge-tree comparison against current `origin/dev`. | PR #168 remains OPEN/DRAFT/CLEAN with PASS markers and no deferred marker. PR #167's `qa-depth-gate` remains SUCCESS. | **Dependency blocker:** PR #167 now reports `mergeable=CONFLICTING` / `mergeStateStatus=DIRTY` against `dev`, while the earlier handoff check reported CLEAN. Conflict scope is limited to shared PM files `docs/pm/current-status.md` and `docs/pm/task-board.md`; no runtime CAP/Fiori conflict was found. | Diagnosed and handed off; do not modify DatDT's branch from SangVN's task. | Merge base `a9fa7981`; current `origin/dev` `c953cd7`; PR #167 QA SUCCESS and conflict metadata; merge-tree shows two `changed in both` PM files; Jira IDTS-94 dependency comment `10666`. | Foundation owner resolves the two PM conflicts. Keep IDTS-94 Draft/In Progress until PR #167 becomes mergeable and human review completes. |
| 2026-07-24 | IDTS-94 foundation resync | Confirmed from Jira and GitHub that DatDT resolved PR #167, merged its latest base into the SangVN IDTS-94 branch, and retained the newer Sprint 5 status plus the SangVN handoff row. | Foundation dependency is synchronized locally at merge commit `f11f26b`; IDTS-94, IDTS-91, and IDTS-92 focused gates plus secret scan and agent-rules check pass. | **Tooling issue:** the first final compile/build command ran from `app/bug-management-ui`, so the repo-relative Verify skill path and `srv` compile target were not found. This was a command working-directory mistake, not a product defect. | Fixed by recording the issue and rerunning repository checks from their correct working directories. | PR #167 is OPEN/READY/CLEAN/MERGEABLE with green `qa-depth-gate`; local focused checks: IDTS-94 `32`, IDTS-91 `19/0`, IDTS-92 `47`, secret scan PASS, agent rules PASS. | Rerun CAP compile from repository root, UI5 build from the app directory, push, and mark PR #168 ready for human review if all checks pass. |
| 2026-07-24 | IDTS-94 post-merge CAP verification | Ran the repository-root CAP compile after loading the Verify skill and passing OfficeCLI preflight. | OfficeCLI preflight passed at version `1.0.141`. | **Tooling/command issue:** `npx cds compile srv --to edmx` returned exit 1 because the model now exposes both `AuthService` and `BugService`, so the compiler requires an explicit service selector. The first invocation also redirected output and hid that diagnostic. | Diagnosed; use the documented multi-service form `npx cds compile srv --to edmx -s all`. No product/model change is required. | OfficeCLI `1.0.141`; compiler diagnostic: `Found multiple service definitions` with suggested `-s all`. | Run the corrected compile, UI5 build, final diff checks, then push and mark PR #168 ready if green. |
| 2026-07-24 | IDTS-94 PR readiness sync | Replaced the stale conflict note in PR #168 through stdin and marked the existing PR ready for human review. | PR #168 is OPEN, non-Draft, CLEAN, and MERGEABLE; no duplicate PR was created. The PASS Knowledge Gate marker remains present. | **Tooling issues:** the first `--body` invocation parsed the multiline body as flags and made no change. The stdin retry updated the body and draft state, but its final inline `--jq` expression lost string quotes under Windows parsing and exited 1 after the mutations had succeeded. | Fixed. Used `--body-file -` for mutation and PowerShell `ConvertFrom-Json` for final verification. | PR #167: OPEN/non-Draft/CLEAN/MERGEABLE, `qa-depth-gate` SUCCESS at `608e41b`; PR #168: OPEN/non-Draft/CLEAN/MERGEABLE at `224a10f`; `ReadyText=True`, `StaleConflictText=False`, `GatePass=True`. | Commit/push the status log and add the final dependency-cleared/readiness evidence to Jira IDTS-94. |
| 2026-07-25 | IDTS-94 integration test after retargeting to dev | Retargeted PR #168 to `dev`, merged the final PR #167 baseline, and reran the focused suite. | The CDS contract still contains `suggestionID` in both response types. | **Test-harness issue:** the static check required a literal LF sequence, while the merged Windows checkout used CRLF, causing a false failure before behavior tests ran. | Fixed in the test only by normalizing CRLF to LF when source files are read; runtime CAP/Fiori code was not changed for this finding. | Initial `npm run qa:idts94:programmatic` failed at the first exact-string assertion even though `srv/service.cds` contains the expected field. The complete suite must be rerun after this harness correction. | Continue focused and browser regression; merge only after fresh PR gate PASS. |
| 2026-07-25 | IDTS-94 isolated browser integration setup | Ran the Smart Assign browser regression after all focused IDTS-94/56/68/69/76 checks passed. | Programmatic integration remained green. | **Environment/data blocker:** the fresh worktree's ignored local SQLite file had no `idts_cap_DeveloperProfiles` table, so fixture setup failed before browser behavior was exercised. | Use the documented local-only `cds deploy --to sqlite` in this isolated worktree, then rerun both browser suites. This does not touch Shared QA or committed seed files. | `npm run qa:idts56:browser` stopped at fixture SELECT with `no such table: idts_cap_DeveloperProfiles`; no product assertion failed. | Deploy the isolated local schema and rerun Smart Assign plus Handoff browser checks. |
| 2026-07-25 | IDTS-94 isolated browser server setup | Deployed the local SQLite schema and retried the Smart Assign browser suite. | Fixture schema is now present. | **Environment blocker:** the browser script expects the CAP/UI5 server at localhost:4004, but no watcher was running, so navigation failed with `ERR_CONNECTION_REFUSED`. | Start the isolated worktree CAP watcher hidden, verify port/health, then rerun. This is not a UI regression. | `npx cds deploy --to sqlite` exit 0; browser failed only at initial `page.goto` before UI assertions. | Start and verify the isolated server, rerun both browser suites, then stop the exact process tree. |
| 2026-07-25 | IDTS-94 browser target isolation | Started the local CAP/UI5 watcher and reran browser regressions. | Smart Assign browser passed all seven scenarios against localhost. | **Environment/configuration issue:** a machine-level `IDTS_QA_BASE_URL` still pointed to Shared QA, so the Handoff fixture guard correctly refused to modify a non-local target. | Override `IDTS_QA_BASE_URL=http://localhost:4004` for the isolated test process only, then rerun Handoff. No Render data or environment setting is changed. | Smart Assign `RESULT: PASS`; Handoff stopped before fixture creation with the explicit localhost safety message. | Rerun Handoff with the local process override and retain the guard. |

## 2026-07-25 - IDTS-95 integration handoff

| Date | Task/WP | Work done | Completed part | Blockers/Bugs found | Fix status | Evidence/command | Next handoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-25 | IDTS-95 duplicate confirmation integration | DonHV integrated the shared IDTS-95 branch with the merged IDTS-91–94 baseline while preserving the latest Sprint 5 PM state and the IDTS-95 runtime/evidence scope. | Dependency integration and conflict curation are in progress. | **Process blocker:** the dedicated SangVN duplicate-confirmation Knowledge Gate is not yet recorded as PASS. This is distinct from the IDTS-94 review-control gate and cannot be inferred or copied. | Runtime/test preparation may continue, but PR merge and Jira Done remain blocked until SangVN completes the gate. | Branch `feature/idts-95-confirm-duplicate-suggestion-sangvn`; baseline `origin/dev` after PR #168. | Run fresh focused/regression checks, prepare the PR, and keep it Draft/blocked if the dedicated gate remains absent. |

Vietnamese:

| Ngay | Task/WP | Da lam gi | Phan da xong | Kho khan/Bug phat hien | Trang thai fix | Bang chung/Lenh da chay | Handoff tiep theo |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-25 | IDTS-95 integration xac nhan duplicate | DonHV tich hop branch IDTS-95 dung chung voi baseline IDTS-91–94 da merge, dong thoi giu PM state Sprint 5 moi nhat va dung scope runtime/evidence cua IDTS-95. | Dang hoan tat dependency integration va curate conflict. | **Process blocker:** chua co ban ghi PASS cho Knowledge Gate rieng cua SangVN ve duplicate confirmation. Gate nay khac gate review-control cua IDTS-94 va khong duoc suy dien hay sao chep. | Co the tiep tuc chuan bi runtime/test, nhung PR khong duoc merge va Jira khong duoc Done truoc khi SangVN hoan thanh gate. | Branch `feature/idts-95-confirm-duplicate-suggestion-sangvn`; baseline `origin/dev` sau PR #168. | Chay lai focused/regression, chuan bi PR va giu Draft/blocked neu gate rieng van thieu. |
