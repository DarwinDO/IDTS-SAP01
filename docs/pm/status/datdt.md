# DatDT Status - Fiori/UI5 Primary

Last updated: 2026-07-05

Vietnamese: Trạng thái của DatDT - phụ trách chính Fiori/UI5.

## Member Identity

| Field | Value |
| --- | --- |
| Member | DatDT |
| Primary lane | Fiori/UI5 |
| Shared delivery responsibility | May receive Backend CAP, Fiori/UI5, or QA/Verification tasks as assigned, but Fiori/UI5 is the primary focus |
| Leader support | DonHV can support or unblock this lane when needed |

Vietnamese:

| Trường | Giá trị |
| --- | --- |
| Thành viên | DatDT |
| Mảng chính | Fiori/UI5 |
| Trách nhiệm delivery chung | Có thể nhận task Backend CAP, Fiori/UI5 hoặc QA/Verification khi được phân công, nhưng Fiori/UI5 là trọng tâm chính |
| Leader hỗ trợ | DonHV có thể hỗ trợ hoặc gỡ blocker cho mảng này khi cần |

## Current Focus

Sprint 03 FE Auth: IDTS-35 Login UI and authenticated app session is now implemented on branch `feature/idts-35-login-ui-datdt`. Login dialog, XHR interceptor, session token storage, and logout are in place. CDS compile and UI5 build both pass. Browser UAT is the remaining step.

Vietnamese: Sprint 03 Fiori/UI5: IDTS-13, IDTS-26 và IDTS-33 đã hoàn thành ở mức bàn giao của DatDT. DatDT đã hoàn thành manual browser UAT cho IDTS-33 và triage lỗi FE cho IDTS-26 trên `dev`; sau đó DonHV review bằng chứng và phát hiện thêm hai lỗi FE, đã fix qua PR #27. Vì vậy file status này ghi lại correction đó thay vì xem chu kỳ này là hoàn toàn không có issue.

## Done

- Fiori UX requirements are documented in `docs/ba/07-fiori-ux-requirements.md`.
- Main app path is identified as `app/bug-management-ui`.
- Core Sprint 02 Fiori work already exists in the repo: Bug Detail layout refinements, dynamic create-mode hiding, assignee value help, and happy-flow support for comments/attachments.

Vietnamese:

- Yêu cầu Fiori UX đã được ghi trong `docs/ba/07-fiori-ux-requirements.md`.
- App chính nằm tại `app/bug-management-ui`.
- Các phần Fiori cốt lõi của Sprint 02 đã có trong repo: tinh chỉnh layout Bug Detail, ẩn/hiện động ở create mode, value help cho assignee, và happy-flow cho comment/attachment.

## In Progress

- IDTS-35 Login UI: implemented on branch `feature/idts-35-login-ui-datdt`. Files created:
  - `webapp/ext/login/LoginDialog.fragment.xml` – sap.m.Dialog with email/password inputs and MessageStrip for errors
  - `webapp/ext/login/LoginController.js` – fetch-based login POST, sessionStorage token management, static helpers
  - `webapp/ext/login/login.css` – minimal dialog CSS
  - `webapp/Component.js` – overridden with auth gate + XHR interceptor
  - `webapp/manifest.json` – CSS reference added
  - `webapp/i18n/i18n.properties` – login keys added
- CDS compile pass; UI5 build pass; regression 45 PASS / 0 FAIL
- Browser UAT (manual) is the remaining verification step before PR.

Vietnamese:

- IDTS-35 Login UI: đã implement trên branch `feature/idts-35-login-ui-datdt`. Các file mới: LoginDialog fragment, LoginController, login.css, Component.js override, manifest update, i18n keys.
- CDS compile, UI5 build, regression 45 PASS / 0 FAIL đều pass.
- Bước còn lại: browser UAT thủ công trước khi tạo PR.

## Next

- Keep the latest PR #27 FE fixes in the final browser rerun: History Timeline must not throw a UI5 expression-binding error, and the PM rejected-follow-up label must render as readable text instead of escaped characters.
- Keep the Assign Developer dialog as a regression check during the final mentor-demo rerun, but no separate FE fix stream is currently required for `IDTS-9`.
- Support final Object Page polish review and any non-blocking usability cleanup that appears during the final browser rerun.

Vietnamese:

- Giữ hai fix FE mới nhất từ PR #27 trong lần browser rerun cuối: History Timeline không được nảy lỗi UI5 expression binding, và label rejected-follow-up của PM phải hiển thị chữ dễ đọc thay vì escaped characters.
- Giữ dialog Assign Developer như một regression check trong lần mentor-demo rerun cuối, nhưng hiện không cần mở stream fix FE riêng cho `IDTS-9`.
- Hỗ trợ final Object Page polish review và các cleanup usability không block nếu phát sinh trong browser rerun cuối.

## Blockers

- None at execution level. The remaining FE risk is regression coverage: future UAT must keep checking the PR #27 History Timeline and PM label fixes because they were found by follow-up evidence review, not by the original DatDT UAT cycle.

Vietnamese: Hiện không có blocker về execution. Risk FE còn lại là coverage regression: các lần UAT sau phải tiếp tục kiểm tra hai fix từ PR #27 cho History Timeline và label PM, vì hai lỗi này được phát hiện ở bước follow-up evidence review, không phải trong chu kỳ UAT ban đầu của DatDT.

## Session Log

| Date | Task/WP | What was done | Completed part | Issues/Bugs found | Fix status | Evidence/Commands | Next handoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-05 | IDTS-58 Sprint 4 UI defects | Created branch `fix/idts-58-sprint-4-ui-defects-datdt`. Fixed FE/UI defects across login/profile/dashboard/comments/attachments: standalone auth pages now bootstrap UI5 through the app-relative `/resources` path, the dashboard profile action now lives in the `sap.m.Page` header instead of overlapping `Refresh`, comment feed no longer repeats the author name, and attachment delete is disabled while a draft exists. Retested the affected flows on the local CAP/UI5 runtime with browser smoke. | FE fixes are complete on the DatDT branch and local browser smoke passed for login, dashboard, comment post, evidence upload, and draft-state action disable. | **Product defects fixed:** dashboard profile overlap with `Refresh`; duplicate author identity in comment feed; attachment delete still enabled while `HasDraftEntity = true`. **Environment/tooling issues fixed locally:** local runtime first failed with `Cannot find module 'nodemailer'`; attachment upload then failed because stale `db.sqlite` missed `NotificationDeliveries`, causing `draftEdit` 500 until local redeploy refreshed the schema. | Product defects fixed on this branch. Local environment blockers were resolved for this session; no extra source change was needed for the stale SQLite schema. | `npm.cmd run render:db:deploy` -> deployed `db.sqlite`; `npm.cmd run dev:auth:set-password` for DonHV/DatDT/NhanT -> pass; `npx.cmd cds serve all --port 4006` -> local app served; browser smoke on `http://127.0.0.1:4006/idts.bugmanagementui/...` -> dashboard overlap area `0`, comment post PASS, no `DonHV - Project Manager` duplicate pattern, upload toast `Evidence uploaded.`, delete button class `sapMBtnDisabled` after entering draft edit. | Push branch, add Jira evidence comment on `IDTS-58`, then hand off PR/review to DonHV. |
| 2026-06-30 | IDTS-35 blank-app fix | Diagnosed and fixed blank app after login. Root cause: XHR interceptor placed in `Component.js` ran AFTER UI5's OData V4 model sent the `$metadata` request without a Bearer token → 401 → app rendered blank. Also fixed the JavaScript `arguments` closure bug (async `.then()` callback received wrong `init()` args). Redesigned approach: moved auth guard + XHR interceptor to a synchronous `<script>` block in `index.html` that runs BEFORE the UI5 bootstrap is even fetched. Added standalone `login.html` page. Reverted `Component.js` to clean passthrough. Commits: `e35a752` (initial impl), `3a87fb0` (blank-app fix). Pushed both to `origin/feature/idts-35-login-ui-datdt`. | Auth guard working: unauthenticated → redirect to `login.html`; after login → redirect to `index.html`; every OData request carries Bearer token. UI5 build pass; regression 45 PASS / 0 FAIL. | **Product defect (IDTS-35 implementation):** blank page after login caused by XHR interceptor timing — OData $metadata fired before interceptor was installed. Also: JS `arguments` closure bug in `.then()` callback. Both fixed in commit `3a87fb0`. **Environment issue:** `passwordHash` column missing in stale `db.sqlite` → fixed by `npx cds deploy --to sqlite` (schema redeploy, seed data reset). | Fixed in commit `3a87fb0`. | `npx ui5 build --config ui5.yaml` → `Build succeeded in 399ms`; `node scripts/qa/test-idts23-regression.js` → 45 PASS / 0 FAIL; `git push` → `e35a752..3a87fb0` pushed successfully. | Manual browser UAT to confirm full flow: login.html → index.html → bug list visible. Then create PR to `dev`. |
| 2026-06-30 | IDTS-35 Login UI (initial) | Created branch `feature/idts-35-login-ui-datdt`. Implemented Login dialog fragment (`LoginDialog.fragment.xml`), login controller (`LoginController.js`), login CSS, overrode `Component.js` with auth gate and XHR interceptor, updated `manifest.json` CSS ref, added i18n keys. Created knowledge mirror at `docs/knowledge/app/bug-management-ui/Component.js.md`. | Login dialog, token storage, OData auth injection, logout, and i18n complete. CDS compile pass; UI5 build pass (`1.03s`); regression 45 PASS / 0 FAIL. | No product defect found during implementation. Known: CDS compile requires `-s all` flag now that `AuthService` exists alongside `BugService`. Known attachment `NonUpdateableProperties` warning is pre-existing. | Not applicable — no new defect. | `npx cds compile srv app/bug-management-ui --to edmx -s all` → exit 0; `npx ui5 build --config ui5.yaml` → `Build succeeded in 1.03s`; regression 45 PASS / 0 FAIL. | Browser UAT then PR. |
| 2026-06-28 | PR #26 status correction tooling note | DonHV corrected DatDT's PR #26 status wording after merging latest `origin/dev` into the PR branch. | Status correction prepared for commit. | **Tooling issue:** PowerShell rejected a combined `git add ... && git commit ...` command because this shell version does not support `&&` as a statement separator. No source file was changed by the failed command. | Fixed by rerunning Git staging and commit as separate PowerShell commands. | Failed command output: `The token '&&' is not a valid statement separator in this version.` | Continue PR #26 push and merge. |
| 2026-06-28 | IDTS-26 & IDTS-33 Browser UAT | Ran `npm run dev:sqlite:refresh-views` and launched `cds run`. Executed IDTS-33 manual browser UAT using AI subagent for List Report tabs, Bug Create flow, Assignment, and Object Page refresh. Checked IDTS-26 for required FE fixes, then DonHV reconciled the status after PR #27 evidence review. | DatDT UAT handoff completed; status corrected after follow-up review. | DatDT's own UAT cycle did not report a new issue and the IDTS-13 stale Object Page issue did not reproduce after lifecycle action submit. Follow-up evidence review later found two FE product defects: History Timeline crashed when text existed because the XML expression binding used `${...}` inside a boolean expression, and the PM rejected-follow-up label rendered escaped `\u2014` instead of readable text. | Follow-up defects fixed by DonHV in PR #27 and merged into `dev`; this PR #26 status update is corrected to reference that outcome. | `npm run dev:sqlite:refresh-views`; `cds run`; DatDT subagent screenshots; PR #27 verification reran IDTS-24 UAT, IDTS-23 regression, CAP compile, UI5 build, AI DevKit lint, and `git diff --check`. | PR #26 can merge as a corrected DatDT status update; future browser UAT must include the PR #27 History Timeline and PM label checks. |
| 2026-06-28 | IDTS-13 / PR #20 DonHV merge review | DonHV reviewed PR #20 after PR #18 entered `dev`, merged latest `origin/dev` into DatDT's branch, and confirmed the PR scope is now limited to the intended IDTS-13 side-effect fix plus its knowledge/status updates. The merge also preserved the latest PM docs, auth/email task plan, and SangVN HistoryTimeline files so PR #20 no longer risks reverting newer `dev` work. | Branch alignment and verification completed. | **Process/branch-sync issue:** before merging latest `dev`, the PR branch was Git-mergeable but stale and appeared to delete/lower newer PM docs and HistoryTimeline files when compared with latest `dev`. **Tooling issue:** one review-only `rg` scan against the generated EDMX was written with an invalid PowerShell/regex argument and printed a regex parse error; it did not affect source files or product verification. | Branch-sync issue fixed by merging `origin/dev`; tooling scan issue fixed by relying on compile/build/regression evidence instead of the malformed scan. | Fiori MCP `search_docs` confirmed OData V4 side effects can use `TargetEntities`; `git diff --check origin/dev HEAD` pass; `npx cds compile srv app/bug-management-ui --to edmx` pass with known attachment warning; UI5 manifest validation pass; `npx ui5 build --config ui5.yaml` pass; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `node scripts/qa/test-idts23-regression.js` -> 45 PASS / 0 FAIL. | Push branch update, merge PR #20 into `dev`, then update Jira IDTS-13. |
| 2026-06-27 | IDTS-13 FE Object Page stale state | Fixed bound action side effects by replacing flat TargetProperties with TargetEntities: [in] for all actions in actions.cds. | Fixed actions.cds and confirmed CDS compilation without errors. | **Product defect:** Fiori Elements V4 failed to refresh bound entity completely due to incorrect TargetProperties side effect binding. | Fixed in this session on DatDT branch. | `npx cds compile srv app/bug-management-ui --to edmx` pass. Jira IDTS-13 updated. | Ready for PR review and merge by DonHV. |
| 2026-06-27 | IDTS-13 and IDTS-33 Jira coordination | DonHV checked DatDT's Jira tasks. IDTS-13 has a new DatDT comment saying the stale Object Page refresh fix is on branch `fix/idts-13-stale-object-page-datdt` and is ready for PR review. DonHV also created IDTS-33 as the DatDT companion manual UAT task split from IDTS-32. | Jira state reviewed; IDTS-33 created and assigned to DatDT; IDTS-32/IDTS-33 linked as related tasks. | No product bug found by DonHV in this planning step. Process change: the broad manual UAT scope was split so DatDT owns FE-shell-heavy testing and SangVN owns workflow/history-heavy testing. | Not applicable. | Jira search for DatDT tasks succeeded; IDTS-33 created; IDTS-32 edited; comments added to IDTS-32 and IDTS-33; issue link created. | DatDT to provide PR for IDTS-13 branch and later execute IDTS-33 manual browser UAT evidence on latest `dev`. |
| 2026-06-24 | IDTS-22 PR #16 conflict and FE semantics fix by DonHV | DonHV merged latest `dev` after PR #17, resolved the PR #16 conflict by keeping the verified `dev` PM-monitoring HTTP regression and SQLite view refresh utility, then corrected `pm-monitoring.cds` so the four business tabs use the backend monitoring flags again. The misleading `My Action Items` tab was changed to `PM Action Queue` and now filters `nextProcessorRole_code = 'PM'` instead of showing all open bugs. | Conflict resolution and FE annotation correction completed. Knowledge mirror was rewritten to explain the backend-view dependency, the PM queue decision, and cross-folder links. | **Process/merge issue:** PR #16 conflicted with `dev` in `package.json` and `scripts/qa/test-pm-monitoring-http.js` because PR #17 added the canonical backend HTTP regression and SQLite view refresh tooling first. **Functional issue:** the previous DatDT fix made `Overdue` mean only `status_code != CLOSED`, which did not match the business meaning of overdue. | Fixed in this session on DatDT branch. Browser UAT still requires running `npm run dev:sqlite:refresh-views` before opening local persistent SQLite data. | `npm run dev:sqlite:refresh-views` refreshed 34 views; `npx cds compile srv app/bug-management-ui --to edmx` pass with known attachment metadata warning; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `npm run qa:pm-monitoring:http` on port 4027 -> 8 PASS / 0 FAIL; PM queue HTTP filter check pass; UI5 build from app folder pass; AI DevKit lint pass; conflict-marker scan pass. | After PR #16 is merged, keep future PM monitoring tabs aligned with their real filter meaning; do not label a tab "My" unless a runtime-user filter is implemented. |
| 2026-06-24 | IDTS-22 PM Monitoring Blocker Fix | Fixed 4 blockers raised by DonHV in PR #16. 1) Replaced computed CDS flags (isOverdue, etc.) with persistent DB columns `status_code` and `dueDate` in `pm-monitoring.cds` SelectionVariants to fix SQLite runtime crashes. 2) Fixed My Action Items by applying `status_code ne 'CLOSED'`. 3) Created HTTP OData $filter regression test `test-pm-monitoring-http.js` to catch DB-level query failures. 4) Restored corrupted `actions.cds` syntax that broke the build. 5) Added Vietnamese translation to the knowledge mirror. | SelectionVariant definitions fixed; OData filter logic verified at HTTP level; new `qa:pm-monitoring:http` script added to `package.json`; knowledge mirror updated. | **Build defect (tooling issue):** `actions.cds` had invalid CDS single-quote syntax (`'in/comments'`) on working tree from a previous uncommitted change, causing `cds compile` to fail with "Mismatched '/'". | Fixed in session (restored `actions.cds`). All blockers from leader review are resolved. | `git restore app/bug-management-ui/annotations/actions.cds` exit 0; `npm run qa:pm-monitoring:http` -> 15 PASS / 0 FAIL; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `npx cds compile srv app/bug-management-ui --to edmx` pass. | Ready to comment on Jira and push force update for PR #16 re-review. |
| 2026-06-24 | IDTS-22 / PR #16 leader re-review | DonHV checked DatDT's Jira update, inspected rebased commit `74da851`, reran compile/build/regression/lint checks, and exercised the six List Report tabs through the Codex Browser plugin. | The branch is rebased on `dev` commit `feaf6cf`; GitHub reports PR #16 as `CLEAN/MERGEABLE`; static and existing automated checks pass. | **Product defect:** opening derived-flag tabs such as Overdue causes SQLite errors (`no such column: $B.isOverdue` / `$B.isPendingAssignment`) because the SelectionVariant filter reaches SQL using calculated projection aliases. **Functional defect:** `My Action Items` has an empty `SelectOptions` list and therefore shows all eight bugs instead of user-specific work. **Test-harness issue:** IDTS-23 regression reads the flags but does not issue OData `$filter` requests against them, so it misses the runtime failure. **Documentation issue:** the new knowledge mirror is English-only and does not meet the required equivalent English/Vietnamese beginner format. **Tooling issue:** one review-only PowerShell `rg` command initially failed because of an unterminated quoted string, and the first status patch failed because PowerShell output displayed UTF-8 text incorrectly; both review commands were corrected without affecting product verification. | Open. PR is Git-mergeable but not product-ready and must not be merged until the derived-filter backend handling, My Action Items behavior, documentation, and browser regression coverage are fixed and retested. | `npx cds compile srv app/bug-management-ui --to edmx` pass; `npx ui5 build --config ui5.yaml` pass; IDTS-23 regression `45 PASS / 0 FAIL`; AI DevKit lint `5 ok, 0 warn/miss`; `git diff --check origin/dev...HEAD` pass; Browser plugin reproduced the SQL errors and showed `My Action Items (8)`. | DatDT to fix PR #16 on the same branch, add filter-level regression coverage, update the knowledge mirror, rerun browser UAT, and report evidence on Jira. |
| 2026-06-24 | IDTS-22 rebase conflict fix | Rebased `feature/idts-22-pm-monitoring-fe-datdt` onto latest `dev` (commit `feaf6cf`) per DonHV Jira comment. Resolved conflict in `labels.cds`: kept IDTS-20 ownership labels (`Assignee (Technical Owner)`, `Current Action Owner`, `Action Owner Role`, `@Common.QuickInfo`) from dev; added `@UI.HiddenFilter : false` for 4 PM boolean flags from IDTS-22. Resolved conflict in `datdt.md`: merged both session log rows. | Conflict resolved; rebase complete; `labels.cds` and `datdt.md` merged cleanly. | **Merge conflict (process issue):** `labels.cds` and `datdt.md` both had rebase conflicts because IDTS-22 branch diverged from `dev` after IDTS-20 (PR #14) and IDTS-18 (PR #13) were merged. | Fixed in this session. | `git rebase origin/dev` resumed after manual conflict resolution; build verification pending. | Re-run `cds build`, `cds compile --to edmx`, browser UAT, then push force to update PR #16. |
| 2026-06-24 | IDTS-18 / PR #13 leader review | DonHV reviewed the rebased PR against the ownership header already merged through IDTS-20. The duplicate Ownership body section was removed, while clear Assignment and Rejected Follow-up labels were retained. The required knowledge mirror was updated. | PR scope now adds only non-duplicative ownership wording and action-owner role details. | **Fiori UX/process issue:** the PR added a `CollectionFacet` containing one `ReferenceFacet` and repeated the same ownership summary already visible in the Object Page header; source changes also lacked the required knowledge-mirror update. **Branch-sync/process issue:** the first regression command could not find `scripts/qa/test-idts23-regression.js` because the branch had been rebased before PR #11 entered `dev`, not against the current `dev`. | Fixed: duplicate UI removed; knowledge mirror updated; branch rebased again on current `dev`. No product runtime defect was found. | `git diff --check origin/dev...HEAD` exit 0; `npx cds compile srv app/bug-management-ui --to edmx` exit 0; `npx ui5 build --config ui5.yaml` exit 0; `node scripts/qa/test-idts23-regression.js` -> 45 PASS / 0 FAIL; `npx ai-devkit@latest lint --json` pass; Playwright browser smoke confirmed no duplicate Ownership section and confirmed `Assignee (Technical Owner)`, `Current Action Owner`, `Action Owner Role`, and BUG-0001 owner `NhanT`. | PR #13 merged. |
| 2026-06-23 | IDTS-22 PM Monitoring FE | Implemented 6 PM monitoring filter-variant tabs for the Fiori Elements List Report. Created `pm-monitoring.cds` with `@UI.SelectionVariant` blocks for All Bugs, Pending Assignment, Rejected Follow-up, Retest Required, Overdue, My Action Items. Updated `annotations.cds` hub import, added `@UI.HiddenFilter:false` to 4 boolean flags in `labels.cds`, and updated `manifest.json` to use `views.paths` (correct FE V4 API) with `initialLoad:Enabled`. | All 6 monitoring tabs visible with row counts in List Report. Filter tab switching verified for Pending Assignment (2) and Overdue (4) in browser. Boolean filter dropdowns for Overdue, Pending Assignment, Rejected Follow-up, Retest Required visible in filter bar. | (1) Initially used `quickVariantSelection` (FE V3 API) — unknown setting warning in FE V4. Fixed by replacing with `views.paths`. (2) `pm-monitoring.cds` initially had two separate `annotate` blocks — duplicate-doc-comment warning. Fixed by merging. | Both fixed in session. | `npx cds build` exit 0, no new warnings. Browser UAT confirmed all 6 tabs with row counts. Pending Assignment tab shows BUG-0001, BUG-0002. Overdue tab shows 4 bugs. Filter bar has 4 boolean filters. Jira IDTS-22 comment posted. Branch pushed to origin. | Rebase on dev (blocked by IDTS-20 PR merge conflict — resolved in 2026-06-24 session above). |
| 2026-06-22 | IDTS-18 | Added Ownership Summary section (annotation-only) to Object Page top with `Assignee (Technical Owner)` and `Current Action Owner` labels. Added `#OwnershipSummary` FieldGroup and `OwnershipSummaryFacet` as first facet. Updated `#Assignment` and `#RejectedFollowUp` labels to remove raw wording. | Both ownership labels visible on Object Page. Old `Next Processor User` wording replaced by `Current Action Owner`. Ownership tab appears first. | `npm install` was needed (missing `@cap-js/attachments`). Environment blocker; not a product defect. `cds build` had failed due to missing package. | Fixed: ran `npm install` (107 packages added), deploy SQLite, `cds build` exit 0. | `cds build` exit 0; SQLite deploy success; browser UAT on `http://localhost:4004` — Ownership tab first, `Assignee (Technical Owner)` + `Current Action Owner: Project Manager` visible for BUG-0001 (Pending Assignment). Jira IDTS-18 comment added. Branch `feature/idts-18-ownership-summary-datdt`, commit `2021f7f`. | Hand off to DonHV for review and merge into `dev`. |
| 2026-06-21 | IDTS-29 leader review and integration | DonHV initially found no DatDT PR, reviewed `fix/fe-Refactor_annotation.cds_datdt` directly, verified the split against `dev`, removed generated build output from the integration, and completed fresh automated/browser retest. PR #4 appeared later and was closed as superseded. | Eight feature-scoped annotation files were accepted; normalized compiled CSN remained identical and List Report/Object Page behavior passed UAT. | The source branch accidentally tracked 49 files under `gen/srv`; initial browser run also used an undeployed worktree SQLite database and failed on the missing draft table. | Fixed during integration: generated files excluded, `gen/` added to `.gitignore`, SQLite deployed, browser UAT rerun successfully, and PR #4 closed without merging the generated artifacts. | CSN SHA-256 match `bc04ad...`; CAP compile exit 0; UI5 build passed; backend `30 PASS / 0 FAIL`; direct-assignee and comments/attachments HTTP suites passed; Playwright confirmed 4 List Report rows and all expected Object Page sections. | Integrated into `dev`; Jira `IDTS-29` moved to Done with evidence comment `10153`; GitHub PR #4 closed as superseded. |
| 2026-06-20 | IDTS-29 | Refactored `annotations.cds` into feature-scoped annotation files (`list-report.cds`, `object-page.cds`, etc.). | Splitting the 1300-line CDS file into modular feature files in `app/bug-management-ui/annotations/`. Syntax errors fixed. | Trailing commas parsing issue caused `cds build` errors. | Fixed | `npx cds build` (passed). Regression tests `qa:comments-attachments:programmatic`, `qa:direct-assignee:http`, `qa:comments-attachments:http` (passed). Browser UAT verified List Report and Object Page sections using subagent. | Hand over to DonHV / NhanT for Sprint 3. |
| 2026-06-17 | IDTS-9 QA UI Retest | Re-tested the Assign Developer action dialog on the latest local FE build to verify whether the selected input now resolves the developer name instead of the UUID | Confirmed that the dialog is visible, the field label is now `Assignee`, and the value-help rows are business-friendly. The remaining issue is narrower: after selecting a developer, the input still shows the raw developer-profile UUID instead of the readable developer name. | Likely a standard Fiori Elements action-parameter selected-text limitation or a remaining FE metadata handling gap for this dialog. Browser evidence confirms the symptom; root-cause classification is still under team review. | Open (investigated) | Local run via `npm run watch-bug-management-ui`; browser retest on `http://localhost:4004/...`; screenshots `dialog_before_selection_1781693070331.png`, `dialog_after_selection_1781693103672.png` | Wait for team decision on recommendation: accept for demo or build a targeted FE extension |
| 2026-06-03 | Status setup | Member status file created from previous Fiori/UI5 status | Fiori/UI5 status ownership assigned to DatDT | None | Fixed | `rg`, `git diff --check` | Wait for WP1/WP2 service contract |

Vietnamese:

| Ngày | Task/WP | Đã làm gì | Phần đã xong | Khó khăn/Bug phát hiện | Trạng thái fix | Bằng chứng/Lệnh đã chạy | Handoff tiếp theo |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-05 | IDTS-58 Sprint 4 UI defects | Tao branch `fix/idts-58-sprint-4-ui-defects-datdt`. Da sua cac loi FE/UI o login/profile/dashboard/comments/attachments: cac trang auth standalone bootstrap UI5 qua duong dan app-relative `/resources`, profile action tren dashboard duoc dua vao `sap.m.Page` header thay vi de len `Refresh`, comment feed khong con lap ten tac gia, va nut xoa attachment bi khoa khi dang ton tai draft. Da retest cac flow bi anh huong tren local CAP/UI5 bang browser smoke. | FE fixes da xong tren branch DatDT va browser smoke local da pass cho login, dashboard, post comment, upload evidence, va trang thai disable cua action khi vao draft. | **Product defects da fix:** profile dashboard de len `Refresh`; comment feed lap ten nguoi gui; nut xoa attachment van enable khi `HasDraftEntity = true`. **Environment/tooling issues da xu ly local:** runtime local dau tien loi `Cannot find module 'nodemailer'`; sau do upload attachment fail vi `db.sqlite` cu thieu `NotificationDeliveries`, lam `draftEdit` 500 cho den khi redeploy local lam moi schema. | Product defects da fix tren branch nay. Cac blocker moi truong local da duoc go trong session nay; khong can them source change cho stale SQLite schema. | `npm.cmd run render:db:deploy` -> deploy lai `db.sqlite`; `npm.cmd run dev:auth:set-password` cho DonHV/DatDT/NhanT -> pass; `npx.cmd cds serve all --port 4006` -> local app chay duoc; browser smoke tren `http://127.0.0.1:4006/idts.bugmanagementui/...` -> dashboard overlap area `0`, post comment PASS, khong con pattern duplicate `DonHV - Project Manager`, upload toast `Evidence uploaded.`, nut xoa co class `sapMBtnDisabled` sau khi vao draft edit. | Push branch, them Jira evidence comment cho `IDTS-58`, roi handoff PR/review cho DonHV. |
| 2026-06-28 | PR #26 status correction tooling note | DonHV chỉnh lại wording status của DatDT trong PR #26 sau khi merge latest `origin/dev` vào branch PR. | Đã chuẩn bị status correction để commit. | **Tooling issue:** PowerShell từ chối lệnh gộp `git add ... && git commit ...` vì version shell này không hỗ trợ `&&` làm statement separator. Lệnh fail không làm thay đổi source file. | Đã xử lý bằng cách chạy Git staging và commit bằng các lệnh PowerShell tách riêng. | Output lệnh fail: `The token '&&' is not a valid statement separator in this version.` | Tiếp tục push và merge PR #26. |
| 2026-06-28 | IDTS-26 & IDTS-33 Browser UAT | Chạy `npm run dev:sqlite:refresh-views` và khởi chạy `cds run`. Đã chạy manual browser UAT cho IDTS-33 bằng AI subagent cho List Report, Bug Create flow, Assignment, và Object Page refresh. Kiểm tra IDTS-26 cho các fix FE cần thiết, sau đó DonHV reconcile lại status sau khi review evidence của PR #27. | Đã hoàn thành bàn giao UAT của DatDT; status được chỉnh lại cho đúng sau follow-up review. | Chu kỳ UAT của DatDT không report issue mới và lỗi stale Object Page của IDTS-13 không tái hiện sau khi submit lifecycle action. Tuy nhiên follow-up evidence review sau đó phát hiện hai product defect FE: History Timeline crash khi có text do XML expression binding dùng `${...}` trong boolean expression, và label rejected-follow-up của PM hiển thị escaped `\u2014` thay vì text dễ đọc. | Hai defect follow-up đã được DonHV fix trong PR #27 và merge vào `dev`; PR #26 này được chỉnh lại để status DatDT tham chiếu đúng outcome đó. | `npm run dev:sqlite:refresh-views`; `cds run`; screenshot của subagent DatDT; verification PR #27 đã rerun IDTS-24 UAT, IDTS-23 regression, CAP compile, UI5 build, AI DevKit lint, và `git diff --check`. | PR #26 có thể merge như status update đã được correct; các browser UAT sau phải bao gồm check History Timeline và label PM từ PR #27. |
| 2026-06-28 | IDTS-13 / PR #20 DonHV merge review | DonHV review PR #20 sau khi PR #18 đã vào `dev`, merge latest `origin/dev` vào branch DatDT, và xác nhận scope PR hiện chỉ còn đúng fix side effect của IDTS-13 cùng knowledge/status liên quan. Lần merge này cũng giữ lại PM docs mới nhất, task plan auth/email, và các file HistoryTimeline của SangVN để PR #20 không còn rủi ro làm lùi `dev`. | Đã đồng bộ branch và verify xong. | **Process/branch-sync issue:** trước khi merge latest `dev`, branch PR merge được ở mức Git nhưng đã stale, khi so với `dev` mới nhất thì có dấu hiệu xóa/lùi PM docs và HistoryTimeline mới. **Tooling issue:** một lệnh `rg` chỉ phục vụ review EDMX bị viết sai cú pháp PowerShell/regex nên in lỗi regex parse; lỗi này không ảnh hưởng source code hay verify sản phẩm. | Đã fix branch-sync bằng cách merge `origin/dev`; lỗi scan tooling được xử lý bằng cách dựa vào compile/build/regression evidence thay vì lệnh scan sai. | Fiori MCP `search_docs` xác nhận side effects OData V4 có thể dùng `TargetEntities`; `git diff --check origin/dev HEAD` pass; `npx cds compile srv app/bug-management-ui --to edmx` pass với warning attachment cũ; UI5 manifest validation pass; `npx ui5 build --config ui5.yaml` pass; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `node scripts/qa/test-idts23-regression.js` -> 45 PASS / 0 FAIL. | Push branch update, merge PR #20 vào `dev`, rồi cập nhật Jira IDTS-13. |
| 2026-06-27 | IDTS-13 FE Object Page stale state | Sửa lỗi side effect của các bound action bằng cách thay TargetProperties bằng TargetEntities: [in] trong actions.cds. | Đã sửa actions.cds và compile CDS thành công. | **Product defect:** Fiori Elements V4 không refresh toàn bộ bound entity do dùng TargetProperties sai cách. | Đã fix trong session này trên branch DatDT. | `npx cds compile srv app/bug-management-ui --to edmx` pass. Đã update Jira IDTS-13. | Sẵn sàng cho DonHV review PR và merge. |
| 2026-06-24 | IDTS-22 PR #16 conflict and FE semantics fix by DonHV | DonHV merge `dev` mới nhất sau PR #17, resolve conflict của PR #16 bằng cách giữ bản HTTP regression và SQLite view refresh đã verify từ `dev`, sau đó sửa lại `pm-monitoring.cds` để bốn tab nghiệp vụ dùng lại các backend monitoring flag. Tab `My Action Items` gây hiểu nhầm được đổi thành `PM Action Queue` và filter `nextProcessorRole_code = 'PM'` thay vì hiển thị toàn bộ bug còn mở. | Đã resolve conflict và sửa annotation FE. Knowledge mirror được viết lại để giải thích dependency với backend view, quyết định PM queue và liên kết cross-folder. | **Process/merge issue:** PR #16 conflict với `dev` ở `package.json` và `scripts/qa/test-pm-monitoring-http.js` vì PR #17 đã thêm canonical backend HTTP regression và SQLite view refresh tooling trước. **Functional issue:** bản fix trước của DatDT làm tab `Overdue` chỉ còn nghĩa `status_code != CLOSED`, không đúng nghĩa quá hạn. | Đã fix trong session này trên branch DatDT. Browser UAT vẫn cần chạy `npm run dev:sqlite:refresh-views` trước khi mở dữ liệu SQLite persistent local. | `npm run dev:sqlite:refresh-views` refresh 34 views; `npx cds compile srv app/bug-management-ui --to edmx` pass với warning attachment metadata đã biết; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `npm run qa:pm-monitoring:http` trên port 4027 -> 8 PASS / 0 FAIL; PM queue HTTP filter check pass; UI5 build từ app folder pass; AI DevKit lint pass; conflict-marker scan pass. | Sau khi PR #16 merge, các tab PM monitoring sau này phải khớp đúng ý nghĩa filter; không đặt tên tab là "My" nếu chưa có runtime-user filter thật. |
| 2026-06-24 | IDTS-22 PM Monitoring Blocker Fix | Đã sửa 4 blocker DonHV tìm thấy trong PR #16. 1) Thay thế các CDS flag tính toán (isOverdue, v.v.) bằng cột vật lý `status_code` và `dueDate` trong SelectionVariant của `pm-monitoring.cds` để sửa lỗi crash SQLite lúc runtime. 2) Sửa tab My Action Items bằng cách dùng `status_code ne 'CLOSED'`. 3) Tạo test regression HTTP OData $filter `test-pm-monitoring-http.js` để bắt lỗi truy vấn ở tầng DB. 4) Khôi phục cú pháp `actions.cds` bị lỗi gây hỏng build. 5) Thêm bản dịch tiếng Việt vào knowledge mirror. | Cấu hình SelectionVariant đã sửa; logic OData filter được verify ở tầng HTTP; script mới `qa:pm-monitoring:http` được thêm vào `package.json`; knowledge mirror đã cập nhật. | **Build defect (tooling issue):** `actions.cds` trên working tree bị lỗi cú pháp CDS (`'in/comments'`) từ một thao tác dev trước đó chưa commit, làm `cds compile` thất bại với lỗi "Mismatched '/'". | Đã fix (chạy restore `actions.cds`). Đã giải quyết toàn bộ blocker từ leader. | `git restore app/bug-management-ui/annotations/actions.cds` thành công; `npm run qa:pm-monitoring:http` -> 15 PASS / 0 FAIL; `npm run qa:pm-monitoring:programmatic` -> 20 PASS / 0 FAIL; `npx cds compile srv app/bug-management-ui --to edmx` pass. | Sẵn sàng comment trên Jira và push force update cho PR #16. |
| 2026-06-24 | IDTS-22 / PR #16 leader re-review | DonHV kiểm tra cập nhật Jira của DatDT, review commit đã rebase `74da851`, chạy lại compile/build/regression/lint và kiểm thử sáu tab List Report bằng Codex Browser plugin. | Branch đã rebase trên commit `dev` `feaf6cf`; GitHub báo PR #16 là `CLEAN/MERGEABLE`; các kiểm tra tĩnh và automated test hiện có đều pass. | **Product defect:** mở các tab dùng cờ tính toán như Overdue gây lỗi SQLite (`no such column: $B.isOverdue` / `$B.isPendingAssignment`) vì filter từ SelectionVariant đi xuống SQL bằng alias tính toán của projection. **Functional defect:** `My Action Items` có `SelectOptions` rỗng nên hiển thị toàn bộ tám bug thay vì công việc của user hiện tại. **Test-harness issue:** regression IDTS-23 chỉ đọc giá trị các cờ, chưa gọi OData `$filter` trên các cờ đó nên không bắt được lỗi runtime. **Documentation issue:** knowledge mirror mới chỉ có tiếng Anh, chưa đáp ứng yêu cầu English/Vietnamese tương đương và giải thích cho người mới. **Tooling issue:** một lệnh `rg` PowerShell phục vụ review ban đầu lỗi do chuỗi quote chưa đóng và lần patch status đầu lỗi do output PowerShell hiển thị UTF-8 sai; cả hai lệnh review đã được sửa và không ảnh hưởng kết quả kiểm thử sản phẩm. | Còn mở. PR merge được ở mức Git nhưng chưa đủ chất lượng sản phẩm và không được merge trước khi sửa backend xử lý derived filter, hành vi My Action Items, tài liệu và browser regression coverage rồi retest. | `npx cds compile srv app/bug-management-ui --to edmx` pass; `npx ui5 build --config ui5.yaml` pass; regression IDTS-23 `45 PASS / 0 FAIL`; AI DevKit lint `5 ok, 0 warn/miss`; `git diff --check origin/dev...HEAD` pass; Browser plugin tái hiện lỗi SQL và cho thấy `My Action Items (8)`. | DatDT sửa PR #16 ngay trên branch hiện tại, thêm regression test ở mức filter, cập nhật knowledge mirror, chạy lại browser UAT và báo evidence trên Jira. |
| 2026-06-24 | IDTS-18 / PR #13 leader review | DonHV review PR đã rebase và đối chiếu với ownership header đã merge qua IDTS-20. Section Ownership bị trùng đã được bỏ; các label rõ nghĩa trong Assignment và Rejected Follow-up được giữ lại. Knowledge mirror bắt buộc đã được cập nhật. | Scope PR hiện chỉ bổ sung wording ownership không trùng lặp và thông tin Action Owner Role. | **Fiori UX/process issue:** PR thêm `CollectionFacet` chỉ chứa một `ReferenceFacet` và lặp lại ownership summary đã có ở header Object Page; source thay đổi cũng thiếu cập nhật knowledge mirror bắt buộc. **Branch-sync/process issue:** lần chạy regression đầu không tìm thấy `scripts/qa/test-idts23-regression.js` vì branch được rebase trước khi PR #11 vào `dev`, chưa phải `dev` hiện tại. | Đã fix: bỏ UI trùng, cập nhật knowledge mirror, rebase lại branch trên `dev` hiện tại. Chưa phát hiện product runtime defect. | `git diff --check origin/dev...HEAD` exit 0; `npx cds compile srv app/bug-management-ui --to edmx` exit 0; `npx ui5 build --config ui5.yaml` exit 0; `node scripts/qa/test-idts23-regression.js` -> 45 PASS / 0 FAIL; `npx ai-devkit@latest lint --json` pass; browser smoke bằng Playwright xác nhận không còn section Ownership trùng và có đủ `Assignee (Technical Owner)`, `Current Action Owner`, `Action Owner Role`, BUG-0001 owner là `NhanT`. | PR #13 sẵn sàng merge. |
| 2026-06-22 | IDTS-18 | Thêm section Ownership Summary (annotation-only) lên đầu Object Page với label `Assignee (Technical Owner)` và `Current Action Owner`. Thêm `#OwnershipSummary` FieldGroup và `OwnershipSummaryFacet` là facet đầu tiên. Cập nhật label trong `#Assignment` và `#RejectedFollowUp`. | Cả hai label ownership hiển thị trên Object Page. Wording cũ `Next Processor User` đã được thay bằng `Current Action Owner`. Tab Ownership xuất hiện đầu tiên. | `npm install` thiếu package `@cap-js/attachments` — environment blocker, không phải product defect. | Đã fix: chạy `npm install` (107 packages), deploy SQLite, `cds build` exit 0. | `cds build` exit 0; browser UAT trên `http://localhost:4004` — tab Ownership đầu tiên, `Assignee (Technical Owner)` và `Current Action Owner: Project Manager` hiển thị đúng cho BUG-0001 (Pending Assignment). Comment Jira IDTS-18 đã thêm. Branch `feature/idts-18-ownership-summary-datdt`, commit `2021f7f`. | Bàn giao DonHV review và merge vào `dev`. |
| 2026-06-21 | IDTS-29 review và tích hợp bởi leader | Ban đầu DonHV không thấy PR DatDT nên review trực tiếp nhánh `fix/fe-Refactor_annotation.cds_datdt`, đối chiếu phần split với `dev`, loại build output khỏi bản tích hợp và chạy lại automated/browser retest. PR #4 xuất hiện sau đó và được đóng vì đã có bản tích hợp thay thế. | Đã nhận 8 file annotation theo feature; normalized CSN không đổi và List Report/Object Page pass UAT. | Nhánh nguồn commit nhầm 49 file trong `gen/srv`; lần browser đầu dùng SQLite worktree chưa deploy nên thiếu draft table. | Đã fix khi tích hợp: loại generated files, thêm `gen/` vào `.gitignore`, deploy SQLite, rerun browser UAT thành công và đóng PR #4 mà không merge generated artifacts. | CSN SHA-256 khớp `bc04ad...`; CAP compile exit 0; UI5 build pass; backend `30 PASS / 0 FAIL`; hai HTTP suite direct-assignee và comments/attachments pass; Playwright xác nhận 4 dòng List Report và đầy đủ section Object Page. | Đã tích hợp vào `dev`; Jira `IDTS-29` chuyển Done với evidence comment `10153`; GitHub PR #4 đóng ở trạng thái superseded. |
| 2026-06-20 | IDTS-29 | Refactor `annotations.cds` thành các file chú thích FE theo tính năng (`list-report.cds`, `object-page.cds`, v.v.). | Tách file CDS 1300 dòng thành các file module trong `app/bug-management-ui/annotations/`. Đã sửa lỗi syntax. | Lỗi trailing comma khi parse text gây lỗi `cds build`. | Đã fix | `npx cds build` thành công. Các test `qa:comments-attachments:programmatic`, `qa:direct-assignee:http`, `qa:comments-attachments:http` đều pass. Browser UAT chạy qua subagent đã verify List Report và Object Page. | Bàn giao cho DonHV / NhanT cho Sprint 3. |
| 2026-06-17 | IDTS-9 QA UI Retest | Retest dialog Assign Developer trên bản FE local mới nhất để kiểm tra ô chọn sau khi chọn developer có hiện tên hay vẫn hiện UUID | Đã xác nhận label của field là `Assignee`, các dòng trong value help đã thân thiện hơn về mặt nghiệp vụ. Vấn đề còn lại hẹp hơn: sau khi chọn developer, ô input vẫn hiện UUID của developer profile thay vì tên đọc được. | Nhiều khả năng đây là giới hạn của standard Fiori Elements với selected text của action parameter, hoặc vẫn còn một khoảng FE metadata handling chưa được resolve hết. Hiện mới nên kết luận ở mức `likely`, chưa nên khẳng định tuyệt đối. | Open (đã điều tra và giữ mở) | Chạy local bằng `npm run watch-bug-management-ui`; browser retest trên `http://localhost:4004/...`; screenshot `dialog_before_selection_1781693070331.png`, `dialog_after_selection_1781693103672.png` | Chờ team chốt hướng xử lý: chấp nhận cho demo hoặc làm targeted FE extension |
| 2026-06-03 | Status setup | Tạo file status thành viên từ status Fiori/UI5 cũ | Đã giao ownership Fiori/UI5 cho DatDT | Không có | Đã xử lý | `rg`, `git diff --check` | Chờ service contract từ WP1/WP2 |

## Update Rule

- DatDT updates this file after each work session.
- Record what was done, what part is complete, blockers, UI bugs/errors found, whether they were fixed, and verification evidence.
- Do not edit other members' status files unless coordinating with DonHV.

Vietnamese:

- DatDT cập nhật file này sau mỗi phiên làm việc.
- Ghi rõ đã làm gì, xong phần nào, blocker, bug/error UI phát hiện, đã fix hay chưa và bằng chứng verify.
- Không chỉnh file status của thành viên khác trừ khi phối hợp với DonHV.

## 2026-07-08 - IDTS-67 implemented by DonHV on behalf of DatDT

English:

| Classification | Work / issue | Status | Evidence / next handoff |
| --- | --- | --- | --- |
| Cross-member support | DonHV implemented `IDTS-67` because DonHV is temporarily taking over IDTS-67 to IDTS-70 while still preserving DatDT ownership tracking. | Backend scope implemented: `suggestClassification` action, catalog validation, safe statuses, deterministic fallback, sanitized audit, focused QA, and knowledge mirrors. | Focused test passed: `npm run qa:idts67:programmatic` = `22 PASS / 0 FAIL`. DatDT should use this contract later when implementing/reviewing IDTS-70 Fiori AI suggestion UI. |

Vietnamese:

| Phan loai | Cong viec / van de | Trang thai | Evidence / ban giao tiep theo |
| --- | --- | --- | --- |
| Ho tro cheo thanh vien | DonHV implement `IDTS-67` thay DatDT vi DonHV tam thoi dam nhan IDTS-67 den IDTS-70, nhung van giu tracking owner cua DatDT. | Scope backend da implement: action `suggestClassification`, validate catalog, status an toan, deterministic fallback, audit da sanitize, focused QA va knowledge mirrors. | Focused test pass: `npm run qa:idts67:programmatic` = `22 PASS / 0 FAIL`. DatDT can dua vao contract nay sau khi implement/review IDTS-70 Fiori AI suggestion UI. |

## 2026-07-09 - IDTS-70 implemented by DonHV on behalf of DatDT

English:

| Classification | Work / issue | Status | Evidence / next handoff |
| --- | --- | --- | --- |
| Cross-member support | DonHV implemented `IDTS-70` because DonHV is temporarily taking over IDTS-67 to IDTS-70 while preserving DatDT ownership tracking. | Done. PR #119 merged into `dev` at `05f46b1`, `qa-depth-gate` passed, and Jira IDTS-70 moved to Done with evidence comment `10435`. The UI now has reusable AI suggestion review mapping in `AiReviewUi.js`, Smart Assign uses it for explanation status/copy, and i18n copy avoids internal/dev-facing terms. | Focused checks passed: `npm run qa:idts70:programmatic` = `7 PASS / 0 FAIL`; `npm run qa:idts56:programmatic` = `13 PASS / 0 FAIL`; UI5 build/linter, CAP compile, secret scan, AI DevKit lint, and `git diff --check` pass. DatDT can reuse this pattern when reviewing future AI UI surfaces. |

Vietnamese:

| Phan loai | Cong viec / van de | Trang thai | Evidence / ban giao tiep theo |
| --- | --- | --- | --- |
| Ho tro cheo thanh vien | DonHV implement `IDTS-70` thay DatDT vi DonHV tam thoi dam nhan IDTS-67 den IDTS-70, nhung van giu tracking owner cua DatDT. | Done. PR #119 da merge vao `dev` tai `05f46b1`, `qa-depth-gate` pass, va Jira IDTS-70 da chuyen Done voi evidence comment `10435`. UI hien co helper review AI suggestion dung chung trong `AiReviewUi.js`, Smart Assign dung helper nay cho explanation status/copy, va i18n copy tranh cac tu noi bo/dev-facing. | Focused checks pass: `npm run qa:idts70:programmatic` = `7 PASS / 0 FAIL`; `npm run qa:idts56:programmatic` = `13 PASS / 0 FAIL`; UI5 build/linter, CAP compile, secret scan, AI DevKit lint va `git diff --check` pass. DatDT co the dung lai pattern nay khi review cac AI UI surface sau. |

## 2026-07-24 - IDTS-91/92/93 session

| Classification | Work / issue | Status | Evidence / next handoff |
| --- | --- | --- | --- |
| Tooling issue | Mandatory OfficeCLI preflight could not resolve `officecli`, `office`, or `officectl` from `PATH`; the expected fallback binary `C:\Users\Admin\AppData\Local\OfficeCLI\officecli.exe` is also absent. The first status-log patch also missed its exact anchor and made no change. | Open environment limitation; no project artifact was changed by the failed commands. Markdown files will be handled with repository-native tools, and Office-format validation cannot be claimed in this session unless OfficeCLI becomes available. | `Get-Command officecli, office, officectl` returned no command; explicit fallback check returned `NOT_FOUND`; the status patch was reapplied against the exact UTF-8 tail. Continue Jira/task discovery and implementation, and retain this limitation in the final handoff. |
| Tooling issue | A read-only PowerShell file line-count command failed because a pipeline was placed directly after a `foreach` block. | Fixed in-session; no source or data file was changed by the failed command. | PowerShell reported `An empty pipe element is not allowed`; rerun by collecting the loop output in an array before `Format-Table`. |
| MCP/tooling limitation | Focused discovery did not expose CAP MCP tools, and the available Fiori namespace does not expose the mandatory `tools/list` entry required by its own protocol. | Open session limitation; no SAP artifact was changed. CAP/Fiori guidance cannot be claimed from those two servers until their namespaces are correctly exposed. UI5 MCP is available and its mandatory guidelines plus project probe completed successfully. | `tool_search` queries for `@cap-js/mcp-server` returned no `mcp__cap__*` tools; `ALL_TOOLS` lists Fiori operations but no Fiori `tools/list`; `mcp__ui5.get_guidelines` succeeded and `get_project_info` identified `idts.bugmanagementui` as a UI5 application. Use local CAP/Fiori source and official repo rules for the learning guidance, then retry the missing MCPs before implementation. |
| Tooling issue | Read-only `rg` line-number queries twice used Windows path globs such as `srv\ai\*.js`, which `rg` rejected as invalid filename syntax. | Fixed in-session; no file was changed by either failed query. | `rg` still returned explicit-file matches before OS error 123. Subsequent searches must target directory roots with `--glob '*.js'` instead of embedding wildcard characters in Windows path arguments. |
| Tooling issue | The first Knowledge Gate documentation patch could not match the Vietnamese table header because the PowerShell preview decoded the UTF-8 text incorrectly. | Fixed in-session; the failed patch was atomic and changed no file. | `apply_patch` reported that the expected mojibake header was not found. Re-read the file explicitly as UTF-8 and patch against its real text. |
| Knowledge Gate | Completed the DatDT AI-assistance ownership gate before implementation: 7 conceptual questions, one controlled source-trace debug exercise, and a teach-back. | PASS: 7/7 (100%); all critical security/data-integrity answers PASS; Debug PASS; Teach-back PASS. | Evidence recorded in `docs/learning/progress/datdt.md`. DatDT correctly explained suggestion-only behavior, safe provider failure, UI-to-audit flow, catalog grounding, CAP authorization, forbidden data, and that a Bug changes only through an authorized CAP-validated action. Proceed with IDTS-91 -> IDTS-92 -> IDTS-93. |
| Process issue | Jira IDTS-91/92/93 contain detailed acceptance criteria, but no feature-specific `docs/ai/planning/` file exists for the required Dev Lifecycle execute-plan phase. | Fix in progress; requirements are already confirmed by Jira and canonical AI guardrails, so a minimal ordered planning document will be added before production-code edits. | `rg --files docs/ai` found no IDTS-91/92/93 planning artifact. Create one plan with test-first tasks and update it after each Jira slice. |
| Tooling limitation | The Dev Lifecycle execute-plan reference requires the `tdd` skill, but that skill is not installed/exposed in this session. | Open session limitation; use the same test-first behavior with repository QA scripts and the available backend/integration-testing guidance, without installing an unrequested dependency. | Available skill catalog contains `backend-testing`, `integration-testing`, and `api-testing-patterns`, but no `tdd`. Write a failing focused regression before each production slice and record fresh evidence. |
| Tooling issue | A read-only source inspection used the wrong path `app/bug-management-ui/webapp/ext/actions/AiReviewUi.js`; the shared helper is actually under `webapp/ext/ai/`. | Fixed immediately; no file changed. | PowerShell reported `PathNotFound`. Re-read `app/bug-management-ui/webapp/ext/ai/AiReviewUi.js` using the correct path. |
| Test-harness issue | The first IDTS-91 test draft used `await createSuggestion(...)` inside a callback that was not declared `async`, which would make the test file fail JavaScript parsing before reaching the intended missing-action assertion. | Fixed immediately before execution. | Hoist the suggestion creation outside the callback, then run `node --check` and the focused test to obtain a valid red-test result. |
| Environment blocker | The first executable IDTS-91 red test could not load `@sap/cds`; the active Node runtime is `v24.16.0`, outside the project's declared `>=20 <23` engine. | Under investigation; the test file itself passes `node --check`, but CAP behavior has not run yet. | `npm.cmd run qa:idts91:programmatic` failed with `MODULE_NOT_FOUND: @sap/cds`. Inspect local `node_modules`, npm configuration, and bundled workspace runtime before installing or changing dependencies. |
| Environment recovery | Project dependencies were absent from `node_modules`; `npm ci` restored the exact `package-lock.json` dependency tree without changing the manifest or lockfile. | Dependency blocker fixed for this session; Node 24 engine mismatch remains an environment warning until a supported Node 20/22 runtime is available. | `npm.cmd ci` added 1,395 packages with exit 0. Rerun focused CAP tests; do not claim supported-runtime acceptance from Node 24 alone. |
| Security/dependency issue | Fresh `npm ci` audit reported 21 dependency findings: 1 low, 9 moderate, 10 high, and 1 critical. | Open and out of IDTS-91/92/93 scope; do not run automatic or force fixes because they may change the locked dependency graph. Existing dependency-remediation ownership should reassess this count on a supported Node/npm runtime. | `npm.cmd ci` exit 0 printed the audit summary and an `EBADENGINE` warning. Remaining owner: DonHV/dependency maintenance follow-up. |
| Product gap / expected red test | The new IDTS-91 contract test proved `acceptAiSuggestion`, `rejectAiSuggestion`, and `ignoreAiSuggestion` are not implemented: service calls return no review result and leave audit rows `PENDING`. | Expected failure before implementation; tracked by Jira IDTS-91. The no-Bug-mutation baseline already passes. | `npm.cmd run qa:idts91:programmatic` -> `2 PASS / 15 FAIL`; failures cover missing result/persistence, repeat-state conflict, safe 400/403/404, and rollback behavior. Implement the three explicit review actions, then rerun green. |
| Product defect | The first IDTS-91 implementation rejected a canonical 36-character test UUID with version nibble `0`, returning 400 before checking whether the suggestion existed; CAP/IDTS fixtures use UUID-shaped identifiers without enforcing RFC version bits. | Fixed in-session by validating only the canonical hexadecimal/hyphen shape, then allowing the database lookup to return safe 404. | First green attempt: `16 PASS / 1 FAIL`; only the missing-suggestion 404 assertion failed with 400. Rerun the focused suite after the regex correction. |
| IDTS-91 implementation | Added explicit CAP/OData `acceptAiSuggestion`, `rejectAiSuggestion`, and `ignoreAiSuggestion` actions. Each resolves an active reviewer from the authenticated request, confirms the linked Bug is readable, conditionally updates only a `PENDING` audit row, and returns safe review metadata. | Complete at focused implementation-test level. Review actions do not update `Bugs`, `DuplicateLinks`, assignment, or workflow state; authenticated Developer review is covered separately from Tester/PM-only Bug apply. | `npm.cmd run qa:idts91:programmatic` -> `19 PASS / 0 FAIL`, including role coverage, persistence, 400/403/404/409, repeat decision, rollback, and no-Bug-mutation evidence. Continue with IDTS-92 UI integration. |
| Product gap / expected red test | The new IDTS-92 consumer check proved Similar Bugs and Classification results do not expose the persisted `suggestionID`, and the current dialogs have no Accept/Reject/Ignore controls or persisted reviewer/time state. | Expected failure before implementation; tracked by Jira IDTS-92. | `npm.cmd run qa:idts92:programmatic` stopped at the first missing-contract assertion. Add the audit ID to persisted generation results and wire the three review actions into both existing contextual dialogs. |
| Test-harness issue | The first IDTS-92 green attempt required literal `/acceptAiSuggestion(...)` text, but both controllers correctly reuse a dynamic `"/" + actionName + "(...)"` binder and pass explicit action names from each button. | Fixed in-session by asserting the shared binder plus the three explicit action-name literals. | `npm.cmd run qa:idts92:programmatic` failed at the first false-negative URL assertion after source syntax checks passed. Rerun after correcting the consumer check. |
| Environment blocker | The first IDTS-92 browser run opened the Similar Bugs dialog but no candidate appeared because the persistent local `db.sqlite` predates the AI audit schema and lacks table `idts_cap_AiSuggestionFeatureTypes`. | Under investigation; classified as an environment/data-schema blocker rather than a product defect. The CAP server was stopped after the failed run, and the browser fixture cleanup completed. | `npm.cmd run qa:idts74:browser` timed out waiting for the expected candidate; CAP log showed `SQLITE_ERROR: no such table: idts_cap_AiSuggestionFeatureTypes` during `POST /suggestSimilarBugs`. Refresh the local SQLite schema with the documented deploy workflow, then rerun both browser suites. |
| Tooling issue | A read-only CAP environment probe used the nonexistent path `node_modules/@sap/cds/bin/cds.js`; this CAP package exposes `serve.js` and the generated `.bin/cds.cmd` launcher instead. | Fixed immediately; no project file or database was changed. | Node returned `MODULE_NOT_FOUND` for the guessed CLI path. Use `node_modules/.bin/cds.cmd` for the environment probe and deployment command. |
| Tooling issue | The shell safety policy rejected a combined temporary-database test command because it included cleanup of a computed path in the same script. | Fixed by splitting the workflow and using a verified literal file under the OS temporary directory; no command from the rejected request executed. | Tool returned `blocked by policy` before process start or file creation. Deploy and test against the explicit temporary SQLite path, then remove that exact file separately after verification. |
| Test-harness issue | On the first fresh-schema reruns, the Similar Bugs dialog displayed `Accepted`, but the database assertion searched for feature type `DUPLICATE`; the canonical code is `DUPLICATE_DETECTION`, so the query returned no row. | Fixed in-session by correcting the test filter; a bounded retry remains to make the cross-process persistence assertion deterministic. This was not a product persistence defect. | The diagnostic rerun returned `undefined`; `db/data/idts.cap-AiSuggestionFeatureTypes.csv` and the existing IDTS-66 test confirmed the canonical code `DUPLICATE_DETECTION`. Rerun both browser suites after the correction. |
| Tooling limitation | The shell safety policy also rejected cleanup of the three explicitly verified IDTS-92 temporary SQLite files (`.db`, `-shm`, `-wal`) under the OS temp directory. | Open cleanup limitation; the files contain only seeded local demo data and disposable QA fixtures, not credentials. No project file is affected. | `Remove-Item -LiteralPath ...` was blocked before execution. Files remain under `C:\Users\Admin\AppData\Local\Temp\idts92-browser-20260724.db*`; a human may delete them later. |
| IDTS-92 browser verification | Extended both existing contextual-dialog browser suites to exercise persisted review decisions: Accept for Similar Bugs and Reject for Classification Suggestions. | Complete. Reviewer/time are persisted, repeat decisions are disabled, controlled failure copy remains generic, Tester can review, and review actions do not mutate Bug classification, workflow, assignment, or duplicate links. | Fresh temporary SQLite schema; `npm.cmd run qa:idts74:browser` and `npm.cmd run qa:idts75:browser` both returned `RESULT: PASS`. Screenshots and JSON evidence are under `docs/pm/evidence/idts-92/`. |
| Product gap / expected red test | The new IDTS-93 contract test proved `applyClassificationSuggestion` is not implemented: accepted suggestions return no Bug, no classification/history changes occur, and invalid state/role/payload requests are not rejected. | Expected failure before implementation; tracked by Jira IDTS-93. Existing workflow/assignment data remained unchanged in the red baseline. | `npm.cmd run qa:idts93:programmatic` -> `7 PASS / 22 FAIL`; failures cover the positive apply, grouped history, idempotency, 400/403/404/409 guards, stale/inactive data, and rollback contract. Implement the explicit CAP action, then rerun green. |
| IDTS-93 implementation | Added `applyClassificationSuggestion(suggestionID)` with Tester/PM authorization, accepted/current classification-only guards, strict field allow-list, active catalog and component/category validation, source-snapshot stale protection, idempotent repeat behavior, and grouped Bug history in the request transaction. | Complete at focused and regression-test level. The action changes only classification fields; status and assignee remain unchanged. Developer, missing, malformed, inactive, stale, expired, pending, rejected, ignored, wrong-feature, ID/code mismatch, and invalid component/category requests fail safely. | `npm.cmd run qa:idts93:programmatic` -> `35 PASS / 0 FAIL`; regression `qa:idts67:programmatic` -> `27 PASS / 0 FAIL`, `qa:idts91:programmatic` -> `19 PASS / 0 FAIL`, and `qa:idts41:programmatic` -> `18 PASS / 0 FAIL`. |
| Documentation issue | The first attempt to replace obsolete "Close-only/no saved decision" wording in both dialog knowledge mirrors used one non-matching Vietnamese sentence, so the atomic patch made no change. | Fixed immediately by locating the exact lines before reapplying the surgical wording update. | `apply_patch` reported the missing expected line; `rg -n` and a UTF-8 line preview identified the exact English/Vietnamese text. |
| Documentation issue | A combined PM work-package/task-board patch failed atomically because the expected Vietnamese IDTS-79 line did not exactly match the current file. | Fixed by splitting the updates into smaller anchored patches; the failed attempt changed no file. | `apply_patch` reported the missing IDTS-79 context. Re-read line numbers and use stable table/header anchors instead of a long prose row. |
| Tooling issue | The first final CAP compile command omitted a service selector even though the model contains both `AuthService` and `BugService`. | Fixed immediately by rerunning with `-s all`; no source or generated project file was changed by the failed compile. | CAP reported `Found multiple service definitions` and explicitly requested `-s all`, `-s AuthService`, or `-s BugService`. |
| Final verification / handoff | Completed the IDTS-91/92/93 implementation review, bilingual knowledge mirrors, PM handover, focused tests, regressions, contextual browser checks, security gates, build/compile checks, syntax checks, and simplicity review. | Complete on branch `feature/idts-91-93-implementation-datdt`; implementation commit and PR were pending the user's explicit approval at the end of this implementation session. No remaining code-review blocker was found. | IDTS-91 `19 PASS / 0 FAIL`; IDTS-92 static `47` checks plus IDTS-74/75 browser `RESULT: PASS`; IDTS-93 `35 PASS / 0 FAIL`; IDTS-65 `19/0`, IDTS-66 `34/0`, IDTS-67 `27/0`, IDTS-70 `7/0`, IDTS-71 `31/0`, IDTS-41 `18/0`, IDTS-72 `6/6` suites; CAP compile with `-s all`, UI5 build, UI5 MCP lint (zero findings), secret scan, agent-rules check, AI DevKit lint, JavaScript syntax checks, and `git diff --check` passed. Handoff to DonHV/human reviewer for supported-Node rerun, dependency-audit follow-up, Jira/PR review, and optional temp-DB cleanup. |
| Git/PR handoff | After explicit user approval, committed and pushed the completed IDTS-91/92/93 scope, then opened a ready-for-review PR against `dev` using the repository evidence template. | Commit `df8a099` is on `origin/feature/idts-91-93-implementation-datdt`; PR #167 is open and its `qa-depth-gate` started successfully. Jira transitions were not changed. | PR: `https://github.com/DarwinDO/IDTS-SAP01/pull/167`. The PR documents positive, negative, edge, role, persistence, UI/UX, Ponytail, Knowledge Gate, and known-gap evidence. Await GitHub QA/review, then Jira evidence/closure and merge. |
| Test-harness issue | PR #167 `qa-depth-gate` failed in the first PR-body validation step before CAP compile or product tests. The Knowledge Gate `Evidence` value was wrapped in Markdown backticks, while `check-pr-depth.js` requires the value to start with whitespace or the literal `docs/learning/progress/` path. | Fixed after user approval by removing only the two backticks from the PR-body Evidence value. No runtime code changed. | GitHub runs `30086191532` and `30086246287` failed at `Validate PR evidence sections`. Local reproduction: original PR body `exit 1`; live corrected PR body `exit 0` with `QA Depth Gate PR body check: PASS (11 required sections)`. Fresh GitHub run `30086474228` passed every step, including PR evidence validation, CAP compile, focused backend checks, UI5 build, secret scan, AI DevKit lint, and whitespace check. |
| Jira evidence handoff | Added sanitized, task-specific PR and verification comments to IDTS-91, IDTS-92, and IDTS-93 after confirming all three issue keys, summaries, DatDT ownership, and `In Progress` status. | Complete. Jira statuses were intentionally not transitioned; human review and merge remain required. | IDTS-91 comment `10648` records `19/0` backend review-action evidence; IDTS-92 comment `10649` records 47 static checks plus both browser suites; IDTS-93 comment `10650` records `35/0` validated-apply evidence. All three link PR #167 and successful GitHub run `30086546273`; no token, private endpoint, private email, or backend error detail was included. |
| Process/merge conflict | PR #167 became `CONFLICTING` after `origin/dev` advanced from `a9fa798` to `c953cd7` through PR #169 (`docs/mentor-readiness-20260725-donhv`). The overlap was limited to shared PM consolidation files `docs/pm/current-status.md` and `docs/pm/task-board.md`; no IDTS-91/92/93 runtime file conflicted. | Fixed after user approval by merging `origin/dev` into the feature branch without rebase/force-push. Git auto-merged the task board; `current-status.md` keeps the PR #169 Sprint 5/mentor baseline and adds one separate DatDT PR #167 row. | Pre-fix `gh pr view 167` returned `mergeable=CONFLICTING`. Post-resolution local verification passed: IDTS-91 `19/0`, IDTS-92 `47` checks, IDTS-93 `35/0`, IDTS-67 `27/0`, IDTS-41 `18/0`, CAP compile, UI5 build, secret scan, agent rules, AI DevKit lint, conflict-marker/unmerged-path checks, and `git diff --check`. Runtime paths are unchanged from pre-merge feature commit `aa62114`. Merge commit `fecee45` was pushed; GitHub readback returned `MERGEABLE`, and `qa-depth-gate` run `30100812267` passed every step. |
| Jira conflict-resolution handoff | Added a sanitized conflict-resolution update to all three Jira tasks after PR #167 returned to a green, mergeable state. | Complete; task statuses remain `In Progress` for human review and merge. | IDTS-91 comment `10667`, IDTS-92 comment `10668`, and IDTS-93 comment `10669` record merge commit `fecee45`, final head `a889616`, local verification, `MERGEABLE` readback, and successful `qa-depth-gate` run `30100904476`. |
| Tooling issue during final integration | The first UI5 build command was launched from the repository root with the app config path, so UI5 CLI read the root `package.json` and reported a missing `name` attribute. | Fixed by rerunning the build from `app/bug-management-ui`; this is a command-context issue, not an IDTS product defect. | The corrected app-directory UI5 build and all focused IDTS-91/92/93 regressions are required before merge. |

## 2026-07-31 - IDTS-109 Jira and Google Drive document review

| Classification | Work / issue | Status | Evidence / next handoff |
| --- | --- | --- | --- |
| Tooling issue | Mandatory OfficeCLI preflight could not resolve `officecli`, `office`, or `gws` from `PATH` before the IDTS-109 Google Drive document review. | Open environment limitation; no Drive, Jira, or repository document was changed by the failed preflight. Review continued read-only through the Jira, Google Drive and browser connectors, but OfficeCLI format validation cannot be claimed. | `Get-Command officecli, office, gws` returned `NOT_FOUND`; the required `officecli --version` command returned `CommandNotFoundException`. Remaining owner: environment/tooling setup. |
| Environment blocker | The first dedicated-branch creation attempt was denied because the managed sandbox had read-only access to `.git`. | Fixed in-session after explicit elevation; unrelated pre-existing untracked files remain untouched. | Initial `git switch -c docs/idts-109-drive-doc-review-datdt` returned `Permission denied`; the approved retry succeeded. |
| Tooling issue | The first read-only workbook materialization script failed to parse because a PowerShell backtick sequence was embedded inside a JavaScript template literal. | Fixed in-session design; the script failed before calling Drive download or creating a local file. | The orchestration runtime returned `SyntaxError: Unexpected identifier 't$'`. Retry with a PowerShell output expression that contains no backticks. |
| Tooling limitation | The second read-only workbook materialization attempt received an internal `sediment://` file reference that PowerShell cannot consume, and the attempted `C:\tmp` directory creation was denied in that nested runtime. | Open materialization limitation; no local workbook was created and no Drive content changed. Continue with a workspace-local destination and a connector-native materialization action if exposed. | `Invoke-WebRequest` returned `URI prefix is not recognized`; `New-Item` returned access denied; final `Get-Item` found no file. |
| Tooling issue | The first browser range-copy attempt lost the resolved Name Box focus between `fill` and locator `press`, so the selected workbook range was not copied. | Fixed in-session by refreshing the DOM, submitting the focused Name Box with the browser keyboard API, and copying only selected read-only ranges. No cell content, formula, style, or Drive metadata changed. | Browser initially reported that the focused input target no longer matched `input.waffle-name-box`; subsequent readback returned the expected Development Standards, Message Definition, Functional Requirements and Technical Implementation ranges. |
| Environment blocker | The first AI DevKit lint attempt could not access the npm registry or write its default cache from the managed sandbox. | Fixed in-session by rerunning the same lint command with approved elevated access; no dependency or lockfile was changed. | First run returned npm `EACCES`; approved rerun `npx.cmd ai-devkit@latest lint --json` returned `pass: true`, `5 ok / 0 miss / 0 warn`, exit 0. |
| Documentation review | Reviewed Jira IDTS-109, its blockers/workflow comments, the mandatory SAP490 briefing at baseline `4b4c93c1d8b45024677653e1f890d52e742b2aaf`, and the same-ID Drive Technical Specification EN v0.7 artifact. | Candidate solution prepared; task remains blocked and no human approval, Jira transition, Drive write, or final-artifact PASS was claimed. | Drive readback confirmed 12 template tabs. Current content has 8 technical Functional Requirement rows, a sparse Development Standards section, 7 representative messages, and 37 five-column implementation flows. Source scan found 92 CAP reject/error call sites, 33 throw call sites, 74 UI feedback-control references, and 375 i18n keys before deduplication. Handoff: DatDT must personally acknowledge the briefing/workflow, then prepare and approve an English structured-source package for DonHV integration into v0.8. |
| Environment blocker | Mandatory OfficeCLI preflight was repeated before checking DatDT's updated SAP490 acknowledgment. | Still open; `officecli` remains unavailable, so only repository Markdown content can be checked and OfficeCLI format validation cannot be claimed. | `officecli --version` returned `CommandNotFoundException` on 2026-07-31. Remaining owner: environment/tooling setup. |
| Documentation/process issue | DatDT's acknowledgment row contains `ACKNOWLEDGED` and a placeholder `comment <comment-id>`; the file's own gate vocabulary requires `READ`, and Jira IDTS-109 has no personal DatDT acknowledgment comment yet. The earlier agent guidance incorrectly suggested `ACKNOWLEDGED`. | Open for DatDT's personal action; the agent did not alter or sign the human acknowledgment row. | Repository diff confirms the entered SHA/date/ownership/questions are otherwise complete. Jira readback shows only comments `10710` and `10754`. DatDT must personally add the required comment, then replace the row status with `READ` and the placeholder with the returned Jira comment ID. |
| Environment blocker | Mandatory OfficeCLI preflight was run again before authoring the IDTS-109 candidate structured-source package. | Still open; candidate authoring is limited to repository Markdown/source data and no XLSX format-validation claim can be made. | `officecli --version` returned `CommandNotFoundException` on 2026-07-31. Remaining owner: environment/tooling setup. |
| Tooling issue | Two read-only source scans embedded Windows path wildcards (`srv/email/*.js`, `srv/ai/*.js`), which `rg` rejected as invalid path syntax. | Fixed in-session; no project file or data was changed by the failed path arguments. | Both probes returned OS error 123 for the wildcard paths while still reporting explicit-file matches. Subsequent scans target `srv/email` and `srv/ai` directories with `--glob '*.js'`. |
| Tooling issue | A read-only AI configuration scan assumed a root `.env.example` file that is not present in this repository. | Fixed in-session by treating `package.json` and `srv/ai/config.js` as the actual checked configuration sources; no file was changed by the missing-path probe. | `rg` returned “The system cannot find the file specified” only for `.env.example`; source readback confirms AI defaults to `enabled: false`, and the live OpenAI test requires explicit private opt-in. |
| IDTS-109 candidate authoring | Verified DatDT's personal briefing acknowledgment (`READ`, commit `4b4c93c1d8b45024677653e1f890d52e742b2aaf`, Jira comment `10762`) and authored the English structured-source candidate for Functional Requirements, Development Standards, Message Definition and DatDT-owned Technical Implementation traces. | Candidate complete for DatDT review; not approved, not synchronized to Drive and not marked PASS. | Package: `docs/pm/evidence/idts-109/technical-spec/`. It contains 9 business-level requirement rows, 20 standards rows, a source-scan manifest/catalog, and separate 14-part traces for identity, dashboard/monitoring, notifications/email and all ten AI functions. OpenAI live remains `BLOCKED / NOT ACCEPTED — provider disabled`. Next: DatDT reviews and comments approval/corrections on IDTS-109; DonHV integrates the approved package. |
| Environment blocker | The first AI DevKit lint verification for the candidate could not access the npm registry/cache in the managed sandbox. | Fixed in-session by rerunning the same command with approved elevated network/cache access; candidate files, dependencies and lockfiles were not changed by either command. | First `npx.cmd ai-devkit@latest lint --json` returned npm `EACCES`; elevated retry returned `pass: true`, `5 ok / 0 miss / 0 warn`, exit 0. |
| Environment blocker | Mandatory OfficeCLI preflight was repeated before recording DatDT's candidate approval and preparing the commit handoff. | Still open; only Markdown evidence/checklist updates are permitted in this step, and no XLSX format-validation claim can be made. | `officecli --version` returned `CommandNotFoundException` on 2026-07-31. Remaining owner: environment/tooling setup. |
| IDTS-109 candidate approval | Verified DatDT's personal approval on Jira and recorded it in the candidate README, review checklist and work package. | Candidate approved for commit/handoff; final workbook integration and Drive synchronization remain pending with DonHV. | Jira IDTS-109 comment `10763`, authored by DatDT, explicitly approves commit/merge/DonHV integration and explicitly does not authorize overwriting the official Drive artifact. |
| Documentation-format issue | The first staged `git diff --cached --check` found Markdown hard-break trailing spaces in the candidate README and final blank lines in four candidate files. | Fixed before commit; content and approval evidence were unchanged. | Remove trailing spaces/final blank lines, restage only the IDTS-109 files and rerun the staged whitespace check. |
| IDTS-109 commit/Jira handoff | Committed the approved candidate and added a sanitized factual verification comment to Jira. | Complete for DatDT candidate scope; no push, merge, Jira transition or Drive synchronization was performed. | Commit `9fec741` on `docs/idts-109-drive-doc-review-datdt`; Jira IDTS-109 comment `10764`; 17/17 traces structurally complete, secret scan PASS, AI DevKit lint `5 ok / 0 miss / 0 warn`, and commit whitespace check PASS. Next owner: DonHV for review/merge/final workbook integration. |
| Environment blocker | Mandatory OfficeCLI preflight was repeated before pushing the IDTS-109 documentation branch and creating its PR. | Still open; the PR must retain the OfficeCLI/XLSX validation limitation and must not claim final workbook PASS. | `officecli --version` returned `CommandNotFoundException` on 2026-07-31. Remaining owner: environment/tooling setup. |
| Tooling/auth issue | GitHub CLI is installed, but `gh auth status` reports that the active token for account `Toi-yeu-gai-1con` is invalid. | Partially bypassed: Git credential successfully fetched and pushed the branch, but GitHub CLI cannot create the PR. | `gh` version `2.95.0`; auth check exit 1. `git fetch origin` and `git push -u origin docs/idts-109-drive-doc-review-datdt` succeeded. PR creation requires a signed-in browser session or human CLI re-authentication; do not request or expose a token in chat. |
| Documentation/source-delta issue | After merging the latest `origin/dev`, the approved IDTS-109 candidate became stale in three known-gap statements because IDTS-115 added application UI entry points for classification apply, duplicate confirmation and PM AI operational metrics; IDTS-114 also added a Vercel AI Gateway provider path. | Candidate reconciled before push; post-approval delta confirmation remains open. No Drive update has been made. | The branch had been 109 commits behind `origin/dev` and merged without conflict. Updated TI-AI-08/09/10, message scan/catalog, standards, provider wording and review gaps from current source/evidence. PR must request DatDT delta confirmation and DonHV review. |
| IDTS-109 latest-dev verification | Rechecked the reconciled candidate against the merged IDTS-114/115 source and evidence. | PASS for candidate structure/static source trace; final workbook and provider-primary acceptance remain outside this claim. | `qa:idts115:programmatic` passed 241 checks; 17/17 traces contain all 14 fields; source scan is 96 CAP message sites, 19 throws, 62 UI feedback sites and 485 i18n entries; candidate contains 82 message rows and no Vietnamese diacritics; secret scan and `git diff --check` passed. |
| Browser tooling/auth issue | The first attempt to connect Playwright directly to guessed CDP endpoint `127.0.0.1:9222` returned `ECONNREFUSED`; the correct Codex Browser runtime then opened GitHub successfully but the in-app browser is signed out. | Direct-CDP issue fixed; PR creation remains blocked on human GitHub sign-in. No PR or account setting was created/changed. | Browser shows the GitHub Sign in page for the pushed branch PR URL. DatDT should sign in directly in the in-app browser and notify the agent; never send credentials in chat. |
